# Fase 5.6 (moonshots) — Discovery via graphify (2026-07-28)

## Achado central
O graphify (grafo do backend inteiro, 6631 nós) revelou **7 módulos de IA avançada JÁ ESTRUTURADOS** em `backend/modules/ai/`, cada um com models+schemas+repositories+services+controllers (15-20 .py), mas **100% DORMENTES**: ZERO referências em `main_production.py`, todas as rotas 404. São "features dos grandes" scaffolded e nunca ligadas (padrão já visto em [[project_auditoria_financeiro_desligado]]).

| Módulo | .py | Do que trata | Valor p/ Conecta (segurança patrimonial) |
|---|---|---|---|
| `ai/fraud_detection` | 17 | FraudAlert/Pattern/Rule, RiskProfile, RuleEngine, DetectionRequest | ALTO — detecção de anomalia/fraude em pagamentos (R$100k/dia), folha, PIX; fit perfeito com propor→aprovar (alerta→diretoria, nunca auto-age) |
| `ai/contract_analysis` | 16 | ContractAnalysis, ExtractedClause, ClauseExtractor, ContractAlert | ALTO — extração de cláusulas dos contratos de condomínio; liga ao jurídico + GED |
| `ai/report_generator` | 20 | ReportGeneratorService, templates | MÉDIO — relatórios automáticos |
| `ai/data_quality` | 18 | DataQualityRepository, QualityScoreByEntity | MÉDIO — qualidade do dado (bate com "nunca fabricar") |
| `ai/sentiment_analysis` | 17 | SentimentRepository, SentimentAnalyzer | MÉDIO — sentimento nas conversas WhatsApp (José Luís) |
| `ai/knowledge_base` | 16 | KnowledgeBaseRepository | MÉDIO — base de conhecimento (liga ao 5.4b/5.5) |
| `ai/meeting_assistant` | 15 | Meeting, MeetingAssistantRepository | BAIXO/MÉDIO — assistente de reunião |

Também: `models/prediction.py` + `PredictionService`/`ChurnPredictor` (1 ref no main — parcialmente ligado?).

## Tese da 5.6
Moonshots = **ligar + expandir** esses módulos dormentes, conectando-os à camada cognitiva (consultores/Hermes/sino) + dado real, respeitando TODAS as paredes (nunca fabricar, RBAC, propor→aprovar, humano no loop, dinheiro/legal gated). NÃO recriar — reaproveitar o que já está estruturado.

## ⚠️ A verificar no brainstorm do módulo escolhido
Se os SERVICES são implementação real ou stubs (`pass`/casca). Muda o esforço de "wiring+dado" (se real) para "implementar" (se casca). O graphify mostrou ESTRUTURA, não profundidade.

## Decomposição (cada módulo = 1 sub-projeto spec→plano→SDD)
5.6 é um PROGRAMA. Atacar UM por vez. Recomendação de primeiro alvo: **fraud_detection** (mais completo + maior valor p/ empresa que move dinheiro + fit limpo com propor→aprovar) OU **contract_analysis** (valor claro, liga jurídico/GED). Decisão de prioridade = Jordan.
