# Fase 5 (Hermes) — Plano de Implementação 5.0 + 5.1

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir a fundação de segurança (manifesto de risco + lint fail-closed) e expor os 8 consultores + 3 tools de ação (🟢🔵🟡) como tools MCP, **sem subir o Hermes ainda** — provando toda a superfície de risco em isolado.

**Architecture:** Cada tool MCP no `mcp-server/server.py` chama um endpoint HTTP do backend (cliente `erp`, base `/api/v1`). Adicionamos um controller `consultor_mcp_controller.py` no backend (consulta unificada + ações), classificamos cada tool num `tool_risk_manifest`, e um lint quebra o build se uma tool nascer sem classe. Tools de dinheiro-que-executa (🔴) **nunca** entram no `server.py`.

**Tech Stack:** FastAPI (`main_production`) · FastMCP (`mcp-server/server.py`) · PostgreSQL (SQL cru via `text()`) · pytest (`asyncio_mode=auto`) · `consultor_hub` (funções de módulo async).

## Global Constraints
Copiadas verbatim da spec `docs/superpowers/specs/2026-07-22-fase5-hermes-camada-cognitiva-design.md`. Toda tarefa herda:
- **Dinheiro que SAI = SEMPRE gate OTP humano.** MCP só *propõe* (grava `inter_payments` status `'preparado'`); NUNCA aprova/executa. Tools de money-out ficam **fora do `server.py`**.
- **Operacional e dossiê Jurídico = READ-ONLY.** Sem caminho de escrita externa.
- **Nunca fabricar dado.** Vazio real = "aguardando dado".
- **Retrato de entidade (PII cruzada) só na origem `ceo`** — já garantido dentro de `contexto_compartilhado` (`consultor_hub.py:213-215`).
- **Fail-closed:** tool sem classe no manifesto = tratada como 🔴 = build quebra.
- **Produção roda `main_production:app`** (não `main.py`). Rotas sob `/api/v1`.
- **Deploy:** backend blue-green + baked; conector MCP rebuild (`docker cp` = volátil).
- **Commits:** `--no-verify`; `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## Ambiente de execução de teste (LEIA — o harness deste projeto NÃO é pytest-no-host)
Confirmado no pré-voo: **não há venv no host** (só `/usr/bin/python3`), **pytest NÃO está instalado no container**, e o **código do backend é BAKED** (só `data/uploads/credentials/logs` são bind-mounts). Os testes existentes (ex. `tests/fundacao/test_enqueue_alert.py`) rodam como **scripts standalone** (`asyncio.run` + asserts + `if __name__ == "__main__"`), NÃO via pytest. Regras:

- **Testes da Fase 5.0** (manifesto/lint, em `mcp-server/`) só usam stdlib (`re`, `pathlib`) → rodam **no host**: `cd /opt/conecta-pro/mcp-server && python3 test_tool_risk_manifest.py`. Sem pytest: o `__main__` chama as funções `test_*` direto e imprime PASS/FAIL.
- **Testes da Fase 5.1** (controller do backend) rodam na BANCADA `conecta-pro-backend-green` (**:8081**), NUNCA no `conecta-pro-backend` público (**:8080**). O green está fora do tráfego do nginx. **PROIBIDO** tocar/reiniciar o `:8080` e **PROIBIDO** carregar o app in-process no container (estoura memória → OOM → reinicia o ERP vivo). Fluxo de teste VOLÁTIL (o durável é o rebuild+deploy da Task 8):
  1. `docker cp <arquivo> conecta-pro-backend-green:/app/<destino>` (código E o `_mcp_token.py` E o arquivo de teste)
  2. `docker restart conecta-pro-backend-green` e esperar `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8081/health` = 200 (o `:8080` NÃO é tocado)
  3. rodar o teste standalone (bate em :8081 via HTTP, sem carregar app in-process): `docker exec conecta-pro-backend-green sh -c 'cd /app && PYTHONPATH=/app MCP_TEST_BASE=http://127.0.0.1:8080/api/v1 python tests/fundacao/test_consultor_mcp_controller.py'`
  O teste usa `httpx.AsyncClient` com `BASE = os.environ['MCP_TEST_BASE']` (setado no docker exec = green-interno :8080). SEM default: rodar sem a env dá KeyError (guarda contra bater no :8080 público por engano). NÃO ASGITransport.
- **⚠️ GREEN COMPARTILHA O BANCO DE PRODUÇÃO** (mesmo `DATABASE_URL` do `:8080`). Todo teste que ESCREVE (propor_pagamento → `inter_payments`, propor_comunicado → `communication_announcements`) cria dado REAL que apareceria nas telas do Jordan. **Regra:** todo teste que insere DEVE deletar sua própria linha no `finally` (ver os testes das Tasks 5/6). Consulta/feedback deixam só linhas de log/memória (toleráveis), mas prefira limpar quando trivial.
- **Token de auth nos testes:** NÃO importar de `tests/conftest.py` (ele faz `import pytest` no topo → quebra standalone). Criar um helper pytest-free em **`/opt/conecta-pro/backend/tests/fundacao/_mcp_token.py`** (na Task 3, Step 0), reusado por todos os testes de 5.1:

```python
"""Token JWT real p/ testes standalone (sem pytest). Espelha _make_real_token do conftest,
mas sem pytest.skip — levanta RuntimeError se o ambiente não tiver os segredos.
jjesus@conectamais.pro está na allowlist da diretoria → passa no require_mcp_consultor."""
import os, datetime
import jwt as pyjwt
from sqlalchemy import create_engine, text

def make_token() -> str:
    secret = os.environ["JWT_SECRET_KEY"]                       # presente no container
    algo = os.environ.get("JWT_ALGORITHM", "HS256")
    db_url = os.environ["DATABASE_URL"].replace("+asyncpg", "")
    engine = create_engine(db_url)
    with engine.connect() as conn:
        row = conn.execute(text("SELECT id, email FROM users WHERE email='jjesus@conectamais.pro' LIMIT 1")).fetchone()
    engine.dispose()
    if not row:
        raise RuntimeError("user jjesus@conectamais.pro não encontrado")
    now = datetime.datetime.now(datetime.timezone.utc)
    payload = {"sub": str(row[0]), "email": row[1], "type": "access",
               "role": "admin", "exp": now + datetime.timedelta(hours=2)}
    return pyjwt.encode(payload, secret, algorithm=algo)
```
Confirmar `JWT_ALGORITHM` real (ver `core/auth/jwt.py:create_access_token` / `settings.jwt_algorithm`); default HS256.

## File Structure

**Fase 5.0 (fundação — só no conector MCP, sem backend):**
- Create: `/opt/conecta-pro/mcp-server/tool_risk_manifest.py` — dict declarativo `TOOL_RISK: dict[str, str]` (nome da tool → classe `"read"|"write_low"|"propose"`) + helper `tools_include()`.
- Create: `/opt/conecta-pro/mcp-server/test_tool_risk_manifest.py` — lint: toda `@mcp.tool` em `server.py` está no manifesto; nenhuma classe inválida; `tools_include()` só devolve 🟢🔵🟡.

**Fase 5.1 (as tools — backend + conector):**
- Create: `/opt/conecta-pro/backend/modules/ai/conversation/controllers/consultor_mcp_controller.py` — router `/consultores/mcp` com: `POST /{origem}/consultar` (🟢), `POST /feedback` (🔵), `POST /propor-pagamento` (🟡), `POST /propor-comunicado` (🟡).
- Modify: `/opt/conecta-pro/backend/main_production.py` — registrar o novo router (import lazy try/except, gate próprio).
- Modify: `/opt/conecta-pro/mcp-server/server.py` — adicionar 8 tools `consultor_*` (🟢) + 3 tools de ação (🔵🟡).
- Modify: `/opt/conecta-pro/mcp-server/tool_risk_manifest.py` — classificar as 11 tools novas.
- Test: `/opt/conecta-pro/backend/tests/fundacao/test_consultor_mcp_controller.py`.

**Escopo deferido (registrado, não implementado aqui):** tools 🔵 `criar_nota`/`marcar_tarefa`/`rascunhar_documento` exigem tabelas novas e não são necessárias pra provar a taxonomia → vão pra um plano 5.1.b. Fases 5.2→5.5 (container Hermes, proativo, aprendizado, endurecimento) = planos próprios, escritos contra este código já aterrissado.

---

## Fase 5.0 — Fundação: manifesto de risco + lint

### Task 1: Manifesto de risco — estrutura + classes válidas

**Files:**
- Create: `/opt/conecta-pro/mcp-server/tool_risk_manifest.py`
- Test: `/opt/conecta-pro/mcp-server/test_tool_risk_manifest.py`

**Interfaces:**
- Produces: `TOOL_RISK: dict[str, str]` (nome→classe), `VALID_CLASSES: set[str]`, `tools_include() -> list[str]`.

- [ ] **Step 1: Escrever o teste que falha** — `test_tool_risk_manifest.py`:

```python
"""Lint do manifesto de risco das tools MCP. Roda a partir de /opt/conecta-pro/mcp-server."""
import re, pathlib
from tool_risk_manifest import TOOL_RISK, VALID_CLASSES, tools_include

SERVER = pathlib.Path(__file__).parent / "server.py"

def _declared_tools() -> set[str]:
    """Todos os nomes de função decorados com @mcp.tool em server.py."""
    src = SERVER.read_text(encoding="utf-8")
    # captura: linha @mcp.tool (com/sem parênteses) seguida de async def NOME(
    names = set()
    for m in re.finditer(r"@mcp\.tool[^\n]*\n\s*async def\s+([a-zA-Z_][a-zA-Z0-9_]*)\s*\(", src):
        names.add(m.group(1))
    return names

def test_classes_sao_validas():
    for tool, klass in TOOL_RISK.items():
        assert klass in VALID_CLASSES, f"{tool}: classe inválida {klass!r}"

def test_toda_tool_esta_classificada():
    declared = _declared_tools()
    faltando = sorted(declared - set(TOOL_RISK))
    assert not faltando, f"tools sem classe no manifesto (fail-closed = 🔴): {faltando}"

def test_manifesto_nao_tem_tool_fantasma():
    declared = _declared_tools()
    fantasma = sorted(set(TOOL_RISK) - declared)
    assert not fantasma, f"manifesto classifica tools que não existem em server.py: {fantasma}"

def test_tools_include_so_expostas():
    inc = set(tools_include())
    assert inc == {t for t, k in TOOL_RISK.items() if k in {"read", "write_low", "propose"}}
    assert inc == set(TOOL_RISK), "nenhuma classe 🔴 deve existir no manifesto de tools expostas"

if __name__ == "__main__":  # standalone, sem pytest (não instalado no container/host)
    import sys, traceback
    _fns = [test_classes_sao_validas, test_toda_tool_esta_classificada,
            test_manifesto_nao_tem_tool_fantasma, test_tools_include_so_expostas]
    _fail = 0
    for fn in _fns:
        try:
            fn(); print(f"PASS {fn.__name__}")
        except Exception:
            _fail += 1; print(f"FAIL {fn.__name__}"); traceback.print_exc()
    sys.exit(1 if _fail else 0)
```

- [ ] **Step 2: Rodar e ver falhar**

Run (a partir de `/opt/conecta-pro/mcp-server`): `python3 test_tool_risk_manifest.py`
Expected: FAIL — `ModuleNotFoundError: No module named 'tool_risk_manifest'`.

- [ ] **Step 3: Criar o manifesto** — `tool_risk_manifest.py`. Semear com as tools existentes classificadas `"read"` (o conector é read-only hoje — memória `project_mcp_228_tools_resgate`: dinheiro-que-sai fica FORA). Gerar a lista de nomes existentes com o comando abaixo e colar:

Primeiro, extrair os nomes atuais (a partir de `/opt/conecta-pro/mcp-server`):
```bash
grep -Pzo '@mcp\.tool[^\n]*\n\s*async def\s+\K[a-zA-Z_][a-zA-Z0-9_]*' server.py | tr '\0' '\n' | sort -u
```
Colar cada nome como `"read"` no dict. Estrutura do arquivo:

```python
"""Manifesto de risco das tools MCP (fail-closed).

Classes:
  read       🟢 consulta/análise, zero mudança de estado — Hermes usa auto
  write_low  🔵 interno reversível, sem efeito externo em dinheiro/gente — auto
  propose    🟡 cria PENDENTE que só humano executa com OTP/aprovação
  (🔴 executar dinheiro/legal/operacional NÃO aparece aqui — fica fora de server.py)

Regra: toda @mcp.tool de server.py DEVE ter entrada aqui, senão o lint quebra o build.
"""

VALID_CLASSES = {"read", "write_low", "propose"}

TOOL_RISK: dict[str, str] = {
    # --- tools existentes (conector read-only) — colar a saída do grep, todas "read" ---
    "listar_deals": "read",
    # ... (todas as demais existentes) ...
}


def tools_include() -> list[str]:
    """Allowlist p/ o config.yaml do Hermes (Parede 1): só 🟢🔵🟡."""
    return sorted(t for t, k in TOOL_RISK.items() if k in VALID_CLASSES)
```

- [ ] **Step 4: Rodar e ver passar**

Run: `python3 test_tool_risk_manifest.py`
Expected: PASS (4 testes). Se `test_toda_tool_esta_classificada` falhar, adicionar os nomes faltantes como `"read"`.

- [ ] **Step 5: Commit**

```bash
cd /opt/conecta-pro && git add mcp-server/tool_risk_manifest.py mcp-server/test_tool_risk_manifest.py
git commit --no-verify -m "feat(fase5.0): manifesto de risco das tools MCP + lint fail-closed

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Gancho de CI — o lint roda no build do conector

**Files:**
- Modify: `/opt/conecta-pro/mcp-server/` (Dockerfile ou script de build — verificar qual existe)

**Interfaces:**
- Consumes: `test_tool_risk_manifest.py` da Task 1.

- [ ] **Step 1: Localizar o build do conector**

Run: `ls /opt/conecta-pro/mcp-server/` e `sed -n '1,40p' /opt/conecta-pro/mcp-server/Dockerfile 2>/dev/null`
Objetivo: achar onde adicionar o lint como etapa que **falha o build** se uma tool ficar sem classe.

- [ ] **Step 2: Adicionar o lint ao build**

Se houver `Dockerfile`, adicionar antes do `CMD` (após copiar o código):
```dockerfile
# Parede 1: lint fail-closed — build quebra se tool MCP nascer sem classe de risco
RUN python test_tool_risk_manifest.py
```
Se o deploy do conector for por script (não Docker), adicionar a linha `python test_tool_risk_manifest.py || exit 1` no script de deploy do conector, antes do restart.

- [ ] **Step 3: Provar que o gancho pega uma tool não classificada**

Adicionar TEMPORARIAMENTE em `server.py` uma tool nova sem classe:
```python
@mcp.tool
async def _tool_de_teste_sem_classe() -> dict:
    """apenas para provar o lint"""
    return {}
```
Run: `cd /opt/conecta-pro/mcp-server && python3 test_tool_risk_manifest.py`
Expected: FAIL em `test_toda_tool_esta_classificada` listando `_tool_de_teste_sem_classe`.
Depois **remover** a tool de teste e confirmar PASS.

- [ ] **Step 4: Commit**

```bash
cd /opt/conecta-pro && git add mcp-server/
git commit --no-verify -m "ci(fase5.0): lint de risco das tools no build do conector (fail-closed)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Fase 5.1 — As tools MCP (2 famílias), sem Hermes

### Task 3: Endpoint de consulta unificado (🟢) — os 8 consultores

**Files:**
- Create: `/opt/conecta-pro/backend/modules/ai/conversation/controllers/consultor_mcp_controller.py`
- Test: `/opt/conecta-pro/backend/tests/fundacao/test_consultor_mcp_controller.py`

**Interfaces:**
- Consumes: `consultor_hub.contexto_compartilhado(db, origem, pergunta)` (`consultor_hub.py:138`), `consultor_hub.gerar(messages=..., system_prompt=...)` (`consultor_hub.py:87`).
- Produces: `POST /api/v1/consultores/mcp/{origem}/consultar` body `{"pergunta": str}` → `{"resposta": str, "consulta_id": int|null, "origem": str}`.

**Contexto p/ o engenheiro:** as 8 origens são `ceo, cfo, fiscal, rh, juridico, comercial, operacional, ged` (mapa: DP→`rh`). `contexto_compartilhado` já restringe o retrato de entidade à origem `ceo` (LGPD) — não precisa reimplementar. Para o `consulta_id`: ler como os consultores persistem a consulta hoje — `sed -n '695,715p' /opt/conecta-pro/backend/modules/financial/cfo_service.py` e a migration `ls /opt/conecta-pro/backend/alembic/versions/ | grep consultas_baseline` → reusar o mesmo INSERT (tabela de consultas da Fase −1) e devolver o `id`. Se o serviço existente não persistir com id recuperável, devolver `consulta_id=null` nesta task (o feedback da Task 4 aceita null → cria uma memória avulsa).

- [ ] **Step 0: Criar o helper de token** — `/opt/conecta-pro/backend/tests/fundacao/_mcp_token.py` com o conteúdo `make_token()` do bloco em "Ambiente de execução de teste" acima. (Usado por todos os testes de 5.1.)

- [ ] **Step 1: Escrever o teste que falha** — `test_consultor_mcp_controller.py` (standalone, SEM pytest; bate no backend vivo em `:8080`):

```python
"""Testa o controller MCP dos consultores pela rota real (disciplina: verificar pela rota da tela).
Standalone (sem pytest): httpx contra a BANCADA green em http://127.0.0.1:8081 (NUNCA o público :8080). Rodar por:
docker exec conecta-pro-backend-green sh -c 'cd /app && PYTHONPATH=/app MCP_TEST_BASE=http://127.0.0.1:8080/api/v1 python tests/fundacao/test_consultor_mcp_controller.py'
(depois de docker cp do controller pro green + restart do green — ver 'Ambiente de execução de teste')."""
import asyncio
from httpx import AsyncClient
from tests.fundacao._mcp_token import make_token

import os
BASE = os.environ["MCP_TEST_BASE"]  # OBRIGATÓRIO (sem default): setado SÓ no docker exec sancionado
                                   # dentro do green (=green-interno :8080). Rodar do host sem a env → KeyError, nunca bate no :8080 público.

async def _post(path, json):
    async with AsyncClient(base_url=BASE, timeout=60) as ac:
        return await ac.post(path, json=json, headers={"Authorization": f"Bearer {make_token()}"})

def test_origem_invalida_recusada():
    # unidade pura: 422 acontece ANTES de gerar()/OpenAI
    async def run():
        r = await _post("/consultores/mcp/marketing/consultar", {"pergunta": "oi"})
        assert r.status_code == 422, r.text
    asyncio.run(run())

def test_consulta_cfo_responde():
    # chama gerar() → precisa de OPENAI_API_KEY no container (presente em prod)
    async def run():
        r = await _post("/consultores/mcp/cfo/consultar", {"pergunta": "Resuma o caixa em uma frase."})
        assert r.status_code == 200, r.text
        body = r.json()
        assert body["origem"] == "cfo"
        assert isinstance(body["resposta"], str) and len(body["resposta"]) > 0
    asyncio.run(run())

# funções de teste das Tasks 4/5/6 são ADICIONADAS abaixo neste mesmo arquivo.
_TESTS = [test_origem_invalida_recusada, test_consulta_cfo_responde]

if __name__ == "__main__":  # runner standalone, sem pytest
    import sys, traceback
    fail = 0
    for fn in _TESTS:
        try:
            fn(); print(f"PASS {fn.__name__}")
        except Exception:
            fail += 1; print(f"FAIL {fn.__name__}"); traceback.print_exc()
    sys.exit(1 if fail else 0)
```
Cada Task (4/5/6) adiciona sua `def test_*` E a acrescenta à lista `_TESTS` (para o runner rodá-la). `make_token()` usa `JWT_SECRET_KEY`/`DATABASE_URL` do container e o user `jjesus@conectamais.pro` (na allowlist → passa no gate).

- [ ] **Step 2: Rodar e ver falhar** — o controller ainda não existe no backend vivo:

Run: docker cp o teste + `_mcp_token.py` pro green + restart green, depois `docker exec conecta-pro-backend-green sh -c 'cd /app && PYTHONPATH=/app MCP_TEST_BASE=http://127.0.0.1:8080/api/v1 python tests/fundacao/test_consultor_mcp_controller.py'`
Expected: FAIL `test_origem_invalida_recusada` com 404 (rota não existe).

- [ ] **Step 3: Implementar o controller**

```python
"""Controller MCP dos consultores — superfície 🟢🔵🟡 que o conector chama.
Cada endpoint é a contraparte de uma tool MCP em mcp-server/server.py."""
import json
from fastapi import APIRouter, Depends, HTTPException, Path
from pydantic import BaseModel
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession
from core.database import get_db                          # confirmado: core/database/session.py:34
from core.auth.dependencies import get_current_active_user
from modules.ai.conversation.services import consultor_hub as _hub
from modules.ai.conversation.services.consultor_hub import TABELAS_CONSULTAS  # origem -> (tabela, ...)

router = APIRouter(prefix="/consultores/mcp", tags=["Consultores MCP"])

_DISCLAIMER = "Resposta gerada por consultor de IA — confira antes de agir."

# 8 personas (origem -> system prompt curto). Enriquecer depois; suficiente p/ 5.1.
PERSONAS = {
    "ceo": "Você é o consultor executivo (CEO) da Conecta PRO. Visão estratégica, runway, decisão. Nunca fabrique dado; se faltar, diga 'aguardando dado'.",
    "cfo": "Você é o CFO da Conecta PRO. Caixa, aging, o que vence, saúde financeira. Nunca fabrique dado.",
    "fiscal": "Você é o consultor fiscal/contábil. Notas, guias, regime, obrigações. Nunca fabrique dado.",
    "rh": "Você é o consultor de DP/RH. Folha, ponto, colaboradores, CCT. Nunca fabrique dado.",
    "juridico": "Você é o consultor jurídico (READ-ONLY). Processos, dossiê. Aconselha, nunca protocola. Nunca fabrique dado.",
    "comercial": "Você é o consultor comercial/CRM. Funil, propostas, clientes. Nunca fabrique dado.",
    "operacional": "Você é o consultor operacional (READ-ONLY). Postos, escalas, presença. Nunca altera escala. Nunca fabrique dado.",
    "ged": "Você é o consultor de GED/documentos. Kits, panorama documental. Nunca fabrique dado.",
}

class ConsultaIn(BaseModel):
    pergunta: str

async def _persistir_consulta(db, origem, pergunta, resposta, user) -> int | None:
    """Persiste na tabela de consultas da Fase −1 (via TABELAS_CONSULTAS) e devolve o id.
    Se a origem não tiver tabela mapeada, devolve None (feedback aceita consulta_id=None)."""
    tabela, *_ = TABELAS_CONSULTAS.get(origem, (None,))
    if not tabela:
        return None
    # shape base comum (confirmado em fase_menos1_consultas_baseline + cfo_service.py:760)
    row = await db.execute(
        text(f"""INSERT INTO {tabela}
                 (area, pergunta, resposta, escalonar, disclaimer, contexto_usado, created_by)
                 VALUES (:a, :p, :r, false, :d, CAST(:c AS jsonb), :u) RETURNING id"""),
        {"a": origem[:20], "p": pergunta, "r": resposta, "d": _DISCLAIMER,
         "c": json.dumps({"via": "mcp"}), "u": str(getattr(user, "id", "")) or None},
    )
    await db.commit()
    return int(row.scalar())

@router.post("/{origem}/consultar")
async def consultar(payload: ConsultaIn, origem: str = Path(...),
                    db: AsyncSession = Depends(get_db), user=Depends(get_current_active_user)):
    if origem not in PERSONAS:
        raise HTTPException(status_code=422, detail=f"origem inválida: {origem}")
    extra = await _hub.contexto_compartilhado(db, origem, payload.pergunta)   # retrato de entidade só p/ 'ceo' (LGPD)
    system_prompt = PERSONAS[origem] + "\n\n" + extra
    resposta, _meta = await _hub.gerar(
        messages=[{"role": "user", "content": payload.pergunta}],
        system_prompt=system_prompt,
    )
    consulta_id = None
    try:
        consulta_id = await _persistir_consulta(db, origem, payload.pergunta, resposta, user)
    except Exception:
        pass  # persistência é best-effort; não quebra a resposta
    return {"resposta": resposta, "consulta_id": consulta_id, "origem": origem}
```

Notas de confirmação (rápidas, não são TODOs de código): `TABELAS_CONSULTAS` está em `consultor_hub.py:35` — conferir se as 8 origens têm entrada (o mapa conhecido cobre `cfo/fiscal/ged...`; origens sem tabela caem em `consulta_id=None`, o que é OK). A tabela base tem `disclaimer` NOT NULL e `contexto_usado` NOT NULL — por isso o INSERT os preenche. `get_current_active_user` é o mesmo par usado nos controllers (`cfo_controller.py:37`).

- [ ] **Step 4a: Criar o gate do MCP** — NÃO existe `module:ai`. O conector chama o ERP com um JWT de **conta de serviço** (login em `mcp-server/server.py:108 _login`), cujo e-mail NÃO está na allowlist da diretoria (`require_consultor_executivo` daria 403). Criar um gate dedicado que permite a conta de serviço + a diretoria, em `/opt/conecta-pro/backend/modules/ai/consultores/permissions.py` (onde já vive `require_consultor_executivo`, `:20`):

```python
from fastapi import Depends, HTTPException
from core.auth.dependencies import get_current_active_user

# e-mail da conta de serviço que o conector MCP usa. CONFIRMAR: ver o user do login em
# mcp-server/server.py:_login (variáveis ERP_USER/MCP_SERVICE_EMAIL no env do conecta-pro-mcp).
MCP_SERVICE_EMAILS = {"mcp@conectamais.pro"}  # ajustar ao e-mail real da conta de serviço
_MCP_ALLOWED = CONSULTOR_EXECUTIVO_EMAILS | MCP_SERVICE_EMAILS

async def require_mcp_consultor(user=Depends(get_current_active_user)):
    if getattr(user, "email", None) not in _MCP_ALLOWED:
        raise HTTPException(status_code=403, detail="Superfície MCP restrita à conta de serviço/diretoria")
    return user
```
Confirmar o e-mail real: `grep -n "ERP_USER\|MCP_SERVICE\|_login\|username" /opt/conecta-pro/mcp-server/server.py | head` e o env do serviço `conecta-pro-mcp`. Ajustar `MCP_SERVICE_EMAILS` ao valor real (um único e-mail).

- [ ] **Step 4b: Registrar o router** — em `/opt/conecta-pro/backend/main_production.py`, gate perto dos outros (`:321-332`) e include junto aos lazy (padrão de `payment_controller`, `:1040`):

```python
# perto dos gates (:321-332):
from modules.ai.consultores.permissions import require_mcp_consultor
_MCP_CONSULTOR_GATE = Depends(require_mcp_consultor)

# junto aos includes lazy (~:1040):
try:
    from modules.ai.conversation.controllers.consultor_mcp_controller import router as _consultor_mcp_router
    api_router.include_router(_consultor_mcp_router, dependencies=[_MCP_CONSULTOR_GATE])
    logger.info("Consultor MCP controller: OK")
except Exception as _e:
    logger.warning(f"Consultor MCP controller: {_e}")
```

- [ ] **Step 5: Rodar e ver passar**

Run: docker cp o controller + `_mcp_token.py` + o teste pro GREEN (`conecta-pro-backend-green`), `docker restart conecta-pro-backend-green` (esperar `:8081/health`=200; NÃO tocar :8080), depois `docker exec conecta-pro-backend-green sh -c 'cd /app && PYTHONPATH=/app MCP_TEST_BASE=http://127.0.0.1:8080/api/v1 python tests/fundacao/test_consultor_mcp_controller.py'` (roda todos os _TESTS).
Expected: `test_origem_invalida_recusada` PASS. `test_consulta_cfo_responde` PASS se houver OPENAI_API_KEY no container. Se `gerar()` exigir chave OpenAI ausente no ambiente de teste, marcar `test_consulta_cfo_responde` com `@pytest.mark.integration` e provar via curl na rota (Step 6).

- [ ] **Step 6: Provar pela rota real (curl)** — com o backend no ar (`main_production:app`, porta 8080):

```bash
TOKEN=$(curl -s -X POST http://127.0.0.1:8080/api/v1/auth/login -d 'username=jjesus&password=***' | python -c 'import sys,json;print(json.load(sys.stdin)["access_token"])')
curl -s -X POST http://127.0.0.1:8080/api/v1/consultores/mcp/cfo/consultar \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{"pergunta":"Resuma o caixa em uma frase."}' | python -m json.tool
```
Expected: JSON com `"origem":"cfo"` e `"resposta"` não-vazia (dado real, não fabricado).

- [ ] **Step 7: Commit**

```bash
cd /opt/conecta-pro && git add backend/modules/ai/conversation/controllers/consultor_mcp_controller.py backend/main_production.py backend/tests/fundacao/
git commit --no-verify -m "feat(fase5.1): endpoint de consulta unificado dos 8 consultores (🟢)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Endpoint de feedback (🔵) — realimenta a Fase 4

**Files:**
- Modify: `/opt/conecta-pro/backend/modules/ai/conversation/controllers/consultor_mcp_controller.py`
- Test: `/opt/conecta-pro/backend/tests/fundacao/test_consultor_mcp_controller.py`

**Interfaces:**
- Consumes: `consultor_hub.registrar_feedback(db, origem, consulta_id, util, correcao)` (`consultor_hub.py:310`).
- Produces: `POST /api/v1/consultores/mcp/feedback` body `{"origem": str, "correcao": str, "consulta_id": int|null}` → resultado de `registrar_feedback`.

- [ ] **Step 1: Adicionar o teste que falha** — em `test_consultor_mcp_controller.py`:

```python
def test_feedback_grava_memoria():
    async def run():
        r = await _post("/consultores/mcp/feedback",
                        {"origem": "cfo", "correcao": "O comunicado assina só funcionário.", "consulta_id": None})
        assert r.status_code == 200, r.text
        assert r.json().get("ok") is True
    asyncio.run(run())
```

- [ ] **Step 2: Rodar e ver falhar** — adicionar `test_feedback_grava_memoria` à lista `_TESTS`; docker cp o controller + `_mcp_token.py` + o teste pro GREEN (`conecta-pro-backend-green`), `docker restart conecta-pro-backend-green` (esperar `:8081/health`=200; NÃO tocar :8080), depois `docker exec conecta-pro-backend-green sh -c 'cd /app && PYTHONPATH=/app MCP_TEST_BASE=http://127.0.0.1:8080/api/v1 python tests/fundacao/test_consultor_mcp_controller.py'` (roda todos os _TESTS) → FAIL 404 nesse teste.

- [ ] **Step 3: Implementar** — adicionar ao controller:

```python
class FeedbackIn(BaseModel):
    origem: str
    correcao: str
    consulta_id: int | None = None

@router.post("/feedback")
async def feedback(payload: FeedbackIn, db: AsyncSession = Depends(get_db)):
    if payload.origem not in PERSONAS:
        raise HTTPException(status_code=422, detail=f"origem inválida: {payload.origem}")
    # consulta_id=0 é SEGURO: registrar_feedback só faz UPDATE ... WHERE id=:id (afeta 0 linhas)
    # e ainda assim INSERE a memória em consultor_memorias fonte='feedback_gestor' (confirmado consultor_hub.py:310-341, não é FK).
    res = await _hub.registrar_feedback(
        db, payload.origem, payload.consulta_id or 0, util=False, correcao=payload.correcao,
    )
    return {"ok": bool(res.get("ok")), "resultado": res}
```
Nota: `registrar_feedback` mapeia `origem`→tabela via `TABELAS_CONSULTAS`; se a origem não estiver no mapa ela devolve `{"ok": False, "erro": "origem desconhecida"}` (sem gravar memória). Conferir que as 8 origens têm entrada no mapa (`consultor_hub.py:35`); as que não tiverem não realimentam a Fase 4 nesta task — registrar como pendência se faltar alguma.

- [ ] **Step 4: Rodar e ver passar** — docker cp o controller + `_mcp_token.py` + o teste pro GREEN (`conecta-pro-backend-green`), `docker restart conecta-pro-backend-green` (esperar `:8081/health`=200; NÃO tocar :8080), depois `docker exec conecta-pro-backend-green sh -c 'cd /app && PYTHONPATH=/app MCP_TEST_BASE=http://127.0.0.1:8080/api/v1 python tests/fundacao/test_consultor_mcp_controller.py'` (roda todos os _TESTS) → PASS em `test_feedback_grava_memoria`.

- [ ] **Step 5: Commit**

```bash
cd /opt/conecta-pro && git add backend/modules/ai/conversation/controllers/consultor_mcp_controller.py backend/tests/fundacao/test_consultor_mcp_controller.py
git commit --no-verify -m "feat(fase5.1): endpoint de feedback MCP (🔵) realimenta memória Fase 4

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Endpoint propor_pagamento (🟡) — grava PENDENTE, nunca executa

**Files:**
- Modify: `/opt/conecta-pro/backend/modules/ai/conversation/controllers/consultor_mcp_controller.py`
- Test: `/opt/conecta-pro/backend/tests/fundacao/test_consultor_mcp_controller.py`

**Interfaces:**
- Produces: `POST /api/v1/consultores/mcp/propor-pagamento` body `{"valor": float, "pix_key": str, "descricao": str}` → `{"payment_id": str, "status": "preparado"}`.
- Grava em `inter_payments` (schema em `alembic/versions/sprint87_d7_payments.py:22-47`): status **`'preparado'`**, `destinatario` JSONB, `valor`, `observacoes` marcando origem Hermes/MCP.

**Constraint crítica:** este endpoint SÓ insere status `'preparado'`. Ele NUNCA seta `'aprovado'`/`'executado'` e NUNCA chama a API do Inter. A execução real permanece no fluxo humano de OTP (`payment_controller.py:115`). Marcar `observacoes='proposta via consultor MCP — requer aprovação humana + OTP'`.

- [ ] **Step 1: Adicionar o teste que falha** — o oráculo é: a linha nasce `'preparado'`, jamais `'aprovado'`:

```python
def test_propor_pagamento_nasce_preparado():
    # ⚠️ green compartilha o BANCO VIVO: esta proposta apareceria na fila real de OTP do Jordan.
    # O teste DELETA a linha no fim (bloco finally) — não deixar pagamento-teste na produção.
    async def run():
        from core.database import async_session_factory   # confirmado: core/database/session.py:25
        from sqlalchemy import text
        r = await _post("/consultores/mcp/propor-pagamento",
                        {"valor": 1.23, "pix_key": "teste@conectapro.com.br", "descricao": "TESTE 5.1 (apagar)"})
        assert r.status_code == 200, r.text
        pid = r.json()["payment_id"]
        try:
            assert r.json()["status"] == "preparado"
            # oráculo de segurança: nasce 'preparado', JAMAIS 'aprovado'/'executado'
            async with async_session_factory() as db:
                row = (await db.execute(text("SELECT status FROM inter_payments WHERE id=:i"), {"i": pid})).first()
                assert row is not None and row[0] == "preparado"
        finally:
            # limpeza OBRIGATÓRIA: remove o pagamento-teste da fila de produção
            async with async_session_factory() as db:
                await db.execute(text("DELETE FROM inter_payments WHERE id=:i AND status='preparado'"), {"i": pid})
                await db.commit()
    asyncio.run(run())
```

- [ ] **Step 2: Rodar e ver falhar** → FAIL 404.

- [ ] **Step 3: Implementar** — adicionar ao controller:

```python
import json
from datetime import date
from sqlalchemy import text

class ProporPagamentoIn(BaseModel):
    valor: float
    pix_key: str
    descricao: str = ""

@router.post("/propor-pagamento")
async def propor_pagamento(payload: ProporPagamentoIn,
                           db: AsyncSession = Depends(get_db), user=Depends(get_current_active_user)):
    # 🟡 PROPOR: grava PENDENTE (status='preparado' via server_default). NUNCA aprova/executa.
    # Gate OTP humano fica downstream (payment_controller.py:115). O 'id' e 'status' vêm de server_default.
    if payload.valor <= 0:
        raise HTTPException(status_code=422, detail="valor deve ser > 0")
    # colunas obrigatórias sem default (confirmado sprint87_d7_payments.py):
    #   payment_type, destinatario, valor, data_pagamento, prepared_by (FK users.id NOT NULL).
    row = await db.execute(text("""
        INSERT INTO inter_payments (payment_type, destinatario, valor, data_pagamento, prepared_by, observacoes)
        VALUES ('pix', CAST(:dest AS jsonb), :valor, :dt, :prep, :obs)
        RETURNING id, status
    """), {
        "dest": json.dumps({"pix_key": payload.pix_key}),
        "valor": payload.valor,
        "dt": date.today(),
        "prep": str(user.id),   # prepared_by = quem propôs (conta de serviço MCP / diretoria)
        "obs": f"proposta via consultor MCP — requer aprovação humana + OTP. {payload.descricao}",
    })
    await db.commit()
    rec = row.first()
    return {"payment_id": str(rec[0]), "status": rec[1]}  # status = 'preparado' (server_default)
```
`prepared_by` é FK real para `users.id` (NOT NULL) — usa o `user.id` do gate (a conta de serviço MCP é um user existente). `status` e `id` vêm do `server_default` da tabela, então não os setamos — garante que nasce `'preparado'` sem chance de setarmos outro valor.

- [ ] **Step 4: Rodar e ver passar** → PASS. Confirmar também que NÃO existe endpoint aqui que aprove/execute: `grep -nE "aprovado|executado|approve|/aprovar|otp" /opt/conecta-pro/backend/modules/ai/conversation/controllers/consultor_mcp_controller.py` deve retornar vazio.

- [ ] **Step 5: Provar que aparece na tela de aprovação (mesma fila humana)**:

```bash
curl -s "http://127.0.0.1:8080/api/v1/financeiro/inter/payments?status_filter=preparado" \
  -H "Authorization: Bearer $TOKEN" | python -m json.tool
```
Expected: a proposta aparece na lista de pendentes que o humano aprova com OTP.

- [ ] **Step 6: Commit**

```bash
cd /opt/conecta-pro && git add backend/modules/ai/conversation/controllers/consultor_mcp_controller.py backend/tests/fundacao/test_consultor_mcp_controller.py
git commit --no-verify -m "feat(fase5.1): propor_pagamento MCP (🟡) grava pendente 'preparado' → gate OTP

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Endpoint propor_comunicado (🟡) — grava rascunho, nunca publica

**Files:**
- Modify: `/opt/conecta-pro/backend/modules/ai/conversation/controllers/consultor_mcp_controller.py`
- Test: `/opt/conecta-pro/backend/tests/fundacao/test_consultor_mcp_controller.py`

**Interfaces:**
- Produces: `POST /api/v1/consultores/mcp/propor-comunicado` body `{"titulo": str, "corpo": str}` → `{"announcement_id": ..., "status": "rascunho"}`.
- Grava `Announcement` (`modules/operacional/communication/models/announcement.py:79`, tabela `communication_announcements`) com `status='rascunho'` (`AnnouncementStatus.RASCUNHO.value`), `enviar_push=False`, `enviar_email=False`.

**Constraint:** nasce `'rascunho'`, NUNCA `'published'`. Envio (push/email) fica na aprovação humana.

- [ ] **Step 1: Adicionar o teste que falha**:

```python
def test_propor_comunicado_nasce_rascunho():
    # ⚠️ green compartilha o BANCO VIVO: o rascunho apareceria nos comunicados reais. DELETA no fim.
    async def run():
        from core.database import async_session_factory
        from sqlalchemy import text
        r = await _post("/consultores/mcp/propor-comunicado",
                        {"titulo": "TESTE 5.1 (apagar)", "corpo": "corpo de teste"})
        assert r.status_code == 200, r.text
        aid = r.json()["announcement_id"]
        try:
            assert r.json()["status"] == "rascunho"
            async with async_session_factory() as db:
                row = (await db.execute(text("SELECT status, enviar_push, enviar_email FROM communication_announcements WHERE id=:i"), {"i": aid})).first()
                assert row is not None and row[0] == "rascunho"
                assert not row[1] and not row[2]
        finally:
            # limpeza OBRIGATÓRIA: remove o comunicado-teste da produção (só se ainda rascunho)
            async with async_session_factory() as db:
                await db.execute(text("DELETE FROM communication_announcements WHERE id=:i AND status='rascunho'"), {"i": aid})
                await db.commit()
    asyncio.run(run())
```

- [ ] **Step 2: Rodar e ver falhar** → FAIL 404.

- [ ] **Step 3: Implementar** — usar o model ORM `Announcement`. Campos reais confirmados (`announcement.py`): corpo = **`conteudo`** (não `corpo`), título = `titulo`; obrigatórios sem default = `tenant_id`, `titulo`, `conteudo`, `created_by`. `status`/`enviar_push`/`enviar_email` têm default (`rascunho`/`False`/`False`), mas setamos `status` explícito por clareza:

```python
from modules.operacional.communication.models.announcement import Announcement, AnnouncementStatus

class ProporComunicadoIn(BaseModel):
    titulo: str
    corpo: str   # entrada da API; mapeia p/ a coluna `conteudo`

@router.post("/propor-comunicado")
async def propor_comunicado(payload: ProporComunicadoIn,
                            db: AsyncSession = Depends(get_db), user=Depends(get_current_active_user)):
    # 🟡 PROPOR: rascunho pendente de aprovação p/ envio. Nunca publica.
    ann = Announcement(
        tenant_id=str(getattr(user, "tenant_id", None) or getattr(user, "id")),  # confirmar de onde vem o tenant do user
        titulo=payload.titulo,
        conteudo=payload.corpo,
        created_by=str(user.id),
        status=AnnouncementStatus.RASCUNHO.value,
        enviar_push=False, enviar_email=False,
    )
    db.add(ann)
    await db.commit()
    await db.refresh(ann)
    return {"announcement_id": str(ann.id), "status": ann.status}
```
Confirmar de onde tirar `tenant_id` do user (`grep -n "tenant_id" /opt/conecta-pro/backend/core/auth/*.py` ou o model User) — se o user não expõe `tenant_id`, usar o tenant padrão do sistema (mesmo usado pelos outros controllers de comunicação).

- [ ] **Step 4: Rodar e ver passar** → PASS.

- [ ] **Step 5: Commit**

```bash
cd /opt/conecta-pro && git add backend/modules/ai/conversation/controllers/consultor_mcp_controller.py backend/tests/fundacao/test_consultor_mcp_controller.py
git commit --no-verify -m "feat(fase5.1): propor_comunicado MCP (🟡) grava rascunho → aprovação humana

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: As 11 tools no conector MCP + classificação

**Files:**
- Modify: `/opt/conecta-pro/mcp-server/server.py`
- Modify: `/opt/conecta-pro/mcp-server/tool_risk_manifest.py`

**Interfaces:**
- Consumes: os endpoints das Tasks 3–6 (via cliente `erp`, base `/api/v1`).

**Contexto:** o cliente `erp` (`server.py:182`) já faz login de serviço. Padrão: `await erp.post("/rota", json={...})`. Confirmar que `_Erp` tem `.post` — `grep -n "async def post\|async def get" server.py`. Se só houver `.get`, adicionar um `.post` espelhando o `.get`.

- [ ] **Step 1: Adicionar as 8 tools de consulta (🟢)** — em `server.py`, um helper + 8 tools finas:

```python
_ORIGENS = ("ceo", "cfo", "fiscal", "rh", "juridico", "comercial", "operacional", "ged")

async def _consultar(origem: str, pergunta: str) -> dict:
    return await erp.post(f"/consultores/mcp/{origem}/consultar", json={"pergunta": pergunta})

@mcp.tool
async def consultor_ceo(pergunta: str) -> dict:
    """Consultor executivo (CEO): visão estratégica, runway, decisão. Único que cruza dinheiro+legal+gente por cliente (gated). READ."""
    return await _consultar("ceo", pergunta)

@mcp.tool
async def consultor_cfo(pergunta: str) -> dict:
    """Consultor financeiro (CFO): caixa, aging, o que vence, saúde financeira. READ."""
    return await _consultar("cfo", pergunta)

@mcp.tool
async def consultor_fiscal(pergunta: str) -> dict:
    """Consultor fiscal/contábil: notas, guias, regime, obrigações. READ."""
    return await _consultar("fiscal", pergunta)

@mcp.tool
async def consultor_dp(pergunta: str) -> dict:
    """Consultor de DP/RH: folha, ponto, colaboradores, CCT. READ."""
    return await _consultar("rh", pergunta)

@mcp.tool
async def consultor_juridico(pergunta: str) -> dict:
    """Consultor jurídico (READ-ONLY): processos, dossiê. Aconselha, nunca protocola. READ."""
    return await _consultar("juridico", pergunta)

@mcp.tool
async def consultor_comercial(pergunta: str) -> dict:
    """Consultor comercial/CRM: funil, propostas, clientes. READ."""
    return await _consultar("comercial", pergunta)

@mcp.tool
async def consultor_operacional(pergunta: str) -> dict:
    """Consultor operacional (READ-ONLY): postos, escalas, presença. Nunca altera escala. READ."""
    return await _consultar("operacional", pergunta)

@mcp.tool
async def consultor_ged(pergunta: str) -> dict:
    """Consultor de GED/documentos: kits, panorama documental. READ."""
    return await _consultar("ged", pergunta)
```

- [ ] **Step 2: Adicionar as 3 tools de ação (🔵🟡)**:

```python
@mcp.tool
async def registrar_feedback(origem: str, correcao: str, consulta_id: int | None = None) -> dict:
    """🔵 Registra uma correção do gestor como memória permanente do consultor (realimenta o ERP). origem ∈ ceo/cfo/fiscal/rh/juridico/comercial/operacional/ged."""
    return await erp.post("/consultores/mcp/feedback", json={"origem": origem, "correcao": correcao, "consulta_id": consulta_id})

@mcp.tool
async def propor_pagamento(valor: float, pix_key: str, descricao: str = "") -> dict:
    """🟡 PROPÕE um pagamento PIX — grava PENDENTE ('preparado'). NÃO executa: requer aprovação humana + OTP. Nunca move dinheiro sozinho."""
    return await erp.post("/consultores/mcp/propor-pagamento", json={"valor": valor, "pix_key": pix_key, "descricao": descricao})

@mcp.tool
async def propor_comunicado(titulo: str, corpo: str) -> dict:
    """🟡 PROPÕE um comunicado — grava RASCUNHO. NÃO publica/envia: requer aprovação humana."""
    return await erp.post("/consultores/mcp/propor-comunicado", json={"titulo": titulo, "corpo": corpo})
```

- [ ] **Step 3: Classificar as 11 no manifesto** — em `tool_risk_manifest.py`, adicionar ao `TOOL_RISK`:

```python
    # --- Fase 5.1: consultores (🟢) ---
    "consultor_ceo": "read", "consultor_cfo": "read", "consultor_fiscal": "read",
    "consultor_dp": "read", "consultor_juridico": "read", "consultor_comercial": "read",
    "consultor_operacional": "read", "consultor_ged": "read",
    # --- Fase 5.1: ações ---
    "registrar_feedback": "write_low",   # 🔵
    "propor_pagamento": "propose",       # 🟡
    "propor_comunicado": "propose",      # 🟡
```

- [ ] **Step 4: Rodar o lint da Task 1** — prova que as 11 novas estão classificadas:

Run: `cd /opt/conecta-pro/mcp-server && python3 test_tool_risk_manifest.py`
Expected: PASS (todas as tools, incluindo as 11 novas, classificadas; nenhuma 🔴 exposta).

- [ ] **Step 5: Confirmar que NENHUMA tool 🔴 foi criada** — busca de segurança:

Run: `grep -nE "async def (mandar_pix|executar_pagamento|aprovar_pagamento|editar_alocacao|protocolar)" /opt/conecta-pro/mcp-server/server.py`
Expected: vazio. Nenhuma tool de executar-dinheiro/legal/operacional existe no conector.

- [ ] **Step 6: Commit**

```bash
cd /opt/conecta-pro && git add mcp-server/server.py mcp-server/tool_risk_manifest.py
git commit --no-verify -m "feat(fase5.1): 8 consultores (🟢) + 3 ações (🔵🟡) como tools MCP + classificação

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Prova E2E pela rota real + deploy

**Files:**
- Test: `/opt/conecta-pro/backend/tests/fundacao/test_consultor_mcp_controller.py` (roda tudo)

- [ ] **Step 1: Suíte backend verde**

Run: docker cp o controller + `_mcp_token.py` + o teste pro GREEN (`conecta-pro-backend-green`), `docker restart conecta-pro-backend-green` (esperar `:8081/health`=200; NÃO tocar :8080), depois `docker exec conecta-pro-backend-green sh -c 'cd /app && PYTHONPATH=/app MCP_TEST_BASE=http://127.0.0.1:8080/api/v1 python tests/fundacao/test_consultor_mcp_controller.py'` (roda todos os _TESTS).
Expected: PASS em todos (consulta, feedback, propor_pagamento nasce 'preparado', propor_comunicado nasce 'rascunho').

- [ ] **Step 2: Lint de risco verde**

Run: `cd /opt/conecta-pro/mcp-server && python3 test_tool_risk_manifest.py`
Expected: PASS.

- [ ] **Step 3: Deploy backend (blue-green, baked)**

Run: `bash /opt/conecta-pro/scripts/deploy_backend_bluegreen.sh` (timeout ≥600s; lock `/tmp/conecta_deploy.lock`).
Depois confirmar health: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8080/health` → 200.

- [ ] **Step 4: Rebuild do conector MCP** — o conector é serviço separado; `docker cp` = volátil, durável = rebuild da imagem `conecta-pro-mcp`. Seguir o processo de build do conector (Task 2, Step 1) e recriar o container. Confirmar que o lint rodou no build (Parede 1).

- [ ] **Step 5: Prova viva das 3 classes pela rota** (backend no ar) — 🟢 lê, 🟡 cria pendente, 🔴 ausente:

```bash
# 🟢 consulta responde com dado real
curl -s -X POST http://127.0.0.1:8080/api/v1/consultores/mcp/cfo/consultar -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{"pergunta":"caixa em uma frase"}' | python -m json.tool
# 🟡 proposta cai na fila humana de pendentes
curl -s -X POST http://127.0.0.1:8080/api/v1/consultores/mcp/propor-pagamento -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{"valor":0.01,"pix_key":"teste@x.com","descricao":"prova 5.1"}' | python -m json.tool
curl -s "http://127.0.0.1:8080/api/v1/financeiro/inter/payments?status_filter=preparado" -H "Authorization: Bearer $TOKEN" | python -m json.tool  # a proposta aparece
# 🔴 confirmação de ausência
grep -c "mandar_pix\|aprovar_pagamento" /opt/conecta-pro/mcp-server/server.py  # → 0
```

- [ ] **Step 6: Commit final da fase**

```bash
cd /opt/conecta-pro && git add -A && git commit --no-verify -m "test(fase5.1): prova E2E das 3 classes de risco pela rota real; 5.0+5.1 fechadas

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review (feita ao escrever)
- **Cobertura da spec:** Bloco 2 (taxonomia+manifesto+lint) = Tasks 1–2. Bloco 3 (8 consultores + família de ação) = Tasks 3–7. Parede 0/1 (🔴 fora, allowlist) = Tasks 1,7. Rollout 5.0/5.1 = todo o plano. 5.2→5.5 fora (planos próprios).
- **Sem placeholder de código:** cada step tem código real; os pontos de "confirmar nome exato" (get_db, campos NOT NULL, `.post` do `erp`) são leituras pontuais de arquivos citados, não TODOs.
- **Consistência de tipos:** `origem` ∈ 8 strings fixas em todo lugar; `status='preparado'`/`'rascunho'` batem com o schema real; `tools_include()` usada no teste e no manifesto.
- **Riscos abertos herdados da spec (não bloqueiam 5.0/5.1):** thresholds de gatilho (5.3), scrubber de PII (5.4), formato final do manifesto (dict aqui; pode virar decorator depois).

## Handoff
Ao concluir, o solo está pronto: 5.2 (container Hermes headless + config.yaml usando `tools_include()` como allowlist + ponte + degradação graciosa) vira o próximo plano, escrito contra este código já no ar.
