# Fase 5.5 — Memória que Aprende (com Curator) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Passos usam checkbox (`- [ ]`).

**Goal:** Pôr um Curator no caminho do aprendizado automático que já roda cego, para que nenhuma memória fabricada/não-ancorada vire autoritativa — quarentenando o que existe e mandando o não-ancorado pra fila de revisão da diretoria.

**Architecture:** `consultor_memorias` ganha `status`/`confidence`/`expira_em`/`curator_veredito`. Um `memoria_curator.curar()` (ancora número via groundedness + ancora entidade via clients/employees + audit) decide status. `consultor_hub.aprender/registrar_feedback` e `agent_service._update_contact_memory` passam a chamar o curator antes de gravar. `contexto_compartilhado` só lê `status='ativo'` não-expirado. Fila de revisão (propor→aprovar) no sino da diretoria. Evals barram regressão.

**Tech Stack:** Python/FastAPI, Postgres (migration alembic aditiva), reuso `garantia/groundedness.py` + `garantia/agent_audit.py` + entrega sino 5.3, testes standalone (sem pytest) em bancada throwaway.

## Global Constraints
- **NUNCA FABRICAR** — memória não-ancorada NÃO é autoritativa; vai pra revisão humana. Jordan=fonte da verdade.
- Não perder aprendizado real (grava sempre; muda status/autoridade, não descarta).
- Não quebrar os 8 consultores (contexto_compartilhado segue funcionando, só filtra 'ativo' não-expirado).
- Audit append-only (`agent_audit.registrar_acao_agente`) de TODO write-back. Expiração/decay.
- RBAC: memória permanente só diretoria (correção) ou curator-aprovado. Endpoint /feedback ganha gate diretoria.
- `.md` humanos (consultor_conhecimento + agent_knowledge) INTOCADOS (sem auto-escrita).
- Bancada throwaway (nunca backend vivo/green:8080); migration aditiva/reversível; git add próprios; commit IMEDIATO após add (peer-churn varre staged); commits --no-verify + `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; TZ Manaus; casts ::text.

---

### Task 1: Migration — colunas de curadoria + quarentena dos existentes + filtro em contexto_compartilhado

**Files:**
- Create: `backend/alembic/versions/fase55_consultor_memorias_curadoria.py`
- Modify: `backend/modules/ai/conversation/services/consultor_hub.py` (`_ensure_schema` ~52-84; `contexto_compartilhado` query ~189-201)

**Interfaces:**
- Produces: `consultor_memorias` com `status text NOT NULL DEFAULT 'pendente_revisao'`, `confidence real`, `autor text`, `curator_veredito jsonb`, `expira_em timestamptz`. `contexto_compartilhado` lê só `status='ativo' AND (expira_em IS NULL OR expira_em>now())`.

- [ ] **Step 1 — migration aditiva:** `ALTER TABLE consultor_memorias ADD COLUMN IF NOT EXISTS status text NOT NULL DEFAULT 'pendente_revisao'`, idem confidence real, autor text, curator_veredito jsonb, expira_em timestamptz. **Quarentena:** `UPDATE consultor_memorias SET status='pendente_revisao' WHERE status NOT IN ('ativo','rejeitado')` — os 36 existentes (não-curados) saem do contexto até revisão. downgrade = DROP COLUMN. `down_revision` = head atual (descobrir por grep em alembic/versions).
- [ ] **Step 2 — `_ensure_schema`:** adicionar as mesmas colunas no CREATE/ALTER de fallback (consultor_hub.py ~52-84) p/ ambientes sem a migration.
- [ ] **Step 3 — filtro em `contexto_compartilhado`:** trocar `WHERE ativo` (linha ~197) por `WHERE status='ativo' AND (expira_em IS NULL OR expira_em > now())`. Manter o resto do bloco.
- [ ] **Step 4 — teste (bancada throwaway, DB):** inserir 1 memória status='ativo' e 1 'pendente_revisao'; `contexto_compartilhado` traz só a 'ativo'. Prova quarentena: as pré-existentes viram 'pendente_revisao'. Limpar por id.
- [ ] **Step 5 — Commit** `feat(5.5): colunas de curadoria em consultor_memorias + quarentena + filtro contexto`.

---

### Task 2: `memoria_curator.curar()`

**Files:**
- Create: `backend/modules/ai/conversation/services/garantia/memoria_curator.py`
- Test: `if __name__=="__main__"` no próprio arquivo.

**Interfaces:**
- Consumes: `garantia.groundedness.verificar(resposta, fonte)->{ok,suspeitos}`; DB p/ checar entidades.
- Produces: `async def curar(db, *, fato: str, origem: str, fonte: str, autor_role: str, fonte_conversa: dict) -> dict` retornando `{"status": "ativo"|"pendente_revisao", "confidence": float, "veredito": {...}}`.

- [ ] **Step 1 — código:**
```python
"""Curator da memória que aprende (5.5). Barra fabricação: memória não-ancorada
NÃO vira autoritativa. Reusa groundedness (número) + checagem de entidade real."""
from __future__ import annotations
import re
from sqlalchemy import text
from modules.ai.conversation.services.garantia import groundedness

_DIRETORIA = {"admin"}  # feedback permanente confiável

async def _entidades_existem(db, fato: str) -> tuple[bool, list[str]]:
    # extrai CNPJs citados; se citar CNPJ, tem que existir em clients/empresas
    cnpjs = re.findall(r"\d{2}\.?\d{3}\.?\d{3}/?\d{4}-?\d{2}", fato)
    faltando = []
    for c in cnpjs:
        dig = re.sub(r"\D", "", c)
        row = (await db.execute(text(
            "SELECT 1 FROM clients WHERE regexp_replace(coalesce(document_number,''),'\\D','','g')=:d "
            "UNION SELECT 1 FROM empresas WHERE regexp_replace(coalesce(cnpj,''),'\\D','','g')=:d LIMIT 1"),
            {"d": dig})).first()
        if not row:
            faltando.append(c)
    return (len(faltando) == 0, faltando)

async def curar(db, *, fato: str, origem: str, fonte: str, autor_role: str,
                fonte_conversa: dict) -> dict:
    veredito: dict = {"checks": {}}
    # 1) ancoragem numérica: números do fato têm que estar na fonte da conversa
    g = groundedness.verificar(fato, fonte_conversa or {})
    veredito["checks"]["numeros"] = g
    numeros_ok = g.get("ok", True)
    # 2) ancoragem de entidade
    ent_ok, faltando = await _entidades_existem(db, fato)
    veredito["checks"]["entidades"] = {"ok": ent_ok, "faltando": faltando}
    # 3) confiança
    autor_confiavel = (fonte == "feedback_gestor" and autor_role in _DIRETORIA)
    confidence = 1.0 if autor_confiavel else (0.8 if (numeros_ok and ent_ok) else 0.3)
    # 4) decisão: só ativa se ancorado (número+entidade) OU diretoria explícita
    if autor_confiavel or (numeros_ok and ent_ok):
        status = "ativo"
    else:
        status = "pendente_revisao"
    veredito["confidence"] = confidence
    return {"status": status, "confidence": confidence, "veredito": veredito}
```
- [ ] **Step 2 — teste standalone:** (a) fato com número FORA da fonte → 'pendente_revisao'; (b) fato com CNPJ inexistente → 'pendente_revisao' + faltando≠[]; (c) fato com número da fonte + sem entidade estranha → 'ativo'; (d) autor diretoria feedback → 'ativo' mesmo sem número. Bancada throwaway c/ DB.
- [ ] **Step 3 — Commit** `feat(5.5): memoria_curator.curar (ancora numero+entidade, decide status)`.

---

### Task 3: Gate no `aprender()` + audit

**Files:**
- Modify: `backend/modules/ai/conversation/services/consultor_hub.py` (`aprender` ~295-347)

- [ ] **Step 1 — LER `aprender` inteiro** (~295-347): entender o que é a "fonte da conversa" disponível (pergunta+resposta+panorama) p/ passar ao curator.
- [ ] **Step 2 — gate:** antes do INSERT (linha ~337), para cada fato extraído chamar `veredito = await memoria_curator.curar(db, fato=fato, origem=origem, fonte='llm_destilado', autor_role='', fonte_conversa=<panorama+resposta>)`; INSERT grava `status=veredito['status'], confidence=..., autor='llm:'+modelo, curator_veredito=veredito['veredito'], expira_em=<now+90d p/ operacional>`. Import lazy do curator.
- [ ] **Step 3 — audit:** após cada INSERT, `agent_audit.registrar_acao_agente(db, origem=origem, pergunta='[aprender]', resposta=fato, modelo='gpt-4o-mini', extra={'status':veredito['status']})` (assinatura real: LER agent_audit.py:33). Não derruba o fluxo em erro (best-effort try/except).
- [ ] **Step 4 — teste:** fato fabricado (número inexistente na fonte) via aprender → grava status='pendente_revisao' (não contamina contexto) + 1 linha audit. Bancada throwaway.
- [ ] **Step 5 — Commit** `feat(5.5): gate curator + audit no aprender() dos consultores`.

---

### Task 4: `registrar_feedback` — RBAC diretoria + curator

**Files:**
- Modify: `backend/modules/ai/conversation/services/consultor_hub.py` (`registrar_feedback` ~353-382)
- Modify: `backend/modules/ai/conversation/controllers/consultor_feedback_controller.py` (rota /feedback ~26-34)

- [ ] **Step 1 — RBAC na rota:** `/ai/consultor/feedback` (controller ~27) — a gravação de MEMÓRIA PERMANENTE (correção) só se `user.role` diretoria (admin). Feedback 👍/👎 simples (sem correção) pode seguir de qualquer user ativo (só marca util). Ler o controller + aplicar o gate no ponto onde `correcao` vira memória.
- [ ] **Step 2 — curator no feedback:** em `registrar_feedback` (~370-377), antes do INSERT da correção como memória, chamar `memoria_curator.curar(..., fonte='feedback_gestor', autor_role=user.role, ...)` — diretoria → 'ativo' (auditado); correção com número fabricado → veredito marca (mas diretoria é confiável, grava 'ativo' + flag no veredito). Audit sempre.
- [ ] **Step 3 — teste:** feedback com correção de user NÃO-diretoria → NÃO grava memória permanente; de diretoria → grava 'ativo' + audit. Bancada.
- [ ] **Step 4 — Commit** `fix(5.5): feedback permanente so diretoria + curator + audit`.

---

### Task 5: `_update_contact_memory` (WhatsApp) — curator leve

**Files:**
- Modify: `backend/modules/integrations/connectors/whatsapp/agent_service.py` (`_update_contact_memory` ~2338-2403)

- [ ] **Step 1 — LER a função:** ela grava resumo `direction='mem'` em cwi_message_log.
- [ ] **Step 2 — ancoragem leve:** o resumo só é gravado como memória "confiável" se as entidades citadas (nome/CNPJ) baterem com o lead/cliente REAL daquele telefone (reusar `_cliente_do_telefone` do 5.4c T1). Se não bater → gravar com prefixo/flag `[não confirmado]` (não citado como fato pelo `_get_contact_memory`).
- [ ] **Step 3 — teste:** resumo com CNPJ que não bate o telefone → gravado como '[não confirmado]'. Bancada throwaway (sem tocar WhatsApp vivo).
- [ ] **Step 4 — Commit** `fix(5.5): curator leve no _update_contact_memory (ancora entidade do telefone)`.

---

### Task 6: Fila de revisão (propor→aprovar de memória) no sino da diretoria

**Files:**
- Modify: `backend/modules/ai/conversation/controllers/consultor_feedback_controller.py` (+ rotas aprovar/rejeitar)
- Reusar: entrega no sino (5.3 `notifications/proativo/entrega.py`) ou base.propor.

- [ ] **Step 1 — entrega:** quando o curator devolve 'pendente_revisao', enfileirar no sino da diretoria (roles admin) uma notificação "Memória aguardando sua revisão: <fato>" com correlation `memoria:{id}` (reusar o entregador da 5.3). 1 por memória, dedup.
- [ ] **Step 2 — rotas:** `POST /ai/consultor/memorias/{id}/aprovar` e `/rejeitar` (diretoria) → UPDATE status='ativo'|'rejeitado' + audit. `GET /ai/consultor/memorias/pendentes` (diretoria) lista a fila.
- [ ] **Step 3 — teste:** memória 'pendente_revisao' → notificação no sino de user admin (não de gestor); aprovar → status='ativo' + entra no contexto. Bancada.
- [ ] **Step 4 — Commit** `feat(5.5): fila de revisao de memoria (sino diretoria + aprovar/rejeitar)`.

---

### Task 7: Suite-oráculo + mutação

**Files:**
- Create: `backend/scripts/orq/test_oraculos_5_5.py`

- [ ] **Step 1 — oráculos:** (a) fato com número não-ancorado NÃO vira 'ativo'; (b) entidade inventada (CNPJ inexistente) NÃO vira 'ativo'; (c) contexto_compartilhado só lê 'ativo' não-expirado (memória expirada some); (d) feedback de não-diretoria NÃO grava memória permanente; (e) todo write-back gera ≥1 linha de audit; (f) os 8 consultores seguem montando contexto (não regride).
- [ ] **Step 2 — MUTAÇÃO (≥2):** (M1) curator sempre retorna 'ativo' → oráculo (a)/(b) FALHA (fabricação passa). (M2) contexto lê status qualquer → oráculo (c) FALHA. Aplicar em copy no throwaway, provar que morde, reverter.
- [ ] **Step 3 — rodar** na bancada; 6 oráculos PASS + 2 mutações mordem; limpeza por id, 0 remanescentes.
- [ ] **Step 4 — Commit** `test(5.5): oraculos memoria curada + mutacao curator-off`.

---

### Task 8: Deploy + verificação

- [ ] **Step 1 — migration no DB vivo** (`alembic upgrade heads`) — aditiva; confirma quarentena (36 → pendente_revisao).
- [ ] **Step 2 — backend blue-green** (`scripts/deploy_backend_bluegreen.sh`), SEM reciclar celery (5.5 não add task celery). OOM-safe.
- [ ] **Step 3 — verificar:** health 200; os 8 consultores respondem (contexto sem as memórias envenenadas); a fila de revisão aparece no sino de user admin (Playwright/curl autenticado); psql: os 36 estão 'pendente_revisao', contexto_compartilhado retorna só 'ativo'.
- [ ] **Step 4 — push** (commits 5.5).

## Self-Review
- **Cobertura spec:** curator (T2), 3 write-backs gated (T3/T4/T5), contexto filtra ativo (T1), quarentena 36 (T1), fila revisão propor→aprovar (T6), RBAC feedback (T4), evals+mutação (T7), .md intocados (não há task que os escreva), deploy (T8). Cobre todos os componentes.
- **Placeholders:** curator com código completo (T2); demais tasks começam com "LER a função atual" (arquivo legado) + pontos exatos.
- **Consistência:** `curar(db,*,fato,origem,fonte,autor_role,fonte_conversa)->{status,confidence,veredito}` usado igual em T3/T4/T5; status ∈ {ativo,pendente_revisao,rejeitado}; filtro `status='ativo' AND expira` em T1 e testado em T7.
