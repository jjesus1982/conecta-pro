# Fase 5.6a — Moonshot Fraud Detection (ligar + consertar + wall-ify) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Passos usam checkbox (`- [ ]`).

**Goal:** Ligar a detecção de fraude/anomalia (lógica real, integração quebrada) sobre inter_payments como sistema de ALERTA propor→aprovar — nunca age sozinho sobre dinheiro, nunca apresenta suspeita como fato.

**Architecture:** Consertar a integração casca (controller↔repo↔model, ~13 mismatches — o repo/model são a verdade), criar a tabela faltante fraud_risk_profiles, RBAC diretoria/financeiro, montar em main_production, alimentar com inter_payments, e ligar alertas HIGH/CRITICAL ao sino da diretoria + contexto do CFO IA rotulados como SUSPEITA.

**Tech Stack:** Python/FastAPI, Postgres (alembic), reuso notifications/proativo/entrega.py (sino), cfo_service (CFO IA), testes standalone (sem pytest) em bancada throwaway.

## Global Constraints
- **NUNCA age sozinho sobre dinheiro** — RuleAction.BLOCK/ESCALATE/LIMIT nunca auto-executadas; `decision:'block'` = alerta p/ aprovação humana, jamais cancelamento. Bloqueio de entidade só humano+RBAC.
- **NUNCA fabricar** — alerta = "SUSPEITA/pendente revisão" (status NEW/INVESTIGATING) até confirm humano; score 0-100 = hipótese NÃO-CALIBRADA, rotulado como tal; nunca "fraude detectada" como fato.
- **RBAC**: ver=`require_permission("financeiro:fraude:ver")`; gerenciar/bloquear=diretoria (admin). LGPD.
- Não reescrever a lógica real (fraud_detector/pattern_analyzer/risk_scorer). Fix cirúrgico da integração.
- DB vivo: fraud_alerts/patterns/rules JÁ existem (sprint45, corretas); fraud_risk_profiles FALTA. Não colisão com sprint04.
- Bancada throwaway (rede conecta-pro_conecta-pro-network, DATABASE_URL de POSTGRES_*; NUNCA backend vivo/green:8080); teste limpa por id, 0 remanescentes, nunca apaga pagamento/dado real; git add próprios + commit IMEDIATO (peer-churn); --no-verify + Co-Authored-By Opus 4.8; TZ Manaus; casts ::text.

---

### Task 1: migration fraud_risk_profiles (faltante) + verificação

**Files:** Create `backend/alembic/versions/fase56a_fraud_risk_profiles.py`; ler `modules/ai/fraud_detection/models/risk_profile.py` (RiskProfile, tablename ~L65).

- [ ] Ler risk_profile.py inteiro (colunas/tipos/índices do model). Criar migration que cria `fraud_risk_profiles` EXATAMENTE como o model espera (aditiva, IF NOT EXISTS; down=drop). down_revision=head real (grep alembic). NÃO tocar fraud_alerts/patterns/rules (já existem).
- [ ] Teste bancada: rodar migration contra postgres throwaway (ou o vivo é T8), criar 1 RiskProfile via o model/repo, ler de volta. Limpar por id.
- [ ] Commit `feat(5.6a): migration fraud_risk_profiles (tabela faltante do modulo dormente)`.

---

### Task 2: consertar integração casca (controller↔repository↔model)

**Files:** Modify `modules/ai/fraud_detection/controllers/fraud_controller.py` + `services/alert_manager.py` (o repo/models são a VERDADE — ajustar chamadores).

- [ ] LER repo (`fraud_repository.py`) e models — anotar as assinaturas REAIS. Corrigir no controller/alert_manager os ~13 mismatches da discovery: `get_alerts(category,severity,status,...,page,page_size)` (não filters/skip/limit); `get_alert`/`get_alert_by_number` (não get_alert_by_id); `create_rule(rule_data: dict)`; `get_rule`/`get_rule_by_code`; **`delete_rule` não existe → adicionar ao repo** (soft-delete/UPDATE is_active=false, seguindo o padrão do repo); `get_patterns`/`get_profiles` assinaturas reais; `get_profile_by_entity(entity_type,entity_id)`; `update_profile(profile_id,dict)`; `alert.resolve(user_id,resolution_type,...)`, `confirm(user_id,...)`, `mark_false_positive(user_id,...)` (todos user_id obrigatório); `check_transaction` sem payer_account/payee_account.
- [ ] Teste por endpoint (não smoke): p/ cada área (alerts/rules/patterns/profiles) um teste que CHAMA o handler real com dado sentinela + prova 200/efeito no banco + limpa. Cobre os mismatches.
- [ ] Commit `fix(5.6a): conserta integracao controller<->repo<->model do fraud_detection (13 mismatches)`.

---

### Task 3: RBAC diretoria/financeiro nos endpoints

**Files:** Modify `fraud_controller.py` (20 endpoints).

- [ ] Adicionar `Depends(require_permission("financeiro:fraude:ver"))` nos GET e diretoria (admin/`require_permission("financeiro:fraude:gerenciar")`) nos POST/PATCH/DELETE (criar regra, resolver, bloquear entidade). Seguir o padrão de `fiscal_controller.py`. Confirmar as strings de permissão reais no sistema (grep require_permission).
- [ ] Teste: user não-diretoria → 403 no bloquear-entidade/criar-regra; diretoria → ok. Bancada.
- [ ] Commit `feat(5.6a): RBAC diretoria/financeiro nos endpoints de fraude`.

---

### Task 4: montar em main_production

**Files:** Modify `backend/main_production.py`.

- [ ] Incluir o `fraud_controller.router` (prefix `/ai/fraud`, tag "AI - Detecção de Fraude"), padrão dos includes (ex.: bidding ~L431). Import + api_router.include_router.
- [ ] Teste: boot sem erro; `GET /api/v1/ai/fraud/alerts` → 401/403 (montado+gated), não 404. Verificar sem docker exec pesado no vivo (bancada ou grep boot).
- [ ] Commit `feat(5.6a): monta fraud_detection em main_production (/ai/fraud)`.

---

### Task 5: alimentar com dado real (inter_payments)

**Files:** Create `modules/ai/fraud_detection/services/inter_payments_adapter.py` (ou hook).

- [ ] Adaptador que, dado um inter_payments (ou lote), monta o input de `check_transaction` (amount, payer=origem/CNPJ, payee=beneficiário, transaction_id=payment id) e roda o detector → cria alerta se score alto. NÃO altera o pagamento (read-only sobre inter_payments). Rodar via beat (padrão 5.3) OU hook não-bloqueante em preparar_lote — decidir pelo menor acoplamento (beat que varre inter_payments recentes é o mais desacoplado).
- [ ] Teste: sobre 1 pagamento sentinela de teste (não real), detecta e cria alerta; estado do pagamento INALTERADO (baseline). Limpar. Bancada.
- [ ] Commit `feat(5.6a): adapter que roda deteccao sobre inter_payments (read-only, so alerta)`.

---

### Task 6: ligar na camada cognitiva (sino diretoria + CFO IA), rotulado SUSPEITA

**Files:** Modify `services/alert_manager.py` (`_send_notifications` ~L610); `modules/financial/cfo_service.py` (`_formatar_contexto_financeiro` ~L589).

- [ ] `_send_notifications`: quando severity in (HIGH,CRITICAL) → `entrega.enviar_individual(db, user_ids=<diretoria via resolver_usuarios_por_roles(['admin'])>, title="⚠️ Suspeita de anomalia (revisar)", body=<resumo ROTULADO como suspeita/pendente, nunca "fraude confirmada">, correlation="fraude:{alert_id}")`. Dedup. Best-effort.
- [ ] cfo_service: injetar no contexto um bloco "ALERTAS DE ANOMALIA ABERTOS (suspeitas pendentes de revisão — NÃO são fatos confirmados)" com os alertas status NEW/INVESTIGATING (não CONFIRMED). Rótulo explícito.
- [ ] Teste: alerta HIGH → notificação SÓ p/ admin (não gestor); texto contém "suspeita/pendente", NÃO "fraude confirmada". Bancada.
- [ ] Commit `feat(5.6a): alerta de fraude no sino da diretoria + CFO IA (rotulado SUSPEITA, nunca fato)`.

---

### Task 7: suite-oráculo + mutação

**Files:** Create `backend/scripts/orq/test_oraculos_5_6a_fraude.py`.

- [ ] Oráculos: (a) rodar check sobre um pagamento NÃO cancela/debita (estado de execução do inter_payments inalterado antes==depois); (b) alerta nasce status NEW/INVESTIGATING (nunca CONFIRMED sem ação humana); (c) RBAC: não-diretoria não bloqueia entidade; (d) alerta HIGH cai no sino SÓ da diretoria; (e) nenhum texto exposto diz "fraude confirmada/detectada" antes de confirm humano (rótulo suspeita); (f) RuleAction.BLOCK NÃO auto-executa sobre transação.
- [ ] MUTAÇÃO (≥2): (M1) fazer o detector auto-executar block sobre a transação → oráculo (a)/(f) FALHA; (M2) expor alerta como "fraude confirmada" sem confirm → oráculo (e) FALHA. Aplicar em copy no throwaway.
- [ ] Rodar; 6 oráculos PASS + 2 mutações mordem; limpeza 0 remanescentes.
- [ ] Commit `test(5.6a): oraculos fraude (nunca age sobre dinheiro, suspeita nao fato, RBAC) + mutacao`.

---

### Task 8: deploy + verificação

- [ ] alembic upgrade heads (cria fraud_risk_profiles no vivo). Backend blue-green (SEM reciclar celery, salvo o adapter virar beat → aí reciclar batch/beat). OOM-safe.
- [ ] Verificar: /ai/fraud/alerts 401/403 (montado+gated); sobre inter_payments reais NÃO altera pagamento; um alerta de teste HIGH aparece no sino de admin rotulado suspeita; consultores/CFO seguem OK.
- [ ] Push (com OK Jordan — dinheiro-adjacente, avisar antes de ir ao ar).

## Self-Review
- Cobertura: DB (T1), integração (T2), RBAC (T3), wiring (T4), dado real (T5), cognitivo (T6), oráculos (T7), deploy (T8). Cobre a spec.
- Placeholders: cada task "LER antes" (legado) + pontos exatos da discovery (arquivo:linha).
- Consistência: alerta status NEW/INVESTIGATING→CONFIRMED só por humano; entrega roles=['admin'] (diretoria); check_transaction nunca altera pagamento (T5/T7).
