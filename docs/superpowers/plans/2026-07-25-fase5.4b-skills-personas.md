# Fase 5.4b — Skills/playbooks + enriquecimento de personas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Passos usam checkbox (`- [ ]`).

**Goal:** Agentes mais assertivos com conhecimento de domínio real + playbooks, via um serviço `consultor_conhecimento` file-based (ensina sem rebuild), sem furar nenhuma parede.

**Architecture:** Generaliza o padrão do Jurídico. Um serviço lê `uploads/agent_knowledge/consultores/<agente>.md` em runtime (cache por mtime), seleciona seções relevantes por overlap de keywords (RAG-lite, sem embeddings), e injeta no system_prompt de cada agente. Playbooks são seções de TEXTO nos mesmos .md (guiam o LLM pelas tools gated existentes — zero código de execução novo). Guarda anti-burla (teste AST) trava handler futuro que pule `propor()`.

**Tech Stack:** Python (backend FastAPI), markdown files em volume Docker, testes standalone (sem pytest) em bancada throwaway `docker run --rm --memory=2g`.

## Global Constraints
- Conhecimento/playbook = TEXTO que orienta; NUNCA cria caminho de execução novo. Ação só via tools gated (`propor()`), RBAC server-side, dinheiro/legal humano+OTP.
- Nunca fabricar: conteúdo = fatos reais (repo/memória/Jordan); o que não for verificável entra como `> _(aguardando confirmação Jordan)_`, não inventado.
- Fail-open: .md ausente → string vazia, nunca exceção (não pode quebrar o chat).
- Ensina sem rebuild: arquivo relido em runtime (cache invalida por mtime).
- Read-only preservado; groundedness segue valendo.
- Bancada throwaway (nunca green/:8080/backend vivo); teste limpa por id; git add só dos próprios; commits --no-verify + `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; TZ Manaus.
- Módulos canônicos: {financeiro,fiscal,dp,ged,juridico,crm,operacional,sst,dev}.

---

### Task 1: Serviço `consultor_conhecimento`

**Files:**
- Create: `backend/modules/ai/conversation/services/consultor_conhecimento_service.py`
- Test: standalone `if __name__ == "__main__"` no próprio arquivo (molde acoes/base.py).

**Interfaces:**
- Produces: `contexto_para_prompt(agente: str, pergunta: str = "", *, max_secoes: int = 3) -> str` — bloco de conhecimento relevante ou "" (fail-open). `_secoes(agente) -> list[tuple[str,str]]` (título, corpo) com cache por mtime.

- [ ] **Step 1:** Escrever o serviço (código completo):

```python
"""Conhecimento de domínio + playbooks por agente (Fase 5.4b).
Lê uploads/agent_knowledge/consultores/<agente>.md em runtime (ensina sem rebuild).
RAG-lite por overlap de keywords (molde juridico/conhecimento_service). Fail-open.
"""
from __future__ import annotations
import os, re
from pathlib import Path

_DIR = Path(os.getenv("CONSULTOR_KB_DIR", "/app/uploads/agent_knowledge/consultores"))
_CACHE: dict[str, tuple[float, list[tuple[str, str]]]] = {}
_STOP = {"de","a","o","que","e","do","da","em","um","para","com","nao","os","no","se","na","por",
         "mais","as","dos","como","mas","ao","das","seu","sua","ou","quando","muito","ja","tambem",
         "so","pelo","pela","ate","isso","entre","sem","mesmo","aos","seus","quem","nas","esse",
         "voce","essa","num","nem","suas","meu","qual","nos","lhe","este","dele","uma"}

def _tokens(txt: str) -> set[str]:
    return {w for w in re.findall(r"[a-z0-9à-ÿ]{3,}", txt.lower()) if w not in _STOP}

def _secoes(agente: str) -> list[tuple[str, str]]:
    p = _DIR / f"{agente}.md"
    try:
        st = p.stat()
    except OSError:
        return []  # fail-open
    cached = _CACHE.get(agente)
    if cached and cached[0] == st.st_mtime:
        return cached[1]
    txt = p.read_text(encoding="utf-8")
    secoes: list[tuple[str, str]] = []
    cur_t, cur_b = None, []
    for line in txt.splitlines():
        if line.startswith("## "):
            if cur_t is not None:
                secoes.append((cur_t, "\n".join(cur_b).strip()))
            cur_t, cur_b = line[3:].strip(), []
        elif cur_t is not None:
            cur_b.append(line)
    if cur_t is not None:
        secoes.append((cur_t, "\n".join(cur_b).strip()))
    _CACHE[agente] = (st.st_mtime, secoes)
    return secoes

def contexto_para_prompt(agente: str, pergunta: str = "", *, max_secoes: int = 3) -> str:
    secoes = _secoes(agente)
    if not secoes:
        return ""
    q = _tokens(pergunta)
    if not q:
        escolhidas = secoes[:max_secoes]
    else:
        ranked = sorted(secoes, key=lambda s: len(_tokens(s[0] + " " + s[1]) & q), reverse=True)
        escolhidas = [s for s in ranked if _tokens(s[0] + " " + s[1]) & q][:max_secoes] or secoes[:1]
    corpo = "\n\n".join(f"### {t}\n{b}" for t, b in escolhidas if b)
    if not corpo:
        return ""
    return "\n\n[CONHECIMENTO DE DOMINIO - fatos reais; use-os, nao invente alem disto]\n" + corpo
```

- [ ] **Step 2:** Teste standalone (escreve .md temporário em CONSULTOR_KB_DIR de teste, verifica: (a) seção certa por keyword; (b) arquivo ausente → ""; (c) cache por mtime; (d) sem pergunta → primeiras N). Limpa o .md temp no finally.
- [ ] **Step 3:** Rodar na bancada throwaway (`docker run --rm --memory=2g`, PYTHONPATH=/app, python -m). Esperado: todos PASS.
- [ ] **Step 4:** Commit `feat(5.4b): consultor_conhecimento_service (RAG-lite file-based, fail-open)`.

---

### Task 2: Conteúdo — 8 .md de conhecimento + playbooks

**Files:**
- Create: `uploads/agent_knowledge/consultores/{cfo,fiscal,juridico,operacional,comercial,chro,ged,ceo}.md`

**Interfaces:** Consome T1 (`contexto_para_prompt` lê estes arquivos).

- [ ] **Step 1:** Cada .md com 2 blocos de seções `## `. `## Conhecimento: <tema>` (fatos REAIS ancorados: CFO/fiscal → CCT SINDECOMPRESTS AM000613/2025 piso 2026 R$1.670, regime por CNPJ [Eletrônica lucro real 01/2026→futuro Simples; Patrimonial Simples CNAE 8111-7/00], ISS Manaus, retenção INSS 11%, FGTS 8%, liminares PIS/COFINS zerados + INSS não retido Patrimonial; extrair de `cfo_service._contexto_empresa` e MEMORY.md — NÃO inventar). `## Playbook: <procedimento>` (CFO "fechamento do mês", fiscal "apuração", jurídico "dossiê processo READ-ONLY", operacional "cobrir posto"→propor_substituicao, comercial "qualificar+proposta"→propor_proposta_comercial: situação → passos → o que verificar → qual tool gated usar). Fato não-verificável = `> _(aguardando confirmação Jordan)_`.
- [ ] **Step 2:** Teste: T1 carrega cada um dos 8 e devolve seções não-vazias; nenhuma seção com número fabricado (checar que valores citados batem com _contexto_empresa/MEMORY). Rodar na bancada.
- [ ] **Step 3:** Commit `content(5.4b): conhecimento+playbooks dos 8 agentes (fatos ancorados)`.

---

### Task 3: Injeção nas personas

**Files:**
- Modify: `backend/modules/financial/cfo_service.py`, `backend/modules/fiscal/consultor_fiscal_service.py`, `backend/modules/juridico/consultor_service.py`, `backend/modules/gedeon/services/consultor_service.py`, `backend/modules/crm/services/consultor_cmo_service.py`, `backend/modules/operacional/services/consultor_coo_service.py`, `backend/modules/people_management/services/consultor_chro_service.py`, `backend/modules/config/services/consultor_ceo_service.py`
- Modify: `backend/modules/ai/conversation/controllers/consultor_escopado_controller.py` (_SYSTEM_BASE), `backend/modules/ai/conversation/services/orquestrador/portal_cliente.py` (_SYSTEM_CLIENTE)
- Modify: `hermes-runtime/SOUL.md` (seção `## Playbooks`)

**Interfaces:** Consome T1 (`contexto_para_prompt(agente, pergunta)`).

- [ ] **Step 1:** Em cada consultor_*_service, no ponto onde o system_prompt é montado (onde já concatena `_contexto_empresa`/`contexto_compartilhado`), adicionar `+ consultor_conhecimento_service.contexto_para_prompt("<agente>", pergunta)`. Mapear o nome do agente (cfo/fiscal/juridico/ged/comercial/operacional/chro/ceo). No orquestrador escopado e portal, idem no _SYSTEM_BASE/_SYSTEM_CLIENTE (agente por módulo/tier). Import lazy p/ evitar ciclo.
- [ ] **Step 2:** Hermes SOUL.md: adicionar seção `## Playbooks` (texto dos procedimentos da diretoria; Hermes já orquestra tools MCP — só orientação).
- [ ] **Step 3:** Teste: para cada agente, montar o system_prompt com um .md de teste presente → contém o bloco "[CONHECIMENTO DE DOMINIO...]"; com .md ausente → prompt inalterado (fail-open). Rodar na bancada (chamar as funções de montagem de prompt, não o LLM).
- [ ] **Step 4:** Commit `feat(5.4b): injeta conhecimento de dominio no system_prompt dos agentes + Hermes SOUL`.

---

### Task 4: Guarda anti-burla + suite-oráculo

**Files:**
- Create: `backend/scripts/orq/test_oraculos_5_4b.py`

- [ ] **Step 1:** Oráculos: (1) fail-open (.md ausente → "" sem exceção); (2) injeção presente quando .md existe / ausente quando não; (3) nunca-fabricar (serviço só devolve o que está no .md — teste com .md de valor conhecido, confirma que a saída não acrescenta número); (4) **anti-burla**: varrer via AST `backend/modules/ai/conversation/services/orquestrador/acoes/*.py` — todo handler de ação (que não seja a primitiva `base.propor`) NÃO pode conter call/import de execução direta (`executar_lote`, `gerar_otp`, `enviar_pix`, `transmitir_evento`, `pagar`, `.emitir(` fora de proposta) — tem que passar por `propor()`.
- [ ] **Step 2:** MUTAÇÃO-TESTE (molde 5.4): (M1) criar um handler-copy no throwaway que chama `executar_lote` direto → o oráculo anti-burla FALHA (morde). (M2) .md com número, adulterar o serviço p/ concatenar texto extra → oráculo nunca-fabricar FALHA.
- [ ] **Step 3:** Rodar na bancada; 4 oráculos PASS + 2 mutações mordem. Limpeza 0 remanescentes.
- [ ] **Step 4:** Commit `test(5.4b): oraculos conhecimento + guarda anti-burla + mutacao`.

---

### Task 5: Deploy

- [ ] **Step 1:** Verificar volume: `uploads/agent_knowledge/consultores/` montado no backend (docker-compose volumes). Se o volume já monta `uploads/`, os .md aparecem sem rebuild; senão, docker cp dos .md pro container.
- [ ] **Step 2:** Backend blue-green (`scripts/deploy_backend_bluegreen.sh` — baka o serviço novo + injeções). SEM reciclar celery (sem task celery). OOM-safe: verificar headroom; lock via o próprio script.
- [ ] **Step 3:** Hermes: `docker cp hermes-runtime/SOUL.md conecta-pro-hermes:/data/SOUL.md` + restart hermes (SOUL é read-at-boot).
- [ ] **Step 4:** Verificação: health 200; boot log sem erro; um consultor respondendo pergunta de domínio cita fato do .md (curl autenticado ou Playwright leve). Confirmar "ensina sem rebuild": editar um .md, nova pergunta reflete (sem redeploy).
- [ ] **Step 5:** Commit (se houver ajuste de volume) + push com OK do Jordan.

---

## Self-Review
- **Cobertura:** serviço (T1), conteúdo (T2), injeção incl. Hermes (T3), anti-burla+oráculos+mutação (T4), deploy+ensina-sem-rebuild (T5). Cobre todos os componentes da spec.
- **Placeholders:** código do serviço completo em T1; conteúdo com regra explícita de "fato não-verificável = aguardando Jordan" (não é placeholder de plano, é regra anti-fabricação).
- **Consistência de tipos:** `contexto_para_prompt(agente, pergunta, *, max_secoes)` usado igual em T3.
