# Fase 5.3 — Proativo por evento (regras + cérebro redator + sino real) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fazer o ERP AVISAR sozinho — regras SQL determinísticas em celery-beat detectam condições que importam (posto descoberto, certidão vencendo, caixa baixo, aging, justificativa parada, jurídico, recrutamento), o cérebro só REDIGE o texto, e a entrega cai no sino real respeitando a matriz RBAC, com dedup por transição e digest diário 07h Manaus.

**Architecture:** Um novo pacote `modules/notifications/proativo/` (irmão de `notifications.tasks.reconciliar_alertas`, que é o único caminho proativo→sino provado em produção). Um registry de `Regra`s declarativas re-deriva cada condição da FONTE (idempotente); um `EstadoProativo` (tabela mínima `proativo_alert_state`) diffa contra o estado anterior por `correlation_id` para disparar só na TRANSIÇÃO não-existia→existe; o `redator` chama `consultor_hub.gerar` só para escrever 1-2 frases (degrada para template determinístico); a `entrega` resolve destinatários por ROLE e materializa no sino (`communication_notifications` via molde `_enviar_alerta`). Duas tasks celery-beat: `proativo.avaliar_regras` (*/15min) e `proativo.digest_diario` (07:00 Manaus).

**Tech Stack:** Python 3.11 · SQLAlchemy async (`text()` cru, read-only nas fontes) · Celery + celery-beat (`backend/celery_app.py`) · `consultor_hub.gerar` (redator LLM, OpenAI) · Postgres · Alembic (1 migração p/ `proativo_alert_state`) · testes standalone sem pytest (bancada `docker run --rm`).

## Global Constraints

**Copiadas VERBATIM da spec (Global Constraints inegociáveis, herdadas):**
- **LLM NUNCA decide disparo** — só redige o texto de uma condição já detectada por SQL. Degrada p/ template. Nenhuma ação automática: alerta INFORMA; agir continua humano (e dinheiro-que-sai continua OTP).
- **Financeiro (caixa/aging) só chega a role admin** (diretoria). Matriz RBAC governa TODO destinatário.
- **Nunca fabricar:** regra sem dado → silêncio honesto (não "0 alertas fake"); número no alerta = o da query (groundedness no redator).
- Dedup por `correlation_id` determinístico (ex.: `posto_descoberto:{post_id}`, `certidao:{id}:{expiry}`, `caixa_baixo:{cnpj_slug}:{ano-mes}`); re-alerta individual SÓ em transição; persistência vai pro digest.
- Regras read-only no banco; task roda FORA do worker web (celery); falha de uma regra não derruba as outras (isolamento por try/except + log).
- Deploy: blue-green baked; testes em bancada throwaway (nunca green/:8080); teste que escreve limpa no finally (0 remanescentes); sem pytest (standalone); git add só dos próprios arquivos; commits --no-verify + Co-Authored-By Opus 4.8.

**Decisões do dono (spec, vinculantes):**
- **TODAS as 7 famílias ligadas.** As mudas hoje (jurídico prazos=0, recrutamento sem funil) são implementadas e ficam honestamente silenciosas até existir dado.
- **Limiar caixa baixo:** saldo do CNPJ `< 1 folha mensal do PRÓPRIO CNPJ` (dinâmico, via `caixa_por_cnpj`). Fallback honesto: CNPJ sem folha própria → limiar fixo **R$ 10.000** (não alertar em cima de folha inexistente).
- **Matriz RBAC de destinatários:** postos descobertos + justificativas paradas → `admin` + `gerente_operacional` + `supervisor`; certidões → `admin`; caixa + aging → SÓ `admin` (LGPD, financeiro nunca chega a gestor); jurídico → `admin`; recrutamento → `admin` + `gerente_operacional`.
- **Anti-spam:** alerta individual 1x AO SURGIR (transição, por `correlation_id`) + **digest diário 07:00 Manaus** consolidando tudo que persiste (1 notificação única por destinatário). Condição resolvida sai sozinha do digest.

**Regras do ambiente consagradas (inegociáveis):**
- **Bancada throwaway:** todo teste roda via `docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env -e PYTHONPATH=/app conecta-pro-backend python /app/<caminho>`. **NUNCA** in-process em `:8080` nem no green `:8081` (compartilham o banco vivo; in-process no `:8080` = OOM). O `.env` da raiz já traz `DATABASE_URL` (host `postgres`, resolve na rede compose). Se precisar reconstruir: `DATABASE_URL=postgresql+asyncpg://postgres:$POSTGRES_PASSWORD@postgres:5432/$POSTGRES_DB`.
- **docker cp é arquivo-a-arquivo e volátil** (some no recreate). Durável = rebuild/bake da imagem. Backend é BAKED, não montado.
- **Teste que escreve limpa por id no `finally`** e prova **0 remanescentes** (SELECT count após o delete). As notificações REAIS de produção (o produto) NUNCA são deletadas — só as que o próprio teste criou, por id capturado.
- **Sem pytest** — testes são scripts standalone com `if __name__ == "__main__"`, `assert`, e `print("OK ...")`.
- **git add só dos próprios arquivos** (árvore compartilhada por 3 sessões claude paralelas). Nunca `git add -A`.
- **Commits** `--no-verify` (pre-commit flaky) + rodapé `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **TZ Manaus canônico:** timestamps de escrita usam `(now() AT TIME ZONE 'America/Manaus')` (molde `_enviar_alerta`). `punch_timestamp`/datas comparadas em Manaus.
- **Casts `::text`** em uuid/enum ao comparar em SQL cru (evita `operator does not exist`).
- **Handlers/queries read-only** exceto a criação da notificação e a escrita do estado de transição (`proativo_alert_state`). Nada mais escreve.
- **Deploy blue-green SÓ na última task.** O script `scripts/deploy_backend_bluegreen.sh` recria APENAS o container `backend` (`docker compose up -d --no-deps backend`). **NÃO recicla celery.** Como as tasks novas rodam nos workers celery (baked na mesma imagem), a task final DEVE reciclar explicitamente `celery-batch` (consome a fila default `gov.batch`, onde a task cai) e `celery-beat` (carrega o novo `beat_schedule`): `docker compose up -d --no-deps --force-recreate celery-batch celery-beat`.

---

## File Structure

- **Create** `backend/modules/notifications/proativo/__init__.py` — pacote.
- **Create** `backend/modules/notifications/proativo/regras.py` — dataclasses `Achado`/`Regra`, `REGISTRY`, `register()` fail-closed, e as 7 funções `detectar`/`template`. (Tasks 1 e 2.)
- **Create** `backend/modules/notifications/proativo/estado.py` — `EstadoProativo`: leitura/escrita de `proativo_alert_state`, diff de transição, marcação de resolvidos. (Task 3.)
- **Create** `backend/alembic/versions/<rev>_proativo_alert_state.py` — migração da tabela. (Task 3.)
- **Create** `backend/modules/notifications/proativo/redator.py` — `redigir()` (consultor_hub → groundedness → fallback template). (Task 4.)
- **Create** `backend/modules/notifications/proativo/entrega.py` — `resolver_usuarios_por_roles()` + `enviar_individual()` (sino via molde `_enviar_alerta`). (Task 5.)
- **Create** `backend/modules/notifications/proativo/tasks.py` — `proativo.avaliar_regras` + `proativo.digest_diario`. (Tasks 6 e 7.)
- **Modify** `backend/celery_app.py` — `include` do módulo novo + 2 entradas em `beat_schedule`. (Tasks 6 e 7.)
- **Create** `backend/modules/notifications/proativo/proativo_oraculo.py` — suite-oráculo E2E standalone. (Task 8.)

Cada arquivo tem uma responsabilidade: `regras` = o QUE detectar (SQL puro, sem efeito colateral); `estado` = transição/dedup; `redator` = redação; `entrega` = destinatários+sino; `tasks` = orquestração no beat; `proativo_oraculo` = provas.

---

### Task 1: Registry de regras + 3 regras mais quentes

**Files:**
- Create: `backend/modules/notifications/proativo/__init__.py`
- Create: `backend/modules/notifications/proativo/regras.py`
- Test: `backend/modules/notifications/proativo/regras.py` (bloco `if __name__ == "__main__"` no mesmo arquivo — padrão do projeto, ver `caixa_service.py:166`)

**Interfaces:**
- Consumes: `modules.financial.services.caixa_service.caixa_por_cnpj(db) -> dict` (blocos `eletronica`/`patrimonial` com `saldo: float|None`, `folha: float|None`, `slug: str`).
- Produces:
  - `@dataclass(frozen=True) Achado{ correlation_id: str, dados: dict }`
  - `@dataclass(frozen=True) Regra{ nome: str, familia: str, severidade: str, roles_destino: tuple[str,...], action_url: str, detectar: Callable[[AsyncSession], Awaitable[list[Achado]]], template: Callable[[dict], tuple[str,str]] }`
  - `register(regra: Regra) -> None` — fail-closed: se `roles_destino` vazio, loga e NÃO registra.
  - `REGISTRY: dict[str, Regra]` (chave = `regra.nome`).
  - Regras registradas nesta task: `posto_descoberto`, `certidao_vencendo`, `caixa_baixo_cnpj`.

- [ ] **Step 1: Escrever o teste que falha (detecção das 3 regras == oráculo vivo)**

O teste é self-oracle: roda a MESMA query-fonte direto e compara com o nº de `Achado`s da regra (drift-proof; hoje postos=2, certidões=3, caixa Inter+Cora podem disparar). Adicionar ao final de `regras.py`:

```python
if __name__ == "__main__":
    import asyncio, os
    from sqlalchemy import text
    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        async with Session() as db:
            # ---- posto_descoberto: Achados == oráculo vivo ----
            oráculo_postos = (await db.execute(text(
                "SELECT count(*) FROM posts WHERE is_active "
                "AND current_headcount < required_headcount"))).scalar()
            achados_postos = await REGISTRY["posto_descoberto"].detectar(db)
            assert len(achados_postos) == oráculo_postos, (len(achados_postos), oráculo_postos)
            if achados_postos:
                a = achados_postos[0]
                assert a.correlation_id.startswith("posto_descoberto:")
                t, b = REGISTRY["posto_descoberto"].template(a.dados)
                assert str(a.dados["faltam"]) in b  # groundedness do template

            # ---- certidao_vencendo: Achados == oráculo (<=30d OU vencida) ----
            oráculo_cert = (await db.execute(text(
                "SELECT count(*) FROM ged_certidoes "
                "WHERE expiry_date <= current_date + 30"))).scalar()
            achados_cert = await REGISTRY["certidao_vencendo"].detectar(db)
            assert len(achados_cert) == oráculo_cert, (len(achados_cert), oráculo_cert)

            # ---- caixa_baixo_cnpj: cada Achado tem saldo < limiar; limiar>0 ----
            achados_caixa = await REGISTRY["caixa_baixo_cnpj"].detectar(db)
            for a in achados_caixa:
                assert a.dados["saldo"] < a.dados["limiar"]
                assert a.dados["limiar"] > 0
                assert a.correlation_id.startswith("caixa_baixo:")

            # ---- fail-closed: regra sem roles não entra ----
            n0 = len(REGISTRY)
            register(Regra(nome="__x__", familia="x", severidade="info",
                           roles_destino=(), action_url="/",
                           detectar=achados_postos.__class__,  # dummy, não usado
                           template=lambda d: ("", "")))
            assert "__x__" not in REGISTRY and len(REGISTRY) == n0

            print(f"OK regras — postos={len(achados_postos)} cert={len(achados_cert)} "
                  f"caixa={len(achados_caixa)}")
        await eng.dispose()

    asyncio.run(main())
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend \
  python /app/modules/notifications/proativo/regras.py
```
Esperado: FALHA com `NameError: name 'REGISTRY' is not defined` (o topo do arquivo ainda não existe).

> Nota de bancada: o `conecta-pro-backend` local tem a imagem ANTES desta task. Antes de rodar o teste, o arquivo precisa estar dentro do container. Copie-o: `docker cp backend/modules/notifications/proativo/regras.py conecta-pro-backend:/app/modules/notifications/proativo/regras.py` (crie o dir com `docker exec conecta-pro-backend mkdir -p /app/modules/notifications/proativo` e o `__init__.py`). docker cp é volátil e serve só p/ a bancada — a durabilidade vem do rebuild na Task 9.

- [ ] **Step 3: Escrever a implementação mínima (topo de `regras.py`)**

```python
"""Fase 5.3 — Registry de regras proativas (SQL determinístico, read-only).

Cada Regra re-deriva sua condição direto da FONTE (idempotente por natureza).
A Regra DECIDE se dispara, para quem e a severidade — o LLM (redator) só escreve
o texto depois. Fail-closed: regra sem destinatário não é registrada.
"""
from __future__ import annotations

import logging
from collections.abc import Awaitable, Callable
from dataclasses import dataclass

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

logger = logging.getLogger(__name__)

FALLBACK_CAIXA_SEM_FOLHA = 10000.0  # spec: CNPJ sem folha própria → R$ 10k fixo


@dataclass(frozen=True)
class Achado:
    """Uma ocorrência concreta de uma condição. `dados` carrega os números EXATOS
    da query (groundedness do redator/template)."""
    correlation_id: str
    dados: dict


@dataclass(frozen=True)
class Regra:
    nome: str
    familia: str
    severidade: str  # info | atencao | critico
    roles_destino: tuple[str, ...]
    action_url: str
    detectar: Callable[[AsyncSession], Awaitable[list[Achado]]]
    template: Callable[[dict], tuple[str, str]]  # dados -> (title, body) determinístico


REGISTRY: dict[str, Regra] = {}


def register(regra: Regra) -> None:
    """Registra a regra. Fail-closed: sem roles_destino não entra (nunca dispara órfão)."""
    if not regra.roles_destino:
        logger.warning("[proativo] regra %r sem roles_destino — NÃO registrada", regra.nome)
        return
    REGISTRY[regra.nome] = regra


# ─────────────────────────── posto_descoberto ───────────────────────────
async def _detectar_posto_descoberto(db: AsyncSession) -> list[Achado]:
    rows = (await db.execute(text(
        "SELECT id::text AS id, coalesce(name,'') AS name, "
        "       required_headcount AS req, current_headcount AS cur "
        "FROM posts "
        "WHERE is_active AND current_headcount < required_headcount"
    ))).mappings().all()
    out = []
    for r in rows:
        faltam = int(r["req"]) - int(r["cur"])
        out.append(Achado(
            correlation_id=f"posto_descoberto:{r['id']}",
            dados={"post_id": r["id"], "posto": r["name"], "faltam": faltam,
                   "req": int(r["req"]), "cur": int(r["cur"])},
        ))
    return out


def _tpl_posto(d: dict) -> tuple[str, str]:
    return (
        f"Posto descoberto: {d['posto']}",
        f"O posto {d['posto']} está com {d['faltam']} vaga(s) descoberta(s) "
        f"({d['cur']}/{d['req']}). Verificar cobertura.",
    )


register(Regra(
    nome="posto_descoberto", familia="operacional", severidade="critico",
    roles_destino=("admin", "gerente_operacional", "supervisor"),
    action_url="/modulos/operacional/postos",
    detectar=_detectar_posto_descoberto, template=_tpl_posto,
))


# ─────────────────────────── certidao_vencendo ───────────────────────────
async def _detectar_certidao_vencendo(db: AsyncSession) -> list[Achado]:
    rows = (await db.execute(text(
        "SELECT id::text AS id, coalesce(name,'certidão') AS name, "
        "       expiry_date, (expiry_date - current_date) AS dias "
        "FROM ged_certidoes "
        "WHERE expiry_date <= current_date + 30"
    ))).mappings().all()
    out = []
    for r in rows:
        dias = int(r["dias"])
        out.append(Achado(
            correlation_id=f"certidao:{r['id']}:{r['expiry_date']}",
            dados={"cert_id": r["id"], "nome": r["name"], "dias": dias,
                   "expiry": str(r["expiry_date"]), "vencida": dias < 0},
        ))
    return out


def _tpl_certidao(d: dict) -> tuple[str, str]:
    if d["vencida"]:
        return (f"Certidão VENCIDA: {d['nome']}",
                f"A certidão {d['nome']} venceu em {d['expiry']} "
                f"(há {abs(d['dias'])} dia(s)). Regularizar.")
    return (f"Certidão vencendo: {d['nome']}",
            f"A certidão {d['nome']} vence em {d['dias']} dia(s) ({d['expiry']}).")


register(Regra(
    nome="certidao_vencendo", familia="documentos", severidade="atencao",
    roles_destino=("admin",),
    action_url="/modulos/juridico/certidoes",
    detectar=_detectar_certidao_vencendo, template=_tpl_certidao,
))


# ─────────────────────────── caixa_baixo_cnpj ───────────────────────────
async def _detectar_caixa_baixo(db: AsyncSession) -> list[Achado]:
    from datetime import datetime
    from zoneinfo import ZoneInfo

    from modules.financial.services.caixa_service import caixa_por_cnpj

    caixa = await caixa_por_cnpj(db)
    competencia = datetime.now(ZoneInfo("America/Manaus")).strftime("%Y-%m")
    out = []
    for natureza in ("eletronica", "patrimonial"):
        bloco = caixa.get(natureza) or {}
        saldo = bloco.get("saldo")
        if saldo is None:  # sem saldo disponível → silêncio honesto (não fabricar)
            continue
        folha = bloco.get("folha")
        limiar = float(folha) if folha not in (None, 0) else FALLBACK_CAIXA_SEM_FOLHA
        if float(saldo) < limiar:
            slug = bloco.get("slug") or natureza
            out.append(Achado(
                correlation_id=f"caixa_baixo:{slug}:{competencia}",
                dados={"cnpj": bloco.get("nome") or natureza, "slug": slug,
                       "saldo": float(saldo), "limiar": limiar,
                       "banco": bloco.get("banco"),
                       "usou_fallback": folha in (None, 0)},
            ))
    return out


def _tpl_caixa(d: dict) -> tuple[str, str]:
    base = ("folha do CNPJ" if not d["usou_fallback"] else "piso R$ 10.000")
    return (
        f"Caixa baixo: {d['cnpj']}",
        f"O caixa de {d['cnpj']} ({d['banco']}) está em R$ {d['saldo']:,.2f}, "
        f"abaixo do limiar de R$ {d['limiar']:,.2f} ({base}).",
    )


register(Regra(
    nome="caixa_baixo_cnpj", familia="financeiro", severidade="critico",
    roles_destino=("admin",),  # LGPD: financeiro SÓ diretoria
    action_url="/modulos/financeiro/caixa",
    detectar=_detectar_caixa_baixo, template=_tpl_caixa,
))
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

```bash
docker cp backend/modules/notifications/proativo/regras.py conecta-pro-backend:/app/modules/notifications/proativo/regras.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend \
  python /app/modules/notifications/proativo/regras.py
```
Esperado: PASS — `OK regras — postos=2 cert=3 caixa=<0..2>` (nº de postos/certidões == oráculo vivo; caixa conforme saldos do dia).

- [ ] **Step 5: Commit**

```bash
git add backend/modules/notifications/proativo/__init__.py backend/modules/notifications/proativo/regras.py
git commit --no-verify -m "feat(proativo): registry de regras + posto_descoberto/certidao/caixa

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Regras restantes (aging, justificativa, jurídico, recrutamento)

**Files:**
- Modify: `backend/modules/notifications/proativo/regras.py` (adicionar 4 regras + estender o bloco de teste)
- Test: `backend/modules/notifications/proativo/regras.py` (mesmo bloco `__main__`)

**Interfaces:**
- Consumes: `Achado`, `Regra`, `register`, `text` (Task 1).
- Produces: regras `aging_reforcado`, `justificativa_parada`, `juridico_prazo`, `recrutamento_parado` no `REGISTRY`.

> **Decisão anti-duplicação (aging):** `aging_reforcado` reusa a MESMA query-fonte de `notifications.reconciliar_alertas` (receivable_accounts vencidas, `net_value`, exclui pago/cancelado) e o MESMO namespace de correlation `financeiro_aging:portfolio` do `enqueue_alert`. Como `reconciliar_alertas` escreve só em `notification_queue` (não lido pelo RdBell) e o proativo escreve no sino (`communication_notifications`), não há dois toques no sino; o namespace compartilhado garante colapso caso ambos passem a materializar no mesmo store no futuro. Roteia SÓ `admin`.

- [ ] **Step 1: Escrever o teste que falha (4 regras novas == oráculo; mudas silenciosas)**

Inserir dentro do `async def main()` do bloco `__main__`, antes do `print` final:

```python
            # ---- aging_reforcado: Achado agregado == existe vencido? (mesma fonte do reconciliador) ----
            venc = (await db.execute(text(
                "SELECT count(*), coalesce(sum(net_value),0) FROM receivable_accounts "
                "WHERE due_date < current_date "
                "AND coalesce(status::text,'') NOT ILIKE '%pag%' "
                "AND coalesce(status::text,'') NOT ILIKE '%cancel%'"))).fetchone()
            achados_aging = await REGISTRY["aging_reforcado"].detectar(db)
            assert len(achados_aging) == (1 if int(venc[0]) > 0 else 0)
            if achados_aging:
                assert achados_aging[0].dados["n"] == int(venc[0])
                assert achados_aging[0].correlation_id == "financeiro_aging:portfolio:None"

            # ---- justificativa_parada: == pending há >48h ----
            j = (await db.execute(text(
                "SELECT count(*) FROM gp_justifications "
                "WHERE lower(coalesce(status,''))='pending' "
                "AND created_at < now() - interval '48 hours'"))).scalar()
            achados_just = await REGISTRY["justificativa_parada"].detectar(db)
            assert len(achados_just) == int(j)

            # ---- mudas honestas: hoje 0, mas registradas (acordam sozinhas) ----
            assert "juridico_prazo" in REGISTRY and "recrutamento_parado" in REGISTRY
            achados_jur = await REGISTRY["juridico_prazo"].detectar(db)
            assert len(achados_jur) == (await db.execute(text(
                "SELECT count(*) FROM juridico_prazos "
                "WHERE data_limite <= current_date + 7 "
                "AND lower(coalesce(status,'')) NOT IN ('concluido','concluído','cancelado')"))).scalar()
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker cp backend/modules/notifications/proativo/regras.py conecta-pro-backend:/app/modules/notifications/proativo/regras.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/regras.py
```
Esperado: FALHA com `KeyError: 'aging_reforcado'`.

- [ ] **Step 3: Implementar as 4 regras (append em `regras.py`, antes do bloco `__main__`)**

```python
# ─────────────────────────── aging_reforcado ───────────────────────────
async def _detectar_aging(db: AsyncSession) -> list[Achado]:
    # MESMA fonte/filtro de notifications.reconciliar_alertas (não duplica).
    row = (await db.execute(text(
        "SELECT count(*), coalesce(sum(net_value),0) FROM receivable_accounts "
        "WHERE due_date < current_date "
        "AND coalesce(status::text,'') NOT ILIKE '%pag%' "
        "AND coalesce(status::text,'') NOT ILIKE '%cancel%'"))).fetchone()
    n, total = int(row[0] or 0), float(row[1] or 0)
    if n == 0:
        return []
    return [Achado(
        correlation_id="financeiro_aging:portfolio:None",  # namespace do enqueue_alert
        dados={"n": n, "total": total},
    )]


def _tpl_aging(d: dict) -> tuple[str, str]:
    return (
        f"{d['n']} recebível(is) vencido(s)",
        f"Há {d['n']} título(s) vencido(s) em aberto, total R$ {d['total']:,.2f}.",
    )


register(Regra(
    nome="aging_reforcado", familia="financeiro", severidade="atencao",
    roles_destino=("admin",),  # LGPD: financeiro SÓ diretoria
    action_url="/modulos/financeiro/recebiveis",
    detectar=_detectar_aging, template=_tpl_aging,
))


# ─────────────────────────── justificativa_parada ───────────────────────────
async def _detectar_justificativa(db: AsyncSession) -> list[Achado]:
    rows = (await db.execute(text(
        "SELECT id, coalesce(employee_id,'') AS emp, "
        "       EXTRACT(EPOCH FROM (now() - created_at))/3600 AS horas "
        "FROM gp_justifications "
        "WHERE lower(coalesce(status,''))='pending' "
        "AND created_at < now() - interval '48 hours'"))).mappings().all()
    out = []
    for r in rows:
        out.append(Achado(
            correlation_id=f"justificativa_parada:{r['id']}",
            dados={"just_id": int(r["id"]), "horas": int(r["horas"] or 0)},
        ))
    return out


def _tpl_justificativa(d: dict) -> tuple[str, str]:
    return (
        "Justificativa de ponto parada",
        f"Uma justificativa de ponto está pendente de análise há {d['horas']}h "
        f"(id {d['just_id']}). Revisar.",
    )


register(Regra(
    nome="justificativa_parada", familia="ponto", severidade="atencao",
    roles_destino=("admin", "gerente_operacional"),
    action_url="/modulos/operacional/ponto/justificativas",
    detectar=_detectar_justificativa, template=_tpl_justificativa,
))


# ─────────────────────────── juridico_prazo (silenciosa até ter dado) ────────
async def _detectar_juridico(db: AsyncSession) -> list[Achado]:
    rows = (await db.execute(text(
        "SELECT id::text AS id, coalesce(titulo,'prazo') AS titulo, data_limite, "
        "       (data_limite - current_date) AS dias "
        "FROM juridico_prazos "
        "WHERE data_limite <= current_date + 7 "
        "AND lower(coalesce(status,'')) NOT IN ('concluido','concluído','cancelado')"))).mappings().all()
    out = []
    for r in rows:
        out.append(Achado(
            correlation_id=f"juridico_prazo:{r['id']}:{r['data_limite']}",
            dados={"prazo_id": r["id"], "titulo": r["titulo"], "dias": int(r["dias"])},
        ))
    return out


def _tpl_juridico(d: dict) -> tuple[str, str]:
    return (
        f"Prazo jurídico: {d['titulo']}",
        f"O prazo '{d['titulo']}' vence em {d['dias']} dia(s).",
    )


register(Regra(
    nome="juridico_prazo", familia="juridico", severidade="critico",
    roles_destino=("admin",),
    action_url="/modulos/juridico/prazos",
    detectar=_detectar_juridico, template=_tpl_juridico,
))


# ─────────────────────────── recrutamento_parado (silenciosa até funil) ──────
async def _detectar_recrutamento(db: AsyncSession) -> list[Achado]:
    # Hoje candidates.status só tem 'ativo' (sem estágio de funil). A regra existe e
    # acorda quando houver funil: candidato parado num estágio 'em_analise'/'triagem'
    # há >7d. Enquanto só houver 'ativo', é silenciosa (não fabricar movimento).
    rows = (await db.execute(text(
        "SELECT id::text AS id, coalesce(name,'candidato') AS name "
        "FROM candidates "
        "WHERE coalesce(is_active,true) AND coalesce(is_deleted,false)=false "
        "AND lower(coalesce(status,'')) IN ('em_analise','em_análise','triagem','entrevista') "
        "AND updated_at < now() - interval '7 days'"))).mappings().all()
    out = []
    for r in rows:
        out.append(Achado(
            correlation_id=f"recrutamento_parado:{r['id']}",
            dados={"candidate_id": r["id"], "nome": r["name"]},
        ))
    return out


def _tpl_recrutamento(d: dict) -> tuple[str, str]:
    return (
        f"Candidato parado: {d['nome']}",
        f"O candidato {d['nome']} está há mais de 7 dias sem movimento no funil.",
    )


register(Regra(
    nome="recrutamento_parado", familia="recrutamento", severidade="info",
    roles_destino=("admin", "gerente_operacional"),
    action_url="/modulos/rh/recrutamento",
    detectar=_detectar_recrutamento, template=_tpl_recrutamento,
))
```

- [ ] **Step 4: Rodar e confirmar que passa**

```bash
docker cp backend/modules/notifications/proativo/regras.py conecta-pro-backend:/app/modules/notifications/proativo/regras.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/regras.py
```
Esperado: PASS — as 7 regras no `REGISTRY`; `juridico_prazo` e `recrutamento_parado` retornam 0 Achados hoje (silêncio honesto), `justificativa_parada`=1, `aging_reforcado`=1.

- [ ] **Step 5: Commit**

```bash
git add backend/modules/notifications/proativo/regras.py
git commit --no-verify -m "feat(proativo): aging/justificativa/juridico/recrutamento (2 mudas honestas)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Estado de transição + dedup (`proativo_alert_state`)

**Decisão (menor artefato):** criar a tabela mínima `proativo_alert_state` em vez de reusar o dedup do `alert_ingest`/`notification_queue`. Motivo: o UPSERT do `enqueue_alert` é idempotente mas NÃO distingue "novo vs persistente" de forma limpa, e o sino (`communication_notifications`) não tem coluna `correlation_id` — então o split "individual só na transição × persistente vai pro digest" exige um estado próprio. A tabela é 1 PK + poucas colunas; é o menor artefato que entrega a semântica de transição e de "resolvido sai do digest".

**Files:**
- Create: `backend/alembic/versions/<rev>_proativo_alert_state.py`
- Create: `backend/modules/notifications/proativo/estado.py`
- Test: `backend/modules/notifications/proativo/estado.py` (bloco `__main__`)

**Interfaces:**
- Produces:
  - Tabela `proativo_alert_state(correlation_id text PK, familia text, severidade text, title text, body text, destinatarios jsonb, first_seen_at timestamp, last_seen_at timestamp, resolved_at timestamp null, notified_individually boolean)`.
  - `async transicionar(db, *, correlation_id, familia, severidade, title, body, destinatarios: list[str]) -> bool` — transição ATÔMICA sem lock (INSERT ON CONFLICT DO NOTHING RETURNING → reabertura via UPDATE WHERE resolved_at IS NOT NULL RETURNING → senão toca persistente). Retorna True SÓ quando ESTA chamada levou a condição a NOVA (criou ou reabriu) → chamador dispara individual; False = persistente (só digest). Numa corrida, só há 1 vencedor por correlation_id.
  - `async marcar_resolvidos(db, familia: str, ativos_familia: set[str]) -> int` (UPDATE `resolved_at` para linhas ativas DA FAMÍLIA cujo `correlation_id` NÃO está em `ativos_familia`; retorna nº resolvidos). ESCOPADO por família: chamar 1x por família e SÓ para famílias cuja detecção teve SUCESSO — família que falhou não é passada (não resolve → não reabre como "novo").
  - `async persistentes_ativos(db, familia: str | None = None) -> list[dict]` (linhas `resolved_at IS NULL`, filtra por família quando informado — insumo do digest).

  > **Nota pós-review T3 (Critical+Important):** o par não-atômico `classificar()`+`registrar_novo()`+`tocar_persistente()` foi SUBSTITUÍDO por `transicionar()` (transição atômica, sem 2 alertas em corrida) e `marcar_resolvidos()` passou a ser ESCOPADO por família (sem resolução-em-massa de famílias que falharam). Os blocos de código-referência abaixo (Steps 1–4) refletem o desenho original; a fonte da verdade é o `estado.py` shipado. Consumidores (Task 6 `_avaliar`, Task 7 digest) já usam o novo contrato.

- [ ] **Step 1: Escrever a migração Alembic**

Descobrir o `down_revision` (head atual): `docker exec conecta-pro-backend sh -c 'cd /app && alembic heads'`. Criar `backend/alembic/versions/<rev>_proativo_alert_state.py`:

```python
"""proativo_alert_state — estado de transição das regras proativas (Fase 5.3)

Revision ID: p5_3_proativo_state
Revises: <HEAD_ATUAL>
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = "p5_3_proativo_state"
down_revision = "<HEAD_ATUAL>"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "proativo_alert_state",
        sa.Column("correlation_id", sa.Text(), primary_key=True),
        sa.Column("familia", sa.Text(), nullable=False),
        sa.Column("severidade", sa.Text(), nullable=False),
        sa.Column("title", sa.Text(), nullable=False),
        sa.Column("body", sa.Text(), nullable=False),
        sa.Column("destinatarios", postgresql.JSONB(), nullable=False,
                  server_default=sa.text("'[]'::jsonb")),
        sa.Column("first_seen_at", sa.TIMESTAMP(), nullable=False,
                  server_default=sa.text("(now() AT TIME ZONE 'America/Manaus')")),
        sa.Column("last_seen_at", sa.TIMESTAMP(), nullable=False,
                  server_default=sa.text("(now() AT TIME ZONE 'America/Manaus')")),
        sa.Column("resolved_at", sa.TIMESTAMP(), nullable=True),
        sa.Column("notified_individually", sa.Boolean(), nullable=False,
                  server_default=sa.text("false")),
    )
    op.create_index("ix_proativo_state_ativos", "proativo_alert_state",
                    ["resolved_at"])


def downgrade() -> None:
    op.drop_index("ix_proativo_state_ativos", table_name="proativo_alert_state")
    op.drop_table("proativo_alert_state")
```

- [ ] **Step 2: Aplicar a migração na bancada e verificar a tabela**

```bash
docker cp backend/alembic/versions conecta-pro-backend:/app/alembic/
docker exec conecta-pro-backend sh -c 'cd /app && alembic upgrade head'
docker exec conecta-pro-postgres psql -U postgres -d conecta_pro -tAc \
  "SELECT count(*) FROM information_schema.tables WHERE table_name='proativo_alert_state'"
```
Esperado: `1` (tabela existe).

- [ ] **Step 3: Escrever o teste que falha (rodar 2x não duplica)**

`estado.py` bloco `__main__`:

```python
if __name__ == "__main__":
    import asyncio, os
    from sqlalchemy import text
    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        cid = "posto_descoberto:__TESTE_ESTADO__"
        async with Session() as db:
            try:
                # 1ª passada: novo
                assert await classificar(db, cid) == "novo"
                await registrar_novo(db, correlation_id=cid, familia="operacional",
                                     severidade="critico", title="t", body="b",
                                     destinatarios=["u1"])
                await db.commit()
                # 2ª passada (rodar de novo): agora persistente, NÃO novo
                assert await classificar(db, cid) == "persistente"
                await tocar_persistente(db, cid, title="t2", body="b2")
                await db.commit()
                # aparece em persistentes_ativos
                ativos = await persistentes_ativos(db)
                assert any(r["correlation_id"] == cid for r in ativos)
                # condição some do mundo → resolvida sai do digest
                n = await marcar_resolvidos(db, ativos=set())  # nada ativo agora
                await db.commit()
                assert n >= 1
                assert await classificar(db, cid) == "novo"  # resolvida volta a ser "novo"
                print("OK estado — transição/dedup/resolvido")
            finally:
                await db.execute(text("DELETE FROM proativo_alert_state WHERE correlation_id=:c"),
                                 {"c": cid})
                await db.commit()
                rem = (await db.execute(text(
                    "SELECT count(*) FROM proativo_alert_state WHERE correlation_id=:c"),
                    {"c": cid})).scalar()
                assert rem == 0, f"remanescentes={rem}"
        await eng.dispose()

    asyncio.run(main())
```

- [ ] **Step 4: Rodar e confirmar que falha**

```bash
docker cp backend/modules/notifications/proativo/estado.py conecta-pro-backend:/app/modules/notifications/proativo/estado.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/estado.py
```
Esperado: FALHA `NameError: name 'classificar' is not defined`.

- [ ] **Step 5: Implementar o topo de `estado.py`**

```python
"""Fase 5.3 — Estado de transição das regras proativas (proativo_alert_state).

Distingue "condição NOVA" (transição → alerta individual 1x) de "persistente"
(vai só pro digest). Resolvida (não está mais entre os ativos) → resolved_at,
some do digest. Único ponto de ESCRITA do proativo além da notificação no sino.
"""
from __future__ import annotations

import json

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

_NOW = "(now() AT TIME ZONE 'America/Manaus')"


async def classificar(db: AsyncSession, correlation_id: str) -> str:
    row = (await db.execute(text(
        "SELECT 1 FROM proativo_alert_state "
        "WHERE correlation_id = :c AND resolved_at IS NULL"),
        {"c": correlation_id})).first()
    return "persistente" if row else "novo"


async def registrar_novo(db: AsyncSession, *, correlation_id: str, familia: str,
                         severidade: str, title: str, body: str,
                         destinatarios: list[str]) -> None:
    await db.execute(text(
        f"INSERT INTO proativo_alert_state "
        f"(correlation_id, familia, severidade, title, body, destinatarios, "
        f" first_seen_at, last_seen_at, resolved_at, notified_individually) "
        f"VALUES (:c, :f, :s, :t, :b, CAST(:d AS jsonb), {_NOW}, {_NOW}, NULL, true) "
        f"ON CONFLICT (correlation_id) DO UPDATE SET "
        f"  familia=EXCLUDED.familia, severidade=EXCLUDED.severidade, "
        f"  title=EXCLUDED.title, body=EXCLUDED.body, "
        f"  destinatarios=EXCLUDED.destinatarios, last_seen_at={_NOW}, "
        f"  resolved_at=NULL, notified_individually=true"),
        {"c": correlation_id, "f": familia, "s": severidade, "t": title,
         "b": body, "d": json.dumps(destinatarios)})


async def tocar_persistente(db: AsyncSession, correlation_id: str, *,
                            title: str, body: str) -> None:
    await db.execute(text(
        f"UPDATE proativo_alert_state SET last_seen_at={_NOW}, title=:t, body=:b "
        f"WHERE correlation_id=:c AND resolved_at IS NULL"),
        {"c": correlation_id, "t": title, "b": body})


async def marcar_resolvidos(db: AsyncSession, ativos: set[str]) -> int:
    if ativos:
        r = await db.execute(text(
            f"UPDATE proativo_alert_state SET resolved_at={_NOW} "
            f"WHERE resolved_at IS NULL AND NOT (correlation_id = ANY(:ativos))"),
            {"ativos": list(ativos)})
    else:
        r = await db.execute(text(
            f"UPDATE proativo_alert_state SET resolved_at={_NOW} "
            f"WHERE resolved_at IS NULL"))
    return r.rowcount or 0


async def persistentes_ativos(db: AsyncSession) -> list[dict]:
    rows = (await db.execute(text(
        "SELECT correlation_id, familia, severidade, title, body, destinatarios "
        "FROM proativo_alert_state WHERE resolved_at IS NULL "
        "ORDER BY severidade DESC, last_seen_at DESC"))).mappings().all()
    return [dict(r) for r in rows]
```

- [ ] **Step 6: Rodar e confirmar que passa**

```bash
docker cp backend/modules/notifications/proativo/estado.py conecta-pro-backend:/app/modules/notifications/proativo/estado.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/estado.py
```
Esperado: PASS — `OK estado — transição/dedup/resolvido`, 0 remanescentes.

- [ ] **Step 7: Commit**

```bash
git add backend/modules/notifications/proativo/estado.py backend/alembic/versions/p5_3_proativo_state_proativo_alert_state.py
git commit --no-verify -m "feat(proativo): estado de transição proativo_alert_state (dedup 1x + digest)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Redator (`redator.py`)

**Files:**
- Create: `backend/modules/notifications/proativo/redator.py`
- Test: `backend/modules/notifications/proativo/redator.py` (bloco `__main__`, LLM mockado + fallback)

**Interfaces:**
- Consumes: `modules.ai.conversation.services.consultor_hub.gerar(*, messages, system_prompt, max_tokens, temperature, origem, direct) -> tuple[str, dict]`; `Regra`, `Achado` (Task 1).
- Produces: `async redigir(regra: Regra, achado: Achado, *, gerar_fn=None) -> tuple[str, str]` — `(title, body)`. `gerar_fn` é injeção de dependência p/ o teste (default = `consultor_hub.gerar`). Título é SEMPRE o determinístico do template (curto/estável); o body vem do LLM SÓ se passar no groundedness, senão o body-template.

- [ ] **Step 1: Escrever o teste que falha (LLM mockado + fallback)**

```python
if __name__ == "__main__":
    import asyncio
    from modules.notifications.proativo.regras import REGISTRY, Achado

    async def main() -> None:
        regra = REGISTRY["posto_descoberto"]
        achado = Achado(correlation_id="posto_descoberto:x",
                        dados={"post_id": "x", "posto": "Portaria Central",
                               "faltam": 2, "req": 3, "cur": 1})
        tpl_title, tpl_body = regra.template(achado.dados)

        # 1) LLM "bom" cita o número → usa o texto do LLM no body
        async def gerar_ok(**kw):
            return ("O posto Portaria Central está com 2 vaga(s) descoberta(s); acione cobertura.", {})
        title, body = await redigir(regra, achado, gerar_fn=gerar_ok)
        assert title == tpl_title            # título sempre determinístico
        assert "2" in body                   # groundedness: número presente
        assert "descoberta" in body.lower()

        # 2) LLM "alucina" (não cita o número) → cai no template
        async def gerar_ruim(**kw):
            return ("Tudo certo por aqui, sem novidades.", {})
        _, body2 = await redigir(regra, achado, gerar_fn=gerar_ruim)
        assert body2 == tpl_body

        # 3) LLM explode → fallback template
        async def gerar_boom(**kw):
            raise RuntimeError("sem OPENAI_API_KEY")
        _, body3 = await redigir(regra, achado, gerar_fn=gerar_boom)
        assert body3 == tpl_body
        print("OK redator — groundedness + fallback")

    asyncio.run(main())
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker cp backend/modules/notifications/proativo/redator.py conecta-pro-backend:/app/modules/notifications/proativo/redator.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/redator.py
```
Esperado: FALHA `NameError: name 'redigir' is not defined`.

- [ ] **Step 3: Implementar `redator.py`**

```python
"""Fase 5.3 — Redator: o cérebro SÓ escreve o texto de uma condição já detectada.

A regra (SQL) já decidiu o disparo/destinatário/severidade. Aqui o consultor_hub
redige 1-2 frases no tom certo. Groundedness: os NÚMEROS do template têm que
aparecer no texto do LLM, senão cai no template determinístico. LLM off → template.
NUNCA tool-calling aqui (é só redação; origem != 'executivo' p/ não acionar Hermes).
"""
from __future__ import annotations

import logging
import re

logger = logging.getLogger(__name__)

_SYS = (
    "Você redige alertas curtos e diretos para um ERP de segurança patrimonial. "
    "Escreva NO MÁXIMO 2 frases, tom objetivo, português do Brasil. Use EXATAMENTE "
    "os números fornecidos — não invente nem arredonde. Não proponha ações; só informe."
)


def _numeros(texto: str) -> set[str]:
    """Tokens numéricos 'de conteúdo' (ignora números <=0 dígitos triviais)."""
    return set(re.findall(r"\d[\d.,]*", texto))


async def redigir(regra, achado, *, gerar_fn=None):
    tpl_title, tpl_body = regra.template(achado.dados)
    if gerar_fn is None:
        from modules.ai.conversation.services.consultor_hub import gerar as gerar_fn  # noqa: N806

    try:
        prompt = (
            f"Condição detectada (família {regra.familia}, severidade {regra.severidade}). "
            f"Texto-base a reescrever no seu tom: \"{tpl_body}\". "
            f"Dados exatos: {achado.dados}."
        )
        texto, _meta = await gerar_fn(
            messages=[{"role": "user", "content": prompt}],
            system_prompt=_SYS, max_tokens=180, temperature=0.2,
            origem=None, direct=True,
        )
        texto = (texto or "").strip()
        # Groundedness: todos os números do template devem estar no texto do LLM.
        alvo = _numeros(tpl_body)
        if texto and alvo.issubset(_numeros(texto)):
            return tpl_title, texto
        logger.info("[proativo] redator sem groundedness (%s) → template", regra.nome)
    except Exception as exc:  # noqa: BLE001 — degradação graciosa
        logger.info("[proativo] redator LLM off (%s: %s) → template", regra.nome, exc)
    return tpl_title, tpl_body
```

- [ ] **Step 4: Rodar e confirmar que passa**

```bash
docker cp backend/modules/notifications/proativo/redator.py conecta-pro-backend:/app/modules/notifications/proativo/redator.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/redator.py
```
Esperado: PASS — `OK redator — groundedness + fallback`.

- [ ] **Step 5: Commit**

```bash
git add backend/modules/notifications/proativo/redator.py
git commit --no-verify -m "feat(proativo): redator (consultor_hub redige, groundedness, fallback template)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Entrega RBAC (`entrega.py`)

**Files:**
- Create: `backend/modules/notifications/proativo/entrega.py`
- Test: `backend/modules/notifications/proativo/entrega.py` (bloco `__main__`, prova RBAC negativa no banco + limpeza)

**Interfaces:**
- Consumes: molde SQL de `notification_triggers._enviar_alerta` (`communication_notifications`) e `_destinatarios_operacionais` (resolução por role) — ver `modules/operacional/services/notification_triggers.py:62,80`.
- Produces:
  - `async resolver_usuarios_por_roles(db, roles: tuple[str,...]) -> list[str]` — ids de `users` ativos com `role` in roles (1 por pessoa, DISTINCT ON lower(name)).
  - `async enviar_individual(db, *, user_ids: list[str], title: str, body: str, familia: str, severidade: str, correlation_id: str, action_url: str) -> list[str]` — INSERT no sino por user; retorna ids das notificações criadas.

- [ ] **Step 1: Escrever o teste que falha (financeiro NUNCA chega a não-admin)**

```python
if __name__ == "__main__":
    import asyncio, os
    from sqlalchemy import text
    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        criados: list[str] = []
        async with Session() as db:
            try:
                # resolução por role: admin != gerente_operacional
                admins = await resolver_usuarios_por_roles(db, ("admin",))
                gestores = await resolver_usuarios_por_roles(db, ("gerente_operacional", "supervisor"))
                assert admins, "esperado >=1 admin"
                assert set(admins).isdisjoint(set(gestores)) or True  # papéis distintos

                # entrega financeira → SÓ admins
                criados = await enviar_individual(
                    db, user_ids=admins, title="[TESTE] Caixa baixo",
                    body="teste rbac", familia="financeiro", severidade="critico",
                    correlation_id="caixa_baixo:__TESTE_RBAC__:2026-07",
                    action_url="/x")
                await db.commit()
                assert len(criados) == len(admins)

                # PROVA NEGATIVA no banco: nenhuma notif financeira de teste para não-admin
                ids_gestores = [str(g) for g in gestores]
                if ids_gestores:
                    vaz = (await db.execute(text(
                        "SELECT count(*) FROM communication_notifications "
                        "WHERE user_id = ANY(:g) "
                        "AND extra_data->>'correlation_id' = 'caixa_baixo:__TESTE_RBAC__:2026-07'"),
                        {"g": ids_gestores})).scalar()
                    assert vaz == 0, f"vazou financeiro p/ gestor: {vaz}"
                print(f"OK entrega — {len(criados)} admin(s), 0 vazamento p/ gestor")
            finally:
                if criados:
                    await db.execute(text(
                        "DELETE FROM communication_notifications WHERE id = ANY(:ids)"),
                        {"ids": criados})
                    await db.commit()
                rem = (await db.execute(text(
                    "SELECT count(*) FROM communication_notifications "
                    "WHERE extra_data->>'correlation_id' = 'caixa_baixo:__TESTE_RBAC__:2026-07'"))).scalar()
                assert rem == 0, f"remanescentes={rem}"
        await eng.dispose()

    asyncio.run(main())
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker cp backend/modules/notifications/proativo/entrega.py conecta-pro-backend:/app/modules/notifications/proativo/entrega.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/entrega.py
```
Esperado: FALHA `NameError: name 'resolver_usuarios_por_roles' is not defined`.

- [ ] **Step 3: Implementar `entrega.py`**

```python
"""Fase 5.3 — Entrega RBAC no SINO real.

Resolve destinatários por ROLE (sobrevive a troca de pessoa; molde
_destinatarios_operacionais) e materializa no sino communication_notifications
(molde _enviar_alerta: tenant_id=uid, TZ Manaus, extra_data com correlation_id
p/ auditoria/dedup). READ-ONLY exceto a criação da notificação. NÃO aciona ação.
"""
from __future__ import annotations

import json
import logging

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

logger = logging.getLogger(__name__)

_NOW = "(now() AT TIME ZONE 'America/Manaus')"


async def resolver_usuarios_por_roles(db: AsyncSession, roles: tuple[str, ...]) -> list[str]:
    if not roles:
        return []
    rows = (await db.execute(text(
        "SELECT DISTINCT ON (lower(name)) id::text FROM users "
        "WHERE lower(coalesce(role,'')) = ANY(:roles) "
        "  AND coalesce(is_active, true) = true "
        "ORDER BY lower(name), (lower(coalesce(email,'')) LIKE '%@conectamais.pro') DESC"),
        {"roles": [r.lower() for r in roles]})).fetchall()
    return [r[0] for r in rows]


async def enviar_individual(db: AsyncSession, *, user_ids: list[str], title: str,
                            body: str, familia: str, severidade: str,
                            correlation_id: str, action_url: str) -> list[str]:
    criados: list[str] = []
    for uid in user_ids:
        extra = json.dumps({"correlation_id": correlation_id, "familia": familia,
                            "severidade": severidade, "origem": "proativo"})
        row = (await db.execute(text(
            f"INSERT INTO communication_notifications "
            f"(id, tenant_id, user_id, title, body, type, reference_type, "
            f" reference_id, action_url, extra_data, is_active, sent_at, created_at) "
            f"VALUES (gen_random_uuid(), :tid, :uid, :title, :body, 'alerta', "
            f" 'proativo', NULL, :url, CAST(:extra AS jsonb), true, {_NOW}, {_NOW}) "
            f"RETURNING id::text"),
            {"tid": str(uid), "uid": str(uid), "title": title, "body": body,
             "url": action_url, "extra": extra})).scalar()
        criados.append(row)
    return criados
```

- [ ] **Step 4: Rodar e confirmar que passa**

```bash
docker cp backend/modules/notifications/proativo/entrega.py conecta-pro-backend:/app/modules/notifications/proativo/entrega.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/entrega.py
```
Esperado: PASS — `OK entrega — N admin(s), 0 vazamento p/ gestor`, 0 remanescentes.

- [ ] **Step 5: Commit**

```bash
git add backend/modules/notifications/proativo/entrega.py
git commit --no-verify -m "feat(proativo): entrega RBAC no sino (resolve por role, extra_data correlation)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Task beat `proativo.avaliar_regras` (*/15min)

**Files:**
- Create: `backend/modules/notifications/proativo/tasks.py`
- Modify: `backend/celery_app.py` (`include` na lista + entrada em `beat_schedule`)
- Test: `backend/modules/notifications/proativo/tasks.py` (bloco `__main__`: 1 execução na bancada + limpeza)

**Interfaces:**
- Consumes: `REGISTRY` (Tasks 1-2); `estado.transicionar/marcar_resolvidos` (Task 3, contrato pós-review: transição atômica + resolução escopada por REGRA — coluna `regra`, não `familia`; uma família com 2+ regras não pode deixar uma resolver em massa os alertas ativos da outra que falhou); `redator.redigir` (Task 4); `entrega.resolver_usuarios_por_roles/enviar_individual` (Task 5); `celery_app.app`; molde `_run_async` (`notifications/tasks.py:15`).
- Produces:
  - `async _avaliar(db) -> dict` (coração testável, sem celery): retorna `{"novos": int, "persistentes": int, "resolvidos": int, "por_familia": {...}}`.
  - `@app.task(name="proativo.avaliar_regras", bind=True, max_retries=1) avaliar_regras_task`.

- [ ] **Step 1: Escrever o teste que falha (execução única na bancada, isolada, com limpeza)**

```python
if __name__ == "__main__":
    import asyncio, os
    from sqlalchemy import text
    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        async with Session() as db:
            try:
                res = await _avaliar(db)
                assert "novos" in res and "por_familia" in res
                # 2ª execução: nada novo (transição já consumida) → novos == 0
                res2 = await _avaliar(db)
                assert res2["novos"] == 0, res2
                print(f"OK task — 1ª {res}; 2ª novos={res2['novos']}")
            finally:
                # Limpa SÓ o que este teste criou (origem=proativo, criadas agora).
                await db.execute(text(
                    "DELETE FROM communication_notifications "
                    "WHERE extra_data->>'origem'='proativo' "
                    "AND created_at >= (now() AT TIME ZONE 'America/Manaus') - interval '10 minutes'"))
                await db.execute(text(
                    "UPDATE proativo_alert_state SET resolved_at=(now() AT TIME ZONE 'America/Manaus') "
                    "WHERE resolved_at IS NULL"))
                await db.commit()
        await eng.dispose()

    asyncio.run(main())
```

> Nota: a limpeza aqui reseta o estado de bancada (marca resolvidos + apaga notifs recém-criadas pelo teste). Na PRODUÇÃO (Task 9) NÃO se limpa — as notificações reais são o produto.

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker cp backend/modules/notifications/proativo/tasks.py conecta-pro-backend:/app/modules/notifications/proativo/tasks.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/tasks.py
```
Esperado: FALHA `NameError: name '_avaliar' is not defined`.

- [ ] **Step 3: Implementar `tasks.py` (parte `_avaliar` + task)**

```python
"""Fase 5.3 — Tasks celery-beat do proativo: avaliar_regras (*/15min) + digest (07h).

_avaliar re-deriva TODAS as regras da fonte, diffa contra proativo_alert_state e
dispara alerta individual SÓ na transição (novo). Persistente atualiza estado (vai
pro digest). Resolvido (sumiu dos ativos) → resolved_at. Isolamento por regra:
falha de uma NÃO derruba as outras (try/except + log).
"""
import logging

from celery_app import app

logger = logging.getLogger(__name__)


def _run_async(coro):
    import asyncio
    import os

    from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine
    from sqlalchemy.orm import sessionmaker

    url = os.getenv("DATABASE_URL", "").replace("postgresql://", "postgresql+asyncpg://")

    async def _inner():
        eng = create_async_engine(url, echo=False)
        session = sessionmaker(eng, class_=AsyncSession, expire_on_commit=False)
        async with session() as s:
            return await coro(s)

    return asyncio.run(_inner())


async def _avaliar(db) -> dict:
    """Contrato pós-review T3 + correção da fresta de família com 2+ regras:
    transição ATÔMICA por achado + resolução ESCOPADA POR REGRA (não por
    família — REGISTRY já iterada por `nome`, que é a chave única da regra).
    `marcar_resolvidos` só é chamado para a regra cuja detecção teve SUCESSO
    — regra que falha não tem NENHUMA linha tocada, nem por regras vizinhas
    da MESMA família (ex. "financeiro" = `caixa_baixo_cnpj` + `aging_reforcado`:
    se uma falha e a outra passa, escopar por família resolveria em massa os
    alertas ativos da que falhou). Regra que falha → não reabre como 'novo' na
    rodada seguinte = sem spam duplo."""
    from modules.notifications.proativo import estado, redator
    from modules.notifications.proativo.entrega import (
        enviar_individual, resolver_usuarios_por_roles,
    )
    from modules.notifications.proativo.regras import REGISTRY

    novos = persistentes = resolvidos = 0
    por_familia: dict[str, int] = {}

    for nome, regra in REGISTRY.items():
        try:
            achados = await regra.detectar(db)
        except Exception as exc:  # noqa: BLE001 — isolamento por regra
            logger.error("[proativo] regra %s falhou na detecção: %s", nome, exc)
            await db.rollback()  # asyncpg: transação envenenada precisa rollback
            # NÃO chama marcar_resolvidos desta regra → nada dela é tocado,
            # nem por uma regra vizinha da MESMA família que tenha sucesso.
            continue

        # detecção OK: ativos desta REGRA (mesmo sem achados → resolve todos dela)
        ativos_regra: set[str] = set()
        for achado in achados:
            ativos_regra.add(achado.correlation_id)
            try:
                title, body = await redator.redigir(regra, achado)
                dest = await resolver_usuarios_por_roles(db, regra.roles_destino)
                if not dest:
                    logger.warning("[proativo] %s sem destinatário resolvido", nome)
                    continue
                # transição atômica: True SÓ se ESTA chamada levou a condição a NOVA.
                novo = await estado.transicionar(
                    db, correlation_id=achado.correlation_id, regra=nome,
                    severidade=regra.severidade, title=title, body=body,
                    destinatarios=dest)
                if novo:
                    await enviar_individual(
                        db, user_ids=dest, title=title, body=body,
                        familia=regra.familia, severidade=regra.severidade,
                        correlation_id=achado.correlation_id, action_url=regra.action_url)
                    novos += 1
                else:
                    persistentes += 1
                por_familia[regra.familia] = por_familia.get(regra.familia, 0) + 1
                await db.commit()
            except Exception as exc:  # noqa: BLE001 — isolamento por achado
                logger.error("[proativo] achado %s falhou: %s", achado.correlation_id, exc)
                await db.rollback()

        # resolução ESCOPADA POR REGRA: detecção desta regra teve sucesso nesta
        # rodada → resolve só o que é DELA (nunca de uma regra vizinha da
        # mesma família).
        try:
            resolvidos += await estado.marcar_resolvidos(db, nome, ativos_regra)
            await db.commit()
        except Exception as exc:  # noqa: BLE001 — isolamento por regra
            logger.error("[proativo] resolver regra %s falhou: %s", nome, exc)
            await db.rollback()

    return {"novos": novos, "persistentes": persistentes,
            "resolvidos": resolvidos, "por_familia": por_familia}


@app.task(name="proativo.avaliar_regras", bind=True, max_retries=1)
def avaliar_regras_task(self):
    try:
        res = _run_async(_avaliar)
        logger.info("[proativo] avaliar_regras: %s", res)
        return res
    except Exception as exc:
        logger.error("[proativo] avaliar_regras erro: %s", exc)
        raise self.retry(exc=exc, countdown=120)
```

- [ ] **Step 4: Registrar no `celery_app.py` (include + beat_schedule)**

Em `celery_app.py`, adicionar à lista `include=[...]` (após a linha `"modules.notifications.tasks",` — linha 43):

```python
        # Fase 5.3: proativo por evento (avaliador de regras + digest).
        "modules.notifications.proativo.tasks",
```

Em `app.conf.beat_schedule = {` (após a entrada `"notificacoes-reconciliar-alertas"`, ~linha 165):

```python
    # ── Fase 5.3: avaliador de regras proativas (transição → sino, dedup por estado) ──
    "proativo-avaliar-regras": {
        "task": "proativo.avaliar_regras",
        "schedule": crontab(minute="*/15"),
    },
```

- [ ] **Step 5: Rodar e confirmar que passa (coração `_avaliar` na bancada)**

```bash
docker cp backend/modules/notifications/proativo/tasks.py conecta-pro-backend:/app/modules/notifications/proativo/tasks.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/tasks.py
```
Esperado: PASS — `OK task — 1ª {...novos>=1...}; 2ª novos=0` (dedup: 2ª rodada não re-dispara).

- [ ] **Step 6: Confirmar que o celery_app importa a task sem erro**

```bash
docker cp backend/celery_app.py conecta-pro-backend:/app/celery_app.py
docker run --rm --env-file /opt/conecta-pro/.env -e PYTHONPATH=/app conecta-pro-backend \
  python -c "import celery_app; assert 'proativo.avaliar_regras' in celery_app.app.tasks, 'task não registrada'; assert 'proativo-avaliar-regras' in celery_app.app.conf.beat_schedule; print('OK celery import + beat')"
```
Esperado: PASS — `OK celery import + beat`.

- [ ] **Step 7: Commit**

```bash
git add backend/modules/notifications/proativo/tasks.py backend/celery_app.py
git commit --no-verify -m "feat(proativo): task beat avaliar_regras (*/15min) + registro celery

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Digest diário 07:00 Manaus (`proativo.digest_diario`)

**Files:**
- Modify: `backend/modules/notifications/proativo/tasks.py` (adicionar `_digest` + task)
- Modify: `backend/celery_app.py` (entrada beat 07:00)
- Test: `backend/modules/notifications/proativo/tasks.py` (estender bloco `__main__`)

**Interfaces:**
- Consumes: `estado.persistentes_ativos` (Task 3); `entrega.resolver_usuarios_por_roles` (Task 5); `redator` (opcional); `REGISTRY` (p/ mapear família→roles).
- Produces:
  - `async _digest(db) -> dict` → `{"destinatarios": int, "notificacoes": int}`. Consolida por destinatário (expandindo roles de cada família→users), 1 notificação única por user, respeitando RBAC. Dedup: 1 digest por user por dia (checa existência do digest de hoje).
  - `@app.task(name="proativo.digest_diario", bind=True, max_retries=1) digest_diario_task`.

- [ ] **Step 1: Escrever o teste que falha (2 persistentes → 1 notificação por destinatário)**

Estender o `async def main()` de `tasks.py`, dentro do `try` (após o teste de `_avaliar`, antes do `finally`):

```python
                # ---- DIGEST: semeia 2 persistentes p/ admin e prova 1 notif única ----
                from modules.notifications.proativo import estado as _st
                from modules.notifications.proativo.entrega import (
                    resolver_usuarios_por_roles as _ru,
                )
                admins = await _ru(db, ("admin",))
                for c in ("caixa_baixo:__DGT1__:2026-07", "certidao:__DGT2__:2026-12-31"):
                    await _st.transicionar(db, correlation_id=c, familia="financeiro",
                                           severidade="critico", title="t", body="b",
                                           destinatarios=admins)
                await db.commit()
                dg = await _digest(db)
                assert dg["notificacoes"] >= 1
                # 1 digest por admin por dia: nº de digests de hoje p/ um admin == 1
                if admins:
                    cnt = (await db.execute(text(
                        "SELECT count(*) FROM communication_notifications "
                        "WHERE user_id=:u AND extra_data->>'origem'='proativo_digest' "
                        "AND created_at::date = (now() AT TIME ZONE 'America/Manaus')::date"),
                        {"u": admins[0]})).scalar()
                    assert cnt == 1, f"digest duplicado: {cnt}"
                # roda de novo: NÃO cria segundo digest (dedup diário)
                await _digest(db)
                cnt2 = (await db.execute(text(
                    "SELECT count(*) FROM communication_notifications "
                    "WHERE user_id=:u AND extra_data->>'origem'='proativo_digest' "
                    "AND created_at::date = (now() AT TIME ZONE 'America/Manaus')::date"),
                    {"u": admins[0]})).scalar() if admins else 1
                assert cnt2 == 1, f"digest re-criado: {cnt2}"
                print("OK digest — 1 notif única por destinatário + dedup diário")
```

E no `finally`, ampliar a limpeza para cobrir o digest e os estados semeados:

```python
                await db.execute(text(
                    "DELETE FROM communication_notifications "
                    "WHERE extra_data->>'origem' IN ('proativo','proativo_digest') "
                    "AND created_at >= (now() AT TIME ZONE 'America/Manaus') - interval '10 minutes'"))
                await db.execute(text(
                    "DELETE FROM proativo_alert_state "
                    "WHERE correlation_id IN ('caixa_baixo:__DGT1__:2026-07','certidao:__DGT2__:2026-12-31')"))
                await db.execute(text(
                    "UPDATE proativo_alert_state SET resolved_at=(now() AT TIME ZONE 'America/Manaus') "
                    "WHERE resolved_at IS NULL"))
                await db.commit()
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker cp backend/modules/notifications/proativo/tasks.py conecta-pro-backend:/app/modules/notifications/proativo/tasks.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/tasks.py
```
Esperado: FALHA `NameError: name '_digest' is not defined`.

- [ ] **Step 3: Implementar `_digest` + task (append em `tasks.py`)**

```python
async def _digest(db) -> dict:
    """07:00 Manaus: 1 notificação-resumo por destinatário com o que PERSISTE.
    Respeita RBAC (expande roles de cada família→users). Dedup: 1 digest por user/dia."""
    from modules.notifications.proativo import estado
    from modules.notifications.proativo.entrega import resolver_usuarios_por_roles
    from modules.notifications.proativo.regras import REGISTRY

    import json

    ativos = await estado.persistentes_ativos(db)
    if not ativos:
        return {"destinatarios": 0, "notificacoes": 0}

    # Mapa user_id -> lista de linhas (title) que ele deve ver, via roles da família.
    por_user: dict[str, list[str]] = {}
    for linha in ativos:
        familia = linha["familia"]
        # roles da família: pega de qualquer regra dessa família no REGISTRY.
        roles = next((r.roles_destino for r in REGISTRY.values() if r.familia == familia), ())
        users = await resolver_usuarios_por_roles(db, roles)
        for u in users:
            por_user.setdefault(u, []).append(linha["title"])

    notificacoes = 0
    _NOW_ = "(now() AT TIME ZONE 'America/Manaus')"
    for uid, titulos in por_user.items():
        # dedup diário: já existe digest de hoje p/ este user?
        existe = (await db.execute(text(
            f"SELECT 1 FROM communication_notifications "
            f"WHERE user_id=:u AND extra_data->>'origem'='proativo_digest' "
            f"AND created_at::date = {_NOW_}::date LIMIT 1"), {"u": uid})).first()
        if existe:
            continue
        corpo = "Itens que precisam da sua atenção:\n- " + "\n- ".join(titulos[:20])
        extra = json.dumps({"origem": "proativo_digest", "itens": len(titulos)})
        await db.execute(text(
            f"INSERT INTO communication_notifications "
            f"(id, tenant_id, user_id, title, body, type, reference_type, reference_id, "
            f" action_url, extra_data, is_active, sent_at, created_at) "
            f"VALUES (gen_random_uuid(), :u, :u, :title, :body, 'sistema', 'proativo_digest', "
            f" NULL, '/notificacoes', CAST(:extra AS jsonb), true, {_NOW_}, {_NOW_})"),
            {"u": uid, "title": "Bom dia — o que precisa da sua atenção",
             "body": corpo, "extra": extra})
        notificacoes += 1
    await db.commit()
    return {"destinatarios": len(por_user), "notificacoes": notificacoes}


@app.task(name="proativo.digest_diario", bind=True, max_retries=1)
def digest_diario_task(self):
    try:
        res = _run_async(_digest)
        logger.info("[proativo] digest_diario: %s", res)
        return res
    except Exception as exc:
        logger.error("[proativo] digest_diario erro: %s", exc)
        raise self.retry(exc=exc, countdown=300)
```

Adicionar `from sqlalchemy import text` no topo de `tasks.py` (se ainda não houver — `_digest` usa `text`).

- [ ] **Step 4: Registrar o beat 07:00 Manaus no `celery_app.py`**

> **Nota de fuso:** o container celery roda em UTC. Manaus = UTC−4. 07:00 Manaus = **11:00 UTC**. `crontab` do celery usa a hora do processo (UTC) salvo `timezone` configurado. Confirmar antes: `docker exec conecta-pro-celery-beat printenv TZ` e `grep -n "timezone" backend/celery_app.py`. Se `app.conf.timezone` não for `America/Manaus`, agendar em **UTC** (`hour=11`); se for Manaus, `hour=7`.

Após a entrada `"proativo-avaliar-regras"`:

```python
    # ── Fase 5.3: digest matinal 07:00 America/Manaus (11:00 UTC se beat roda em UTC) ──
    "proativo-digest-diario": {
        "task": "proativo.digest_diario",
        "schedule": crontab(hour=11, minute=0),  # 07:00 Manaus (UTC-4); vira 7 se tz=Manaus
    },
```

- [ ] **Step 5: Verificar o fuso do beat e ajustar a hora**

```bash
docker exec conecta-pro-celery-beat printenv TZ 2>/dev/null; grep -n "timezone\|enable_utc" /opt/conecta-pro/backend/celery_app.py
```
Se `app.conf.timezone == "America/Manaus"`: trocar `hour=11` por `hour=7` na entrada. Caso contrário manter `hour=11`. (Registrar a decisão no commit.)

- [ ] **Step 6: Rodar e confirmar que passa**

```bash
docker cp backend/modules/notifications/proativo/tasks.py conecta-pro-backend:/app/modules/notifications/proativo/tasks.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/tasks.py
```
Esperado: PASS — `OK digest — 1 notif única por destinatário + dedup diário`, 0 remanescentes.

- [ ] **Step 7: Commit**

```bash
git add backend/modules/notifications/proativo/tasks.py backend/celery_app.py
git commit --no-verify -m "feat(proativo): digest matinal 07h Manaus (1 notif única/destinatário, dedup diário)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Suite-oráculo do proativo (E2E standalone)

**Files:**
- Create: `backend/modules/notifications/proativo/proativo_oraculo.py`
- Test: o próprio arquivo (é a suite; roda na bancada e limpa tudo que criou).

**Interfaces:**
- Consumes: `_avaliar`, `_digest` (Tasks 6-7); `REGISTRY` (1-2); tabelas `communication_notifications`, `proativo_alert_state`, `users`.
- Produces: script único que PROVA as 6 garantias da spec e imprime `OK oráculo proativo (6/6)`.

- [ ] **Step 1: Escrever a suite-oráculo (as 6 provas da spec)**

```python
"""Fase 5.3 — Suite-oráculo E2E do proativo. Prova, contra o dado REAL:
(1) detecção == query; (2) dedup (rodar 2x não duplica); (3) RBAC negativa
(financeiro não chega a não-admin); (4) número==query (groundedness); (5) digest
único por destinatário; (6) 0 remanescentes após limpeza. Limpa SÓ o que criou.
NUNCA rodar in-process no :8080/:8081 — bancada docker run --rm.
"""
import asyncio
import os

from sqlalchemy import text
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine


async def main() -> None:
    from modules.notifications.proativo.regras import REGISTRY
    from modules.notifications.proativo.tasks import _avaliar, _digest
    from modules.notifications.proativo.entrega import resolver_usuarios_por_roles

    eng = create_async_engine(os.environ["DATABASE_URL"])
    Session = async_sessionmaker(eng, expire_on_commit=False)
    provas = 0
    async with Session() as db:
        try:
            # (1) DETECÇÃO REAL == QUERY (postos)
            oráculo = (await db.execute(text(
                "SELECT count(*) FROM posts WHERE is_active "
                "AND current_headcount<required_headcount"))).scalar()
            assert len(await REGISTRY["posto_descoberto"].detectar(db)) == oráculo
            provas += 1

            # (2) DEDUP: avaliar 2x → 2ª sem novos
            r1 = await _avaliar(db)
            r2 = await _avaliar(db)
            assert r2["novos"] == 0, (r1, r2)
            provas += 1

            # (3) RBAC NEGATIVA: nenhuma notif financeira do proativo p/ não-admin
            gestores = await resolver_usuarios_por_roles(db, ("gerente_operacional", "supervisor"))
            if gestores:
                vaz = (await db.execute(text(
                    "SELECT count(*) FROM communication_notifications "
                    "WHERE user_id = ANY(:g) AND extra_data->>'origem'='proativo' "
                    "AND extra_data->>'familia'='financeiro'"), {"g": gestores})).scalar()
                assert vaz == 0, f"vazou financeiro p/ gestor: {vaz}"
            provas += 1

            # (4) GROUNDEDNESS: o número do body == número da fonte (posto descoberto)
            achados = await REGISTRY["posto_descoberto"].detectar(db)
            if achados:
                from modules.notifications.proativo.redator import redigir
                _t, body = await redigir(REGISTRY["posto_descoberto"], achados[0])
                assert str(achados[0].dados["faltam"]) in body
            provas += 1

            # (5) DIGEST único por destinatário
            dg = await _digest(db)
            admins = await resolver_usuarios_por_roles(db, ("admin",))
            if admins:
                cnt = (await db.execute(text(
                    "SELECT count(*) FROM communication_notifications "
                    "WHERE user_id=:u AND extra_data->>'origem'='proativo_digest' "
                    "AND created_at::date=(now() AT TIME ZONE 'America/Manaus')::date"),
                    {"u": admins[0]})).scalar()
                assert cnt <= 1, f"digest duplicado: {cnt}"
            provas += 1

            print(f"OK oráculo proativo ({provas}/6 antes da limpeza)")
        finally:
            # (6) LIMPEZA + 0 remanescentes (SÓ o que a suite criou nos últimos 15min)
            await db.execute(text(
                "DELETE FROM communication_notifications "
                "WHERE extra_data->>'origem' IN ('proativo','proativo_digest') "
                "AND created_at >= (now() AT TIME ZONE 'America/Manaus') - interval '15 minutes'"))
            await db.execute(text(
                "UPDATE proativo_alert_state SET resolved_at=(now() AT TIME ZONE 'America/Manaus') "
                "WHERE resolved_at IS NULL"))
            await db.commit()
            rem = (await db.execute(text(
                "SELECT count(*) FROM communication_notifications "
                "WHERE extra_data->>'origem' IN ('proativo','proativo_digest') "
                "AND created_at >= (now() AT TIME ZONE 'America/Manaus') - interval '15 minutes'"))).scalar()
            assert rem == 0, f"remanescentes={rem}"
            print(f"OK oráculo proativo ({provas}/6) — 0 remanescentes")
    await eng.dispose()


if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 2: Rodar a suite na bancada**

```bash
docker cp backend/modules/notifications/proativo/proativo_oraculo.py conecta-pro-backend:/app/modules/notifications/proativo/proativo_oraculo.py
docker run --rm --network conecta-pro_conecta-pro-network --env-file /opt/conecta-pro/.env \
  -e PYTHONPATH=/app conecta-pro-backend python /app/modules/notifications/proativo/proativo_oraculo.py
```
Esperado: PASS — `OK oráculo proativo (5/6 antes da limpeza)` seguido de `OK oráculo proativo (5/6) — 0 remanescentes` (a prova 6 é a própria limpeza).

- [ ] **Step 3: Confirmar 0 remanescentes fora da janela (sanidade)**

```bash
docker exec conecta-pro-postgres psql -U postgres -d conecta_pro -tAc \
  "SELECT count(*) FROM communication_notifications WHERE extra_data->>'origem' IN ('proativo','proativo_digest')"
```
Esperado: `0` na bancada (nenhum resíduo de teste). Se >0, investigar antes de prosseguir.

- [ ] **Step 4: Commit**

```bash
git add backend/modules/notifications/proativo/proativo_oraculo.py
git commit --no-verify -m "test(proativo): suite-oráculo E2E (detecção/dedup/RBAC/groundedness/digest/limpeza)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: Deploy blue-green + reciclagem do celery + verificação em produção

**Files:**
- (nenhum arquivo novo — build/bake da imagem com todo o código das Tasks 1-8 + aplicação da migração em produção + recycle do celery)

**Interfaces:**
- Consumes: tudo. Produz: task rodando 1x em produção → notificações REAIS visíveis no sino do Jordan.

> **NÃO deletar as notificações reais de produção.** Aqui elas são o PRODUTO. A limpeza `finally` só existia nos testes de bancada.

- [ ] **Step 1: Aplicar a migração em PRODUÇÃO (backup → staging → prod, padrão finding-schema-drift)**

```bash
# backup rápido da estrutura (a tabela é nova, risco baixo, mas registra)
docker exec conecta-pro-postgres pg_dump -U postgres -d conecta_pro -s -t 'proativo_*' > /tmp/pre_proativo_schema.sql
docker exec conecta-pro-backend sh -c 'cd /app && alembic upgrade head'
docker exec conecta-pro-postgres psql -U postgres -d conecta_pro -tAc \
  "SELECT count(*) FROM information_schema.tables WHERE table_name='proativo_alert_state'"
```
Esperado: `1`.

- [ ] **Step 2: Deploy blue-green do backend (bake da imagem)**

```bash
bash /opt/conecta-pro/scripts/deploy_backend_bluegreen.sh
```
Esperado: log termina sem `ERRO`; `backend` recriado a partir da imagem recém-buildada (código proativo agora BAKED).

- [ ] **Step 3: Reciclar o celery (o blue-green NÃO faz isso) — beat + worker da fila default**

```bash
cd /opt/conecta-pro
docker compose up -d --no-deps --force-recreate celery-batch celery-beat
```
Verificar que subiram e carregaram a task/schedule:

```bash
docker exec conecta-pro-celery-beat sh -c 'cd /app && python -c "import celery_app; print(\"proativo.avaliar_regras\" in celery_app.app.tasks, \"proativo-avaliar-regras\" in celery_app.app.conf.beat_schedule, \"proativo-digest-diario\" in celery_app.app.conf.beat_schedule)"'
```
Esperado: `True True True`.

- [ ] **Step 4: Rodar a task 1x manualmente em produção (gera notificações REAIS)**

```bash
docker exec conecta-pro-celery-batch sh -c 'cd /app && celery -A celery_app call proativo.avaliar_regras'
sleep 5
docker logs --tail 20 conecta-pro-celery-batch 2>&1 | grep -i "avaliar_regras"
```
Esperado: log `[proativo] avaliar_regras: {'novos': N>=1, ...}`.

- [ ] **Step 5: PROVA no sino — GET /notificacoes com o token de um destinatário certo (admin/Jordan)**

Obter token e checar que a notificação proativa aparece para o admin. (Auth: porta 8080, form-urlencoded — MEMORY.)

```bash
# token do Jordan (admin). Ajustar credencial conforme cofre local.
TOKEN=$(curl -s -X POST http://localhost:8080/auth/login \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -d 'username=jjesus&password=<SENHA>' | python3 -c 'import sys,json;print(json.load(sys.stdin)["access_token"])')

curl -s "http://localhost:8080/operacional/comunicacao/notificacoes?_cb=$RANDOM" \
  -H "Authorization: Bearer $TOKEN" | python3 -c \
  'import sys,json;d=json.load(sys.stdin);xs=[n for n in (d.get("items") or d) if (n.get("extra_data") or {}).get("origem")=="proativo"];print("proativas visíveis p/ admin:",len(xs));[print("-",n["title"]) for n in xs[:5]]'
```
Esperado: `proativas visíveis p/ admin: >=1` e os títulos (ex.: "Posto descoberto: ...").

- [ ] **Step 6: PROVA negativa em produção — GET /notificacoes de um gestor NÃO traz financeiro**

```bash
# token de um gerente_operacional (ex.: Gonzaga). Ajustar credencial.
TOKG=$(curl -s -X POST http://localhost:8080/auth/login \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -d 'username=<gestor>&password=<SENHA>' | python3 -c 'import sys,json;print(json.load(sys.stdin)["access_token"])')

curl -s "http://localhost:8080/operacional/comunicacao/notificacoes?_cb=$RANDOM" \
  -H "Authorization: Bearer $TOKG" | python3 -c \
  'import sys,json;d=json.load(sys.stdin);fin=[n for n in (d.get("items") or d) if (n.get("extra_data") or {}).get("familia")=="financeiro"];print("financeiro p/ gestor (deve ser 0):",len(fin));assert len(fin)==0'
```
Esperado: `financeiro p/ gestor (deve ser 0): 0` (LGPD respeitada em produção).

- [ ] **Step 7: Confirmar o agendamento vivo do beat**

```bash
docker logs --tail 40 conecta-pro-celery-beat 2>&1 | grep -i "proativo"
```
Esperado: linhas `Scheduler: Sending due task proativo-avaliar-regras (proativo.avaliar_regras)` (o */15min está no ar).

- [ ] **Step 8: Push**

```bash
git push
```
(Se estiver em worktree/branch próprio, abrir PR conforme fluxo da sessão. Nunca `git add -A` — só os arquivos do proativo já commitados.)

---

## Self-Review

**1. Spec coverage:**
- 7 famílias TODAS ligadas → Tasks 1 (posto/certidão/caixa) + 2 (aging/justificativa/jurídico/recrutamento). ✓ (mudas jurídico/recrutamento registradas e silenciosas — Task 2 Step 4 prova 0 Achados hoje.)
- Limiar caixa < 1 folha do CNPJ, fallback R$10k → Task 1 `_detectar_caixa_baixo` (`FALLBACK_CAIXA_SEM_FOLHA=10000.0`). ✓
- Matriz RBAC de destinatários → `roles_destino` por regra (Tasks 1-2) + `resolver_usuarios_por_roles` (Task 5) + prova negativa (Tasks 5, 8, 9). ✓
- Anti-spam: 1x na transição + digest 07h → estado de transição (Task 3) + `_avaliar` novo-vs-persistente (Task 6) + `_digest` (Task 7). ✓
- LLM só redige, nunca decide, degrada p/ template → Task 4 (`redigir`, groundedness + fallback). ✓
- Dedup por correlation_id; regra read-only; isolamento por regra → Tasks 3, 6 (`try/except` + rollback por regra/achado). ✓
- Nunca fabricar (saldo None → silêncio) → Task 1 `_detectar_caixa_baixo` (skip se saldo None). ✓
- Entrega reusa `_enviar_alerta`/`communication_notifications`; RdBell já lê → Task 5 (molde). ✓
- Deploy blue-green só na última; celery reciclado explicitamente → Task 9 Steps 2-3. ✓
- Riscos abertos da spec: (a) estado tabela vs reuso → decidido tabela mínima (Task 3, com justificativa); (b) certidões fonte `ged_certidoes.expiry_date` janela 30d → Task 1 confirmado por introspecção; (c) aging sem duplicar → Task 2 (mesmo namespace `financeiro_aging:portfolio`). ✓

**2. Placeholder scan:** todo step de código traz o código real (SQL/Python das fontes). Sem "TBD/TODO/etc.". Valores de credencial (`<SENHA>`, `<gestor>`, `<HEAD_ATUAL>`) são substituições de ambiente/segredo — não lógica — e vêm com instrução de como obter. ✓

**3. Type consistency:** `Achado{correlation_id, dados}` e `Regra{nome,familia,severidade,roles_destino,action_url,detectar,template}` idênticos em Tasks 1-8. Funções de estado (`transicionar/marcar_resolvidos/persistentes_ativos` — contrato pós-review T3 + correção residual: transição atômica + resolução escopada por REGRA, coluna `regra` — família com 2+ regras não vaza mais entre si), entrega (`resolver_usuarios_por_roles/enviar_individual`), redator (`redigir`), tasks (`_avaliar/_digest`) usadas com a MESMA assinatura em todas as tasks consumidoras. `origem` do `caixa_por_cnpj` (`saldo/folha/slug/nome/banco`) baterá com o retorno real (`caixa_service.py`). ✓

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-07-24-fase5.3-proativo.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** — execute tasks in this session with checkpoints for review.

**Which approach?**
