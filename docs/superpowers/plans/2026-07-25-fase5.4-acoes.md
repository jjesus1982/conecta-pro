# Fase 5.4 — Ações propor→aprovar em escala Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao agente a capacidade de PROPOR seis ações (cobrança, proposta comercial, kit, substituição de posto, lote de pagamento, eSocial) que caem numa fila de aprovação humana no sino pela matriz RBAC — sem que o LLM jamais execute nada.

**Architecture:** Uma única primitiva "Proposta de Ação" (`orquestrador/acoes/base.py`) reusa o `ToolDef`/engine da Peça 3, o `agent_audit` da 5.2a e a entrega/sino da 5.3. Cada ação é um `ToolDef` cujo handler SÓ grava um registro PENDENTE na tabela NATIVA do domínio (via callback `inserir`), audita append-only, e entrega no sino ao aprovador por role. A aprovação/execução reusa 100% os fluxos humanos já existentes e provados (payment OTP, proposal send, kit_orchestrator, esocial_tasks) — nenhum código de execução novo, nenhum gate de dinheiro novo.

**Tech Stack:** FastAPI + SQLAlchemy async (`text()` SQL cru, como todos os moldes), PostgreSQL, Alembic, OpenAI function-calling (engine existente), Docker blue-green. Sem pytest — testes são scripts standalone `python <arquivo>.py` com asyncio + asserts.

## Global Constraints

**INVIOLÁVEIS (verbatim da spec §Global Constraints — toda task herda isto):**
- **O LLM NUNCA executa.** Toda tool de ação só faz INSERT de proposta PENDENTE na tabela nativa do domínio. Zero chamada de execução no handler. A execução é sempre disparada por HUMANO no fluxo existente.
- **Dinheiro que SAI = SEMPRE gate OTP humano.** Reusa `InterPaymentService` (preparar→OTP email TTL 300s→aprovar FOR UPDATE→executar atômico+idempotência), INTACTO. Money-out FORA do conector MCP. Teto `CONECTA_LIMITE_DIARIO_PAGAMENTOS` (R$100k) respeitado na proposta.
- **eSocial / ato legal = 🔴 gate humano** (transmissão gov real S-22x0 só após aprovação humana).
- **Operacional READ-ONLY** para o agente: substituição é PROPOSTA, nunca alteração de escala.
- **3 papéis nunca no mesmo ator:** quem PROPÕE (agente/usuário) ≠ quem APROVA ≠ quem gera/valida OTP ≠ quem executa. O "aprovar" é inforjável.
- **Idempotência** (anti-PIX-dobro): correlation/idempotency key em toda proposta; 2 propostas idênticas não duplicam pendente nem execução.
- **RBAC por domínio** governa quem vê a tool de ação e quem aprova (reusa `user_modules`/roles da peça 3).
- **Nunca fabricar.** **Auditoria append-only** de todo passo (propor→aprovar→executar), ligada ao `agent_audit`.

**Regras de ambiente (herdadas de 5.1/5.2a/5.3):**
- **Bancada throwaway:** `docker run --rm --memory=2g` com imagem do backend; `PYTHONPATH=/app`; `DATABASE_URL` montado a partir de `POSTGRES_*` (host, user, pass, db). Copiar cada arquivo alterado com `docker cp <host> <container>:/app/...` (arquivo-a-arquivo — nunca `docker cp` de diretório inteiro). **NUNCA rodar contra green/:8080** (in-process = OOM; green compartilha banco vivo).
- **Teste que escreve limpa por id no `finally`** (0 remanescentes verificados por count) e **NUNCA apaga linha real** — só as marcadas com o correlation/idempotency key `__TESTE_5.4__`.
- **Celery reciclado no deploy** quando a mudança toca tasks/tools carregadas no worker (esta fase toca; reciclar celery-worker/beat no deploy).
- **Sem pytest.** Todo teste é `python modules/.../<arquivo>.py` com asyncio + asserts + print final "TODOS OS TESTES ... PASSARAM".
- **git add só dos próprios arquivos** (a árvore é compartilhada por 3 sessões claude paralelas — nunca `git add -A`).
- **Commits:** `git commit --no-verify` (pre-commit flaky) + trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **TZ Manaus:** timestamps de aplicação usam `(now() AT TIME ZONE 'America/Manaus')` (molde entrega.py `_NOW`).
- **Casts `::text`** ao comparar UUID com string; enum→`::text` quando necessário.
- **Deploy backend:** SEMPRE `scripts/deploy_backend_bluegreen.sh` (backend BAKED na imagem; `docker cp`+restart = volátil; durável = rebuild). Lock `/tmp/conecta_deploy.lock`.

**Decisões tomadas onde spec/discovery eram ambíguas (ver §Self-Review no fim; confirmar com Jordan no deploy):**
1. **Valor canônico do drift `gp_justifications`:** `'pendente'` (PT) — spec §Decisões-do-dono é explícita.
2. **Roles aprovadores:** money+esocial → `("admin",)`; kit+substituição → `("admin","gerente_operacional")`; proposta+cobrança → `("admin","comercial")`. `"comercial"` é assumido; `"admin"` sempre no conjunto ⇒ nenhuma proposta fica órfã de aprovador.
3. **eSocial não tem tabela de proposta nativa** (as tasks leem de tabelas-fonte com coluna `esocial_status`). Criamos uma tabela mínima de proposta `esocial_transmissao_propostas` (espelha `op_substituicao_propostas`) — o agente marca pendente ali; o humano dispara `esocial_tasks` existente.
4. **Assinatura da primitiva:** a spec esboçou `propor(..., tabela, payload, ...)`; adaptamos para um callback `inserir(db)->entity_id` porque cada tabela nativa tem colunas próprias — o callback torna o "zero-execução" auditável por construção (só um INSERT).
5. **Dedup/idempotência:** query-based sobre `communication_notifications.extra_data->>'idempotency_key'` (mesmo espírito do molde `tools_ponto`); a trava anti-dobro REAL de dinheiro continua no `executar()` atômico (`InterPaymentService`/lote).
6. **Fail-closed reforçado:** se `roles_aprovador` for vazio OU se a resolução de roles retornar zero usuários ativos, a primitiva NÃO grava nada (proposta órfã de aprovador é recusada).
7. **`scope_kind="org"`** para as seis ações (são propostas de nível-módulo, escopadas pela posse do módulo no belt + RBAC do aprovador).

---

## File Structure

- **Create** `backend/modules/ai/conversation/services/orquestrador/acoes/__init__.py` — pacote das ações.
- **Create** `backend/modules/ai/conversation/services/orquestrador/acoes/base.py` — a primitiva `propor(...)` + gates + roles + testes standalone.
- **Create** `backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py` — `propor_cobranca`, `propor_proposta_comercial`, `propor_kit`.
- **Create** `backend/modules/ai/conversation/services/orquestrador/acoes/onda_b.py` — `propor_substituicao`.
- **Create** `backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py` — `propor_lote_pagamento`, `propor_esocial`.
- **Create** `backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py` — suite-oráculo das INVIOLÁVEIS + mutação-teste.
- **Modify** `backend/modules/notifications/proativo/entrega.py` — `enviar_individual` ganha `reference_type`/`reference_id`/`idempotency_key` opcionais (retrocompatível).
- **Modify** `backend/modules/ai/conversation/services/garantia/agent_audit.py` — adiciona `registrar_proposta_acao(...)`.
- **Modify** `backend/modules/people_management/employee_portal/controllers/portal_docs_ouvidoria_controller.py:108` — writer grava `'pendente'` (não `'pending'`).
- **Modify** `backend/modules/ai/conversation/controllers/consultor_escopado_controller.py` — wiring das ações no tier por módulo+role.
- **Create** `backend/alembic/versions/fase54_gp_just_pending_fix.py` — normaliza a linha viva `'pending'`→`'pendente'` (idempotente).
- **Create** `backend/alembic/versions/fase54_op_substituicao_propostas.py` — tabela nova (Onda B).
- **Create** `backend/alembic/versions/fase54_esocial_propostas.py` — tabela nova (Onda C).

---

### Task 1: Pré-step — normalizar o drift `gp_justifications` `'pending'` → `'pendente'`

**Files:**
- Modify: `backend/modules/people_management/employee_portal/controllers/portal_docs_ouvidoria_controller.py:108`
- Create: `backend/alembic/versions/fase54_gp_just_pending_fix.py`
- Test: script inline (comando abaixo)

**Interfaces:**
- Produces: garantia de que `gp_justifications.status` só usa o valor canônico `'pendente'` (PT); nenhuma task posterior precisa lidar com `'pending'` (EN).

- [ ] **Step 1: Corrigir o writer que grava `'pending'`**

Em `portal_docs_ouvidoria_controller.py`, a linha ~108 grava o atestado como `'pending'`. Trocar o literal:

```python
                "INSERT INTO gp_justifications (justification_id, employee_id, justification_type, "
                " reason, category, status, attachments, source, source_id, created_at, updated_at) "
                " VALUES (gen_random_uuid()::text, :emp, :jtype, :reason, :cat, "
                " 'pendente', CAST(:att AS jsonb), 'portal_funcionario', :sid, now(), now()) RETURNING id::text"
```

(Só o literal `'pending'` → `'pendente'` muda; o resto da string permanece idêntico. Verificar a forma exata antes de editar — o INSERT quebra em duas strings adjacentes no código; o literal `'pending'` está na segunda.)

- [ ] **Step 2: Escrever a migration idempotente que conserta a(s) linha(s) viva(s)**

Create `backend/alembic/versions/fase54_gp_just_pending_fix.py`:

```python
"""fase5.4 — normaliza gp_justifications status 'pending' (EN) -> 'pendente' (PT canônico)

Revision ID: fase54_gp_just_fix
Revises: p5_3_proativo_state_regra
"""
from alembic import op

revision = "fase54_gp_just_fix"
down_revision = "p5_3_proativo_state_regra"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # Idempotente: só toca linhas que ainda estão no valor EN antigo.
    op.execute(
        "UPDATE gp_justifications SET status = 'pendente' WHERE status = 'pending'"
    )


def downgrade() -> None:
    # Sem downgrade destrutivo: não re-inglesamos o dado (o canônico é 'pendente').
    pass
```

- [ ] **Step 3: Aplicar a migration na bancada e provar 0 linhas em inglês**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/modules/people_management/employee_portal/controllers/portal_docs_ouvidoria_controller.py $CID:/app/modules/people_management/employee_portal/controllers/portal_docs_ouvidoria_controller.py
docker cp backend/alembic/versions/fase54_gp_just_pending_fix.py $CID:/app/alembic/versions/fase54_gp_just_pending_fix.py
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python -c "
import asyncio,os
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
async def m():
    e=create_async_engine(os.environ[\"DATABASE_URL\"])
    async with e.begin() as c:
        await c.execute(text(\"UPDATE gp_justifications SET status=\x27pendente\x27 WHERE status=\x27pending\x27\"))
    async with e.connect() as c:
        n=(await c.execute(text(\"SELECT count(*) FROM gp_justifications WHERE status=\x27pending\x27\"))).scalar()
    assert n==0, f\"ainda ha {n} linhas pending EN\"
    print(\"OK — 0 linhas gp_justifications status=pending (EN)\")
    await e.dispose()
asyncio.run(m())
"'
```
Expected: `OK — 0 linhas gp_justifications status=pending (EN)`

- [ ] **Step 4: Commit**

```bash
git add backend/modules/people_management/employee_portal/controllers/portal_docs_ouvidoria_controller.py backend/alembic/versions/fase54_gp_just_pending_fix.py
git commit --no-verify -m "fix(5.4): normaliza gp_justifications status pending->pendente (writer+migration idempotente)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Primitiva de proposta (`acoes/base.py`) + extensões de audit e sino

**Files:**
- Create: `backend/modules/ai/conversation/services/orquestrador/acoes/__init__.py`
- Create: `backend/modules/ai/conversation/services/orquestrador/acoes/base.py`
- Modify: `backend/modules/notifications/proativo/entrega.py`
- Modify: `backend/modules/ai/conversation/services/garantia/agent_audit.py`
- Test: `backend/modules/ai/conversation/services/orquestrador/acoes/base.py` (bloco `__main__`)

**Interfaces:**
- Consumes: `entrega.resolver_usuarios_por_roles`, `entrega.enviar_individual` (5.3); `agent_audit.registrar_proposta_acao` (novo neste task).
- Produces:
  - `GATES = frozenset({"🔴", "🟡", "🔵"})`
  - `ROLES_MONEY = ("admin",)`, `ROLES_KIT_OP = ("admin", "gerente_operacional")`, `ROLES_COMERCIAL = ("admin", "comercial")`
  - `async def propor(db, *, user, scope, dominio, gate, roles_aprovador, idempotency_key, titulo, corpo, action_url, tool, args, entity_type, inserir) -> dict` — grava PENDENTE via `inserir(db)->entity_id`, audita, entrega no sino; fail-closed; NUNCA executa. Retorna `{"status":"pendente","dominio",...,"entity_id","gate","aprovadores"}` ou `{"status":"pendente","duplicado":True,...}` ou `{"erro":...}`.
  - `agent_audit.registrar_proposta_acao(db, *, origem, tool, args, dominio, gate, entity_type, entity_id, aprovadores, proposto_por, trace_id) -> str`

- [ ] **Step 1: Estender `entrega.enviar_individual` (retrocompatível)**

Em `backend/modules/notifications/proativo/entrega.py`, substituir a assinatura e o corpo de `enviar_individual`:

```python
async def enviar_individual(db: AsyncSession, *, user_ids: list[str], title: str,
                            body: str, familia: str, severidade: str,
                            correlation_id: str, action_url: str,
                            reference_type: str = "proativo",
                            reference_id: str | None = None,
                            idempotency_key: str | None = None) -> list[str]:
    """Materializa o alerta no sino (communication_notifications), 1 linha por
    destinatário. `user_ids` já deve vir de `resolver_usuarios_por_roles` com os
    roles de destino — esta função não decide RBAC, só grava.
    tenant_id = uid (users não tem coluna tenant_id; o sino filtra por
    tenant_id == user.id). extra_data carrega correlation_id + (opcional)
    idempotency_key p/ rastreio/dedup/auditoria. reference_type/reference_id
    ligam a notificação à entidade proposta (ex.: 'proposta_acao' + entity_id)."""
    criados: list[str] = []
    for uid in user_ids:
        extra = json.dumps({"correlation_id": correlation_id, "familia": familia,
                            "severidade": severidade, "origem": "proativo",
                            "idempotency_key": idempotency_key})
        row = (await db.execute(text(
            f"INSERT INTO communication_notifications "
            f"(id, tenant_id, user_id, title, body, type, reference_type, "
            f" reference_id, action_url, extra_data, is_active, sent_at, created_at) "
            f"VALUES (gen_random_uuid(), :tid, :uid, :title, :body, 'alerta', "
            f" :rtype, :rid, :url, CAST(:extra AS jsonb), true, {_NOW}, {_NOW}) "
            f"RETURNING id::text"),
            {"tid": str(uid), "uid": str(uid), "title": title, "body": body,
             "rtype": reference_type, "rid": reference_id,
             "url": action_url, "extra": extra})).scalar()
        criados.append(row)
    return criados
```

- [ ] **Step 2: Adicionar `registrar_proposta_acao` em `agent_audit.py`**

Ao fim de `backend/modules/ai/conversation/services/garantia/agent_audit.py` (antes do bloco `if __name__`), adicionar:

```python
async def registrar_proposta_acao(
    db: AsyncSession,
    *,
    origem: str,
    tool: str,
    args: dict,
    dominio: str,
    gate: str,
    entity_type: str,
    entity_id: str,
    aprovadores: list[str],
    proposto_por: str,
    trace_id: str | None = None,
) -> str:
    """Append-only em `audit_logs`: 1 linha por PROPOSTA de ação (propor→aprovar).
    Registra tool/args/entity/gate/aprovadores + quem propôs (3 papéis). NUNCA
    representa execução — só a criação do PENDENTE."""
    ts = datetime.utcnow().strftime("%Y%m%d%H%M%S")
    event_id = f"EVT-PROP-{ts}-{secrets.token_hex(4).upper()}"
    details = {
        "origem": origem, "tool": tool, "args": args, "dominio": dominio,
        "gate": gate, "entity_type": entity_type, "entity_id": entity_id,
        "aprovadores": aprovadores, "proposto_por": proposto_por,
        "trace_id": trace_id, "fase": "5.4", "acao": "propor",
    }
    await db.execute(
        text(
            """
            INSERT INTO audit_logs (
                id, event_id, action, category, severity, result, description,
                details, entity_type, is_sensitive, is_pii, requires_review,
                archived, created_at
            ) VALUES (
                :id, :event_id, 'agent_action', 'agent', 'info', 'success',
                :description, CAST(:details AS jsonb), :entity_type,
                false, false, true, false, :created_at
            )
            """
        ),
        {
            "id": str(uuid4()),
            "event_id": event_id,
            "description": f"Agente propôs {tool} ({dominio}, gate {gate}) — PENDENTE de aprovação",
            "details": json.dumps(details, ensure_ascii=False, default=str),
            "entity_type": entity_type,
            "created_at": datetime.utcnow(),
        },
    )
    await db.commit()
    logger.info("agent_audit: proposta %s tool=%s dominio=%s gate=%s entity=%s",
                event_id, tool, dominio, gate, entity_id)
    return event_id
```

- [ ] **Step 3: Criar o pacote e a primitiva**

Create `backend/modules/ai/conversation/services/orquestrador/acoes/__init__.py`:

```python
"""Fase 5.4 — Ações propor→aprovar do orquestrador escopado.

A primitiva `base.propor` grava PENDENTE na tabela nativa do domínio, audita
append-only e entrega no sino ao aprovador por role. O LLM NUNCA executa.
"""
```

Create `backend/modules/ai/conversation/services/orquestrador/acoes/base.py`:

```python
"""Fase 5.4 — Primitiva "Proposta de Ação".

Uma única função `propor(...)` que TODAS as seis ações usam. Por construção:
  1. só grava PENDENTE na tabela nativa (via callback `inserir`; zero execução);
  2. audita append-only (agent_audit.registrar_proposta_acao);
  3. entrega no sino (5.3) ao aprovador por role (matriz RBAC server-side).
Fail-closed: sem roles_aprovador declarado, ou se a resolução de roles não
achar nenhum usuário ativo, NÃO grava nada (proposta órfã de aprovador é
recusada). Idempotência: 2 propostas com a mesma idempotency_key não duplicam
o pendente (dedup via communication_notifications.extra_data->>'idempotency_key').
"""
from __future__ import annotations

from collections.abc import Awaitable, Callable
from typing import Any

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from modules.ai.conversation.services.garantia import agent_audit
from modules.notifications.proativo import entrega

#: Classes de risco declaradas por ação (espelha o tool_risk_manifest do conector).
GATES = frozenset({"🔴", "🟡", "🔵"})

#: Matriz RBAC de APROVADORES por natureza (server-side; NUNCA vem do LLM/achado).
#: 'admin' está sempre presente ⇒ nenhuma proposta fica órfã de aprovador.
ROLES_MONEY = ("admin",)                         # 🔴 dinheiro/eSocial → diretoria
ROLES_KIT_OP = ("admin", "gerente_operacional")  # kit / substituição
ROLES_COMERCIAL = ("admin", "comercial")         # proposta / cobrança


async def propor(
    db: AsyncSession,
    *,
    user,
    scope,
    dominio: str,
    gate: str,
    roles_aprovador: tuple[str, ...],
    idempotency_key: str,
    titulo: str,
    corpo: str,
    action_url: str,
    tool: str,
    args: dict[str, Any],
    entity_type: str,
    inserir: Callable[[AsyncSession], Awaitable[str]],
) -> dict[str, Any]:
    """Grava a proposta PENDENTE e a entrega ao aprovador. NUNCA executa.

    `inserir(db)` faz o ÚNICO INSERT na tabela nativa e devolve o entity_id do
    PENDENTE criado. Nenhum caminho deste código chama execução (pagar/transmitir/
    enviar) — a execução real permanece na tela humana existente.
    """
    if gate not in GATES:
        return {"erro": f"gate inválido {gate!r} — recusado (fail-closed)"}
    # fail-closed 1: sem roles_aprovador declarado, nada é registrado.
    if not roles_aprovador:
        return {"erro": "sem aprovador declarado — proposta recusada (fail-closed)"}

    # Idempotência: um pendente vivo com a MESMA idempotency_key não é duplicado.
    ja = (await db.execute(text(
        "SELECT reference_id FROM communication_notifications "
        "WHERE extra_data->>'idempotency_key' = :k "
        "  AND coalesce(is_active, true) = true LIMIT 1"),
        {"k": idempotency_key})).scalar()
    if ja:
        return {"status": "pendente", "duplicado": True, "dominio": dominio,
                "entity_id": ja, "gate": gate,
                "aviso": "Proposta idêntica já está aguardando aprovação."}

    # fail-closed 2: aprovador precisa existir de fato (proposta órfã é recusada).
    aprovadores = await entrega.resolver_usuarios_por_roles(db, roles_aprovador)
    if not aprovadores:
        return {"erro": "nenhum aprovador ativo para este domínio — proposta recusada (fail-closed)"}

    # ÚNICO ponto de escrita na tabela nativa: só INSERT de PENDENTE.
    entity_id = await inserir(db)

    # Auditoria append-only (quem propôs ≠ quem aprova — os 3 papéis).
    await agent_audit.registrar_proposta_acao(
        db, origem=getattr(scope, "tier", "consultor_escopado"),
        tool=tool, args=args, dominio=dominio, gate=gate,
        entity_type=entity_type, entity_id=str(entity_id),
        aprovadores=aprovadores, proposto_por=str(getattr(user, "id", "")),
        trace_id=f"5.4.{dominio}",
    )

    # Entrega no sino ao aprovador por ROLE (RBAC server-side).
    await entrega.enviar_individual(
        db, user_ids=aprovadores, title=titulo, body=corpo,
        familia=dominio, severidade=("critico" if gate == "🔴" else "aviso"),
        correlation_id=idempotency_key, action_url=action_url,
        reference_type="proposta_acao", reference_id=str(entity_id),
        idempotency_key=idempotency_key,
    )
    await db.commit()
    return {"status": "pendente", "dominio": dominio, "entity_id": str(entity_id),
            "gate": gate, "aprovadores": aprovadores,
            "aviso": f"Proposta ENVIADA para aprovação ({dominio}). "
                     f"NADA foi executado — {'exige OTP humano' if gate == '🔴' else 'aguarda aprovação humana'}."}
```

- [ ] **Step 4: Escrever os testes da primitiva (bloco `__main__` em base.py)**

Adicionar ao fim de `base.py`:

```python
if __name__ == "__main__":
    import asyncio
    import os
    import uuid

    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    class _U:  # usuário-stub (só precisa de .id)
        id = "00000000-0000-0000-0000-0000000000ff"

    class _S:  # scope-stub
        tier = "gestor"

    KEY = f"__TESTE_5.4__:base:{uuid.uuid4()}"

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        notif_ids: list[str] = []
        audit_ids: list[str] = []
        inseriu = {"n": 0}

        async def _fake_insert(db) -> str:
            inseriu["n"] += 1
            return "entity-abc"

        async def _boom_insert(db) -> str:  # não deve ser chamado nos fail-closed
            raise AssertionError("inserir NÃO pode ser chamado quando fail-closed")

        async with Session() as db:
            try:
                # 1) fail-closed: sem roles_aprovador → nada grava, inserir não roda
                r = await propor(
                    db, user=_U(), scope=_S(), dominio="teste", gate="🟡",
                    roles_aprovador=(), idempotency_key=KEY + ":a",
                    titulo="t", corpo="c", action_url="/x", tool="t", args={},
                    entity_type="teste", inserir=_boom_insert)
                assert "erro" in r, r
                print("TESTE 1 (fail-closed sem aprovador) PASS")

                # 2) gate inválido → recusa
                r = await propor(
                    db, user=_U(), scope=_S(), dominio="teste", gate="X",
                    roles_aprovador=ROLES_MONEY, idempotency_key=KEY + ":b",
                    titulo="t", corpo="c", action_url="/x", tool="t", args={},
                    entity_type="teste", inserir=_boom_insert)
                assert "erro" in r, r
                print("TESTE 2 (gate inválido) PASS")

                # 3) caminho feliz → cria pendente, audita, entrega no sino
                r = await propor(
                    db, user=_U(), scope=_S(), dominio="teste", gate="🟡",
                    roles_aprovador=ROLES_MONEY, idempotency_key=KEY,
                    titulo="[TESTE 5.4] proposta", corpo="corpo teste",
                    action_url="/x", tool="propor_teste", args={"k": 1},
                    entity_type="teste", inserir=_fake_insert)
                assert r["status"] == "pendente" and not r.get("duplicado"), r
                assert inseriu["n"] == 1, inseriu
                assert r["aprovadores"], r
                print("TESTE 3 (caminho feliz cria pendente) PASS")

                # 4) idempotência: mesma key → duplicado, inserir NÃO roda de novo
                r2 = await propor(
                    db, user=_U(), scope=_S(), dominio="teste", gate="🟡",
                    roles_aprovador=ROLES_MONEY, idempotency_key=KEY,
                    titulo="[TESTE 5.4] proposta", corpo="corpo teste",
                    action_url="/x", tool="propor_teste", args={"k": 1},
                    entity_type="teste", inserir=_fake_insert)
                assert r2.get("duplicado") is True, r2
                assert inseriu["n"] == 1, "inserir rodou de novo (quebrou idempotência)"
                print("TESTE 4 (idempotência) PASS")

                # coleta ids p/ limpeza
                notif_ids = [x for x in (await db.execute(text(
                    "SELECT id::text FROM communication_notifications "
                    "WHERE extra_data->>'idempotency_key' = :k"), {"k": KEY})).scalars().all()]
                audit_ids = [x for x in (await db.execute(text(
                    "SELECT id::text FROM audit_logs "
                    "WHERE details->>'trace_id' = '5.4.teste'"))).scalars().all()]
                print("TODOS OS TESTES DE base.py PASSARAM")
            finally:
                if notif_ids:
                    await db.execute(text("DELETE FROM communication_notifications WHERE id = ANY(:i)"),
                                     {"i": notif_ids})
                if audit_ids:
                    await db.execute(text("DELETE FROM audit_logs WHERE id = ANY(:i)"),
                                     {"i": audit_ids})
                await db.commit()
                rem = (await db.execute(text(
                    "SELECT count(*) FROM communication_notifications "
                    "WHERE extra_data->>'idempotency_key' LIKE :k"),
                    {"k": "__TESTE_5.4__:base:%"})).scalar()
                assert rem == 0, f"remanescentes={rem}"
        await eng.dispose()

    asyncio.run(main())
```

- [ ] **Step 5: Rodar o teste na bancada**

Run:
```bash
CID=conecta-pro-backend
for f in modules/notifications/proativo/entrega.py \
         modules/ai/conversation/services/garantia/agent_audit.py \
         modules/ai/conversation/services/orquestrador/acoes/__init__.py \
         modules/ai/conversation/services/orquestrador/acoes/base.py ; do
  docker cp backend/$f $CID:/app/$f
done
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/base.py'
```
Expected: `TESTE 1 ... PASS` → `TESTE 4 (idempotência) PASS` → `TODOS OS TESTES DE base.py PASSARAM` (sem AssertionError).

- [ ] **Step 6: Commit**

```bash
git add backend/modules/ai/conversation/services/orquestrador/acoes/__init__.py \
        backend/modules/ai/conversation/services/orquestrador/acoes/base.py \
        backend/modules/notifications/proativo/entrega.py \
        backend/modules/ai/conversation/services/garantia/agent_audit.py
git commit --no-verify -m "feat(5.4): primitiva de proposta (propor) + audit de proposta + sino c/ idempotency_key

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Onda A — `propor_cobranca` (🟡, money-in)

**Files:**
- Create: `backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py`
- Test: `onda_a.py` (bloco `__main__`, subteste cobrança)

**Interfaces:**
- Consumes: `base.propor`, `base.GATES`, `base.ROLES_COMERCIAL`; `ToolDef`/`register` do `tool_registry`.
- Produces: `COBRANCA_TOOL: ToolDef` (name=`propor_cobranca`, module=`comercial`, scope_kind=`org`) e o handler `async _propor_cobranca(db,user,scope,*,cliente_crm_id,valor,vencimento,descricao="",**_)`.

- [ ] **Step 1: Escrever o handler `_propor_cobranca` (grava `inter_cobrancas` PENDENTE)**

Create `backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py`:

```python
"""Fase 5.4 — Onda A: cobrança (🟡), proposta comercial (🟡), kit (🔵).

Cada ação grava um PENDENTE na tabela nativa via base.propor. NENHUMA executa:
- cobrança: inter_cobrancas status 'PENDENTE' (a emissão real fica no CobrancaService.emitir humano)
- proposta: proposals status 'draft' (envio/assinatura ficam no proposal_controller humano)
- kit: ged_document_kits status 'proposto' (a montagem/envio ficam no kit_orchestrator humano)
"""
from __future__ import annotations

import json
import uuid
from datetime import date
from typing import Any

from sqlalchemy import text

from ..tool_registry import ToolDef, register
from .base import GATES, ROLES_COMERCIAL, ROLES_KIT_OP, propor  # noqa: F401

# ───────────────────────── cobrança (🟡, dinheiro que ENTRA) ─────────────────

_ARGS_COBRANCA = {
    "type": "object",
    "properties": {
        "cliente_crm_id": {"type": "string", "description": "ID do cliente no CRM (obrigatório)."},
        "valor": {"type": "number", "minimum": 0.01, "description": "Valor da cobrança (R$)."},
        "vencimento": {"type": "string", "description": "Data de vencimento AAAA-MM-DD."},
        "descricao": {"type": "string", "maxLength": 500},
    },
    "required": ["cliente_crm_id", "valor", "vencimento"],
}


async def _propor_cobranca(
    db, user, scope, *, cliente_crm_id: str, valor: float,
    vencimento: str, descricao: str = "", **_
) -> dict[str, Any]:
    if not cliente_crm_id or float(valor) <= 0:
        return {"erro": "cliente e valor (>0) são obrigatórios"}
    idem = f"cobranca:{cliente_crm_id}:{float(valor):.2f}:{vencimento}"

    async def _inserir(db) -> str:
        local_id = str(uuid.uuid4())
        seu_numero = f"CPRO-{local_id[:8].upper()}"
        await db.execute(text("""
            INSERT INTO inter_cobrancas
                (id, seu_numero, valor, vencimento, status, descricao, pagador, created_at, updated_at)
            VALUES
                (:id, :sn, :valor, :venc, 'PENDENTE', :desc, CAST(:pag AS jsonb), NOW(), NOW())
        """), {"id": local_id, "sn": seu_numero, "valor": float(valor),
               "venc": vencimento, "desc": descricao or "",
               "pag": json.dumps({"cliente_crm_id": cliente_crm_id})})
        return local_id

    return await propor(
        db, user=user, scope=scope, dominio="cobranca", gate="🟡",
        roles_aprovador=ROLES_COMERCIAL, idempotency_key=idem,
        titulo="[Proposta] Emitir cobrança",
        corpo=f"Cobrança de R$ {float(valor):.2f} (venc. {vencimento}) — {descricao or 'sem descrição'}. Aguarda sua aprovação.",
        action_url="/financeiro/cobrancas",
        tool="propor_cobranca",
        args={"cliente_crm_id": cliente_crm_id, "valor": float(valor), "vencimento": vencimento},
        entity_type="inter_cobranca", inserir=_inserir,
    )


COBRANCA_TOOL: ToolDef = register(ToolDef(
    "propor_cobranca", "comercial",
    "Propor a emissão de uma cobrança a um cliente (fica PENDENTE de aprovação; não emite nada).",
    _ARGS_COBRANCA, _propor_cobranca, scope_kind="org",
))
```

- [ ] **Step 2: Escrever o subteste de cobrança (bloco `__main__`, será estendido nas Tasks 4-5)**

Adicionar ao fim de `onda_a.py`:

```python
if __name__ == "__main__":
    import asyncio
    import os

    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    class _U:
        id = "00000000-0000-0000-0000-0000000000ff"
        role = "comercial"

    class _S:
        tier = "gestor"

    async def _limpar(db, idem_like: str) -> None:
        ids = [x for x in (await db.execute(text(
            "SELECT id::text FROM communication_notifications "
            "WHERE extra_data->>'idempotency_key' LIKE :k"), {"k": idem_like})).scalars().all()]
        if ids:
            await db.execute(text("DELETE FROM communication_notifications WHERE id = ANY(:i)"), {"i": ids})
        await db.commit()

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        async with Session() as db:
            cob_ids: list[str] = []
            aud_ids: list[str] = []
            try:
                # baseline: nenhuma cobrança RECEBIDA/executada deve mudar
                recebidas_antes = (await db.execute(text(
                    "SELECT count(*) FROM inter_cobrancas WHERE status <> 'PENDENTE'"))).scalar()

                venc = "2099-12-31"  # data marcada de teste (nunca real)
                r = await _propor_cobranca(db, _U(), _S(),
                    cliente_crm_id="TESTE-5.4-CLI", valor=12.34, vencimento=venc,
                    descricao="__TESTE_5.4__ cobranca")
                assert r["status"] == "pendente", r
                eid = r["entity_id"]
                cob_ids.append(eid)

                # a linha nasceu PENDENTE (nunca emitida)
                st = (await db.execute(text(
                    "SELECT status FROM inter_cobrancas WHERE id = :i"), {"i": eid})).scalar()
                assert st == "PENDENTE", f"status inesperado: {st}"

                # estado de execução INTOCADO
                recebidas_depois = (await db.execute(text(
                    "SELECT count(*) FROM inter_cobrancas WHERE status <> 'PENDENTE'"))).scalar()
                assert recebidas_depois == recebidas_antes, "cobrança executada mudou (não deveria)"

                # idempotência
                r2 = await _propor_cobranca(db, _U(), _S(),
                    cliente_crm_id="TESTE-5.4-CLI", valor=12.34, vencimento=venc,
                    descricao="__TESTE_5.4__ cobranca")
                assert r2.get("duplicado") is True, r2
                print("SUBTESTE cobrança PASS (pendente criado, execução intocada, idempotente)")
                print("TODOS OS SUBTESTES DE onda_a.py PASSARAM")
            finally:
                if cob_ids:
                    await db.execute(text("DELETE FROM inter_cobrancas WHERE id = ANY(:i)"), {"i": cob_ids})
                await _limpar(db, "cobranca:TESTE-5.4-CLI:%")
                rem = (await db.execute(text(
                    "SELECT count(*) FROM inter_cobrancas WHERE pagador->>'cliente_crm_id' = 'TESTE-5.4-CLI'"))).scalar()
                assert rem == 0, f"remanescentes cobranca={rem}"
        await eng.dispose()

    asyncio.run(main())
```

- [ ] **Step 3: Rodar o subteste de cobrança na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/onda_a.py
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/onda_a.py'
```
Expected: `SUBTESTE cobrança PASS ...` → `TODOS OS SUBTESTES DE onda_a.py PASSARAM`

- [ ] **Step 4: Commit**

```bash
git add backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py
git commit --no-verify -m "feat(5.4): propor_cobranca (Onda A, 🟡) — pendente em inter_cobrancas, execução intocada

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Onda A — `propor_proposta_comercial` (🟡)

**Files:**
- Modify: `backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py`
- Test: `onda_a.py` (bloco `__main__`, subteste proposta)

**Interfaces:**
- Consumes: `base.propor`, `base.ROLES_COMERCIAL`.
- Produces: `PROPOSTA_TOOL: ToolDef` (name=`propor_proposta_comercial`, module=`comercial`, scope_kind=`org`) e handler `_propor_proposta(db,user,scope,*,client_name,title,valor_total,descricao="",**_)`.

- [ ] **Step 1: Adicionar o handler de proposta em `onda_a.py`** (antes do bloco `__main__`)

```python
# ───────────────────────── proposta comercial (🟡, draft) ───────────────────

_ARGS_PROPOSTA = {
    "type": "object",
    "properties": {
        "client_name": {"type": "string", "minLength": 2, "description": "Nome do cliente (obrigatório)."},
        "title": {"type": "string", "minLength": 2, "description": "Título da proposta."},
        "valor_total": {"type": "number", "minimum": 0, "description": "Valor total (R$)."},
        "descricao": {"type": "string", "maxLength": 2000},
    },
    "required": ["client_name", "title", "valor_total"],
}


async def _propor_proposta(
    db, user, scope, *, client_name: str, title: str,
    valor_total: float, descricao: str = "", **_
) -> dict[str, Any]:
    client_name = (client_name or "").strip()
    title = (title or "").strip()
    if len(client_name) < 2 or len(title) < 2:
        return {"erro": "client_name e title são obrigatórios"}
    idem = f"proposta:{client_name}:{title}:{float(valor_total):.2f}"

    async def _inserir(db) -> str:
        pid = str(uuid.uuid4())
        number = f"PROP-{pid[:8].upper()}"
        await db.execute(text("""
            INSERT INTO proposals
                (id, number, version, client_name, title, description, proposal_type,
                 subtotal, discount_value, taxes, total, installments, issue_date,
                 status, created_by_id, is_active, created_at, updated_at)
            VALUES
                (:id, :num, 1, :cli, :ttl, :desc, 'service',
                 :val, 0, 0, :val, 1, :hoje,
                 'draft', :uid, true, now(), now())
        """), {"id": pid, "num": number, "cli": client_name, "ttl": title,
               "desc": descricao or None, "val": float(valor_total),
               "hoje": date.today(), "uid": str(getattr(user, "id", None))})
        return pid

    return await propor(
        db, user=user, scope=scope, dominio="proposta", gate="🟡",
        roles_aprovador=ROLES_COMERCIAL, idempotency_key=idem,
        titulo="[Proposta] Enviar proposta comercial",
        corpo=f"Proposta '{title}' para {client_name} (R$ {float(valor_total):.2f}) em rascunho. Aguarda sua revisão/envio.",
        action_url="/comercial/propostas",
        tool="propor_proposta_comercial",
        args={"client_name": client_name, "title": title, "valor_total": float(valor_total)},
        entity_type="proposal", inserir=_inserir,
    )


PROPOSTA_TOOL: ToolDef = register(ToolDef(
    "propor_proposta_comercial", "comercial",
    "Criar um RASCUNHO de proposta comercial (fica draft; o envio/assinatura ao cliente é aprovado por humano).",
    _ARGS_PROPOSTA, _propor_proposta, scope_kind="org",
))
```

- [ ] **Step 2: Adicionar o subteste de proposta ao `main()` de `onda_a.py`**

Dentro do `try:` de `main()`, logo após o bloco de cobrança e antes do `print("TODOS OS SUBTESTES...")`, inserir:

```python
                # ── proposta comercial ──
                prop_baseline = (await db.execute(text(
                    "SELECT count(*) FROM proposals WHERE status = 'sent'"))).scalar()
                rp = await _propor_proposta(db, _U(), _S(),
                    client_name="__TESTE_5.4__ Cliente", title="__TESTE_5.4__ Título",
                    valor_total=999.99, descricao="teste")
                assert rp["status"] == "pendente", rp
                pid = rp["entity_id"]
                cob_ids  # (reuso da lista abaixo via prop_ids separada)
                prop_ids.append(pid)
                st = (await db.execute(text("SELECT status FROM proposals WHERE id = :i"), {"i": pid})).scalar()
                assert st == "draft", f"proposta nasceu {st}, esperado draft"
                prop_sent = (await db.execute(text(
                    "SELECT count(*) FROM proposals WHERE status = 'sent'"))).scalar()
                assert prop_sent == prop_baseline, "proposta enviada mudou (não deveria)"
                rp2 = await _propor_proposta(db, _U(), _S(),
                    client_name="__TESTE_5.4__ Cliente", title="__TESTE_5.4__ Título",
                    valor_total=999.99)
                assert rp2.get("duplicado") is True, rp2
                print("SUBTESTE proposta PASS (draft criado, sent intocado, idempotente)")
```

E declarar `prop_ids: list[str] = []` junto de `cob_ids`/`aud_ids`, e no `finally` adicionar a limpeza:

```python
                if prop_ids:
                    await db.execute(text("DELETE FROM proposals WHERE id = ANY(:i)"), {"i": prop_ids})
                await _limpar(db, "proposta:__TESTE_5.4__%")
```

(A linha `cob_ids  # (...)` do rascunho acima é só um lembrete — removê-la; o real é `prop_ids.append(pid)`.)

- [ ] **Step 3: Rodar os subtestes na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/onda_a.py
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/onda_a.py'
```
Expected: `SUBTESTE cobrança PASS ...` + `SUBTESTE proposta PASS ...` + `TODOS OS SUBTESTES DE onda_a.py PASSARAM`

- [ ] **Step 4: Commit**

```bash
git add backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py
git commit --no-verify -m "feat(5.4): propor_proposta_comercial (Onda A, 🟡) — draft em proposals, sent intocado

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Onda A — `propor_kit` (🔵)

**Files:**
- Modify: `backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py`
- Test: `onda_a.py` (bloco `__main__`, subteste kit)

**Interfaces:**
- Consumes: `base.propor`, `base.ROLES_KIT_OP`.
- Produces: `KIT_TOOL: ToolDef` (name=`propor_kit`, module=`gedeon`, scope_kind=`org`) e handler `_propor_kit(db,user,scope,*,client_id,tipo_kit,descricao="",**_)`.

- [ ] **Step 1: Confirmar as colunas mínimas de `ged_document_kits`**

Run (só leitura, na bancada):
```bash
CID=conecta-pro-backend
docker exec $CID psql "$POSTGRES_DB" -U "$POSTGRES_USER" -c "\d ged_document_kits" 2>/dev/null || \
docker exec -e PGPASSWORD="$POSTGRES_PASSWORD" $CID psql -h "$POSTGRES_HOST" -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "\d ged_document_kits"
```
Expected: lista de colunas. Ajustar o INSERT do Step 2 às colunas reais NOT NULL (o esqueleto abaixo assume `id, client_id, tipo, status, created_at, updated_at`; se a coluna de status/tipo tiver outro nome, alinhar aqui — este é o único ponto que depende do schema real do kit).

- [ ] **Step 2: Adicionar o handler de kit em `onda_a.py`** (antes do `__main__`)

```python
# ───────────────────────── kit documental (🔵, reversível interno) ───────────

_ARGS_KIT = {
    "type": "object",
    "properties": {
        "client_id": {"type": "string", "description": "ID do cliente/condomínio do kit."},
        "tipo_kit": {"type": "string", "minLength": 2, "description": "Tipo/modelo do kit documental."},
        "descricao": {"type": "string", "maxLength": 500},
    },
    "required": ["client_id", "tipo_kit"],
}


async def _propor_kit(
    db, user, scope, *, client_id: str, tipo_kit: str, descricao: str = "", **_
) -> dict[str, Any]:
    if not client_id or len((tipo_kit or "").strip()) < 2:
        return {"erro": "client_id e tipo_kit são obrigatórios"}
    idem = f"kit:{client_id}:{tipo_kit}"

    async def _inserir(db) -> str:
        kid = str(uuid.uuid4())
        # Colunas alinhadas ao \d ged_document_kits do Step 1. 'proposto' = pendente
        # de montagem/envio pelo kit_orchestrator humano (nunca monta/envia aqui).
        await db.execute(text("""
            INSERT INTO ged_document_kits (id, client_id, tipo, status, created_at, updated_at)
            VALUES (:id, :cli, :tipo, 'proposto', now(), now())
        """), {"id": kid, "cli": client_id, "tipo": tipo_kit})
        return kid

    return await propor(
        db, user=user, scope=scope, dominio="kit", gate="🔵",
        roles_aprovador=ROLES_KIT_OP, idempotency_key=idem,
        titulo="[Proposta] Montar kit documental",
        corpo=f"Kit '{tipo_kit}' para cliente {client_id}. Aguarda aprovação p/ montar/enviar.",
        action_url="/gedeon/kits",
        tool="propor_kit",
        args={"client_id": client_id, "tipo_kit": tipo_kit},
        entity_type="ged_document_kit", inserir=_inserir,
    )


KIT_TOOL: ToolDef = register(ToolDef(
    "propor_kit", "gedeon",
    "Propor a montagem de um kit documental (fica 'proposto'; a montagem/envio é aprovada por humano).",
    _ARGS_KIT, _propor_kit, scope_kind="org",
))
```

- [ ] **Step 2b: Alinhar `ONDA_A_TOOLS` (export para o wiring)**

Adicionar, logo após `KIT_TOOL`:

```python
ONDA_A_TOOLS = [COBRANCA_TOOL, PROPOSTA_TOOL, KIT_TOOL]
```

- [ ] **Step 3: Adicionar o subteste de kit ao `main()` de `onda_a.py`**

Dentro do `try:`, após o subteste de proposta:

```python
                # ── kit documental ──
                rk = await _propor_kit(db, _U(), _S(),
                    client_id="__TESTE_5.4__ CLI", tipo_kit="__TESTE_5.4__ kit")
                assert rk["status"] == "pendente", rk
                kid = rk["entity_id"]
                kit_ids.append(kid)
                st = (await db.execute(text("SELECT status FROM ged_document_kits WHERE id = :i"), {"i": kid})).scalar()
                assert st == "proposto", f"kit nasceu {st}, esperado proposto"
                rk2 = await _propor_kit(db, _U(), _S(),
                    client_id="__TESTE_5.4__ CLI", tipo_kit="__TESTE_5.4__ kit")
                assert rk2.get("duplicado") is True, rk2
                print("SUBTESTE kit PASS (proposto criado, idempotente)")
```

Declarar `kit_ids: list[str] = []` junto das outras listas; no `finally` adicionar:

```python
                if kit_ids:
                    await db.execute(text("DELETE FROM ged_document_kits WHERE id = ANY(:i)"), {"i": kit_ids})
                await _limpar(db, "kit:__TESTE_5.4__%")
```

- [ ] **Step 4: Rodar os subtestes na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/onda_a.py
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/onda_a.py'
```
Expected: `SUBTESTE cobrança PASS` + `SUBTESTE proposta PASS` + `SUBTESTE kit PASS` + `TODOS OS SUBTESTES DE onda_a.py PASSARAM`

- [ ] **Step 5: Commit**

```bash
git add backend/modules/ai/conversation/services/orquestrador/acoes/onda_a.py
git commit --no-verify -m "feat(5.4): propor_kit (Onda A, 🔵) + ONDA_A_TOOLS export

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Onda B — tabela `op_substituicao_propostas` + `propor_substituicao`

**Files:**
- Create: `backend/alembic/versions/fase54_op_substituicao_propostas.py`
- Create: `backend/modules/ai/conversation/services/orquestrador/acoes/onda_b.py`
- Test: `onda_b.py` (bloco `__main__`)

**Interfaces:**
- Consumes: `base.propor`, `base.ROLES_KIT_OP`.
- Produces: tabela `op_substituicao_propostas`; `SUBSTITUICAO_TOOL: ToolDef` (name=`propor_substituicao`, module=`operacional`, scope_kind=`org`) e handler `_propor_substituicao(db,user,scope,*,post_id,data,ausente_employee_id,substituto_employee_id,motivo="",**_)`.

- [ ] **Step 1: Escrever a migration da tabela nova**

Create `backend/alembic/versions/fase54_op_substituicao_propostas.py`:

```python
"""fase5.4 Onda B — op_substituicao_propostas (proposta de substituição de posto)

O agente PROPÕE substituto; o gestor aprova e aplica MANUALMENTE. Operacional
(allocations/escala) permanece READ-ONLY para o agente.

Revision ID: fase54_op_subst
Revises: fase54_gp_just_fix
"""
from alembic import op
import sqlalchemy as sa

revision = "fase54_op_subst"
down_revision = "fase54_gp_just_fix"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "op_substituicao_propostas",
        sa.Column("id", sa.Text(), primary_key=True),
        sa.Column("post_id", sa.Text(), nullable=False),
        sa.Column("data", sa.Date(), nullable=False),
        sa.Column("ausente_employee_id", sa.Text(), nullable=False),
        sa.Column("substituto_employee_id", sa.Text(), nullable=False),
        sa.Column("motivo", sa.Text(), nullable=True),
        sa.Column("status", sa.Text(), nullable=False, server_default=sa.text("'pendente'")),
        sa.Column("proposto_por", sa.Text(), nullable=True),
        sa.Column("aprovado_por", sa.Text(), nullable=True),
        sa.Column("aprovado_em", sa.TIMESTAMP(), nullable=True),
        sa.Column("created_at", sa.TIMESTAMP(), nullable=False,
                  server_default=sa.text("(now() AT TIME ZONE 'America/Manaus')")),
    )
    op.create_index("ix_op_subst_pendentes", "op_substituicao_propostas", ["status"])


def downgrade() -> None:
    op.drop_index("ix_op_subst_pendentes", table_name="op_substituicao_propostas")
    op.drop_table("op_substituicao_propostas")
```

- [ ] **Step 2: Escrever o handler `_propor_substituicao`**

Create `backend/modules/ai/conversation/services/orquestrador/acoes/onda_b.py`:

```python
"""Fase 5.4 — Onda B: substituição de posto (propor→humano).

O agente PROPÕE um substituto; a proposta cai em op_substituicao_propostas
'pendente'. O gestor aprova e aplica MANUALMENTE. NUNCA escreve allocations
(operacional READ-ONLY para o agente).
"""
from __future__ import annotations

import uuid
from typing import Any

from sqlalchemy import text

from ..tool_registry import ToolDef, register
from .base import ROLES_KIT_OP, propor

_ARGS_SUBST = {
    "type": "object",
    "properties": {
        "post_id": {"type": "string", "description": "ID do posto a cobrir."},
        "data": {"type": "string", "description": "Data da cobertura AAAA-MM-DD."},
        "ausente_employee_id": {"type": "string", "description": "Quem faltará/faltou."},
        "substituto_employee_id": {"type": "string", "description": "Substituto proposto."},
        "motivo": {"type": "string", "maxLength": 500},
    },
    "required": ["post_id", "data", "ausente_employee_id", "substituto_employee_id"],
}


async def _propor_substituicao(
    db, user, scope, *, post_id: str, data: str, ausente_employee_id: str,
    substituto_employee_id: str, motivo: str = "", **_
) -> dict[str, Any]:
    if not (post_id and data and ausente_employee_id and substituto_employee_id):
        return {"erro": "post_id, data, ausente e substituto são obrigatórios"}
    if ausente_employee_id == substituto_employee_id:
        return {"erro": "substituto não pode ser a própria pessoa ausente"}
    idem = f"substituicao:{post_id}:{data}:{ausente_employee_id}"

    async def _inserir(db) -> str:
        sid = str(uuid.uuid4())
        await db.execute(text("""
            INSERT INTO op_substituicao_propostas
                (id, post_id, data, ausente_employee_id, substituto_employee_id,
                 motivo, status, proposto_por)
            VALUES (:id, :pid, :dt, :aus, :sub, :mot, 'pendente', :prop)
        """), {"id": sid, "pid": post_id, "dt": data, "aus": ausente_employee_id,
               "sub": substituto_employee_id, "mot": motivo or None,
               "prop": str(getattr(user, "id", None))})
        return sid

    return await propor(
        db, user=user, scope=scope, dominio="substituicao", gate="🔵",
        roles_aprovador=ROLES_KIT_OP, idempotency_key=idem,
        titulo="[Proposta] Substituição de posto",
        corpo=f"Cobrir posto {post_id} em {data}: propor {substituto_employee_id} no lugar de {ausente_employee_id}. {motivo or ''}".strip(),
        action_url="/operacional/escala",
        tool="propor_substituicao",
        args={"post_id": post_id, "data": data, "ausente_employee_id": ausente_employee_id,
              "substituto_employee_id": substituto_employee_id},
        entity_type="op_substituicao_proposta", inserir=_inserir,
    )


SUBSTITUICAO_TOOL: ToolDef = register(ToolDef(
    "propor_substituicao", "operacional",
    "Propor um substituto para cobrir um posto (fica PENDENTE; o gestor aprova e aplica a escala à mão — o agente NUNCA altera a escala).",
    _ARGS_SUBST, _propor_substituicao, scope_kind="org",
))

SUBSTITUICAO_TOOLS = [SUBSTITUICAO_TOOL]
```

- [ ] **Step 3: Escrever o teste (bloco `__main__` em `onda_b.py`)** — prova pendente + escala INTOCADA

```python
if __name__ == "__main__":
    import asyncio
    import os

    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    class _U:
        id = "00000000-0000-0000-0000-0000000000ff"
        role = "gerente_operacional"

    class _S:
        tier = "gestor"

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        async with Session() as db:
            sub_ids: list[str] = []
            try:
                # snapshot de allocations: substituição NUNCA pode mexer nisto
                aloc_antes = (await db.execute(text("SELECT count(*) FROM allocations"))).scalar()

                r = await _propor_substituicao(db, _U(), _S(),
                    post_id="__TESTE_5.4__POSTO", data="2099-01-01",
                    ausente_employee_id="EMP-A", substituto_employee_id="EMP-B",
                    motivo="__TESTE_5.4__")
                assert r["status"] == "pendente", r
                sid = r["entity_id"]
                sub_ids.append(sid)
                st = (await db.execute(text(
                    "SELECT status FROM op_substituicao_propostas WHERE id = :i"), {"i": sid})).scalar()
                assert st == "pendente", st

                # escala INTOCADA
                aloc_depois = (await db.execute(text("SELECT count(*) FROM allocations"))).scalar()
                assert aloc_depois == aloc_antes, "allocations mudou (operacional deveria ser read-only)"

                # idempotência
                r2 = await _propor_substituicao(db, _U(), _S(),
                    post_id="__TESTE_5.4__POSTO", data="2099-01-01",
                    ausente_employee_id="EMP-A", substituto_employee_id="EMP-B")
                assert r2.get("duplicado") is True, r2
                print("TESTE substituição PASS (pendente criado, escala intocada, idempotente)")
                print("TODOS OS TESTES DE onda_b.py PASSARAM")
            finally:
                if sub_ids:
                    await db.execute(text("DELETE FROM op_substituicao_propostas WHERE id = ANY(:i)"), {"i": sub_ids})
                ids = [x for x in (await db.execute(text(
                    "SELECT id::text FROM communication_notifications "
                    "WHERE extra_data->>'idempotency_key' LIKE 'substituicao:__TESTE_5.4__%'"))).scalars().all()]
                if ids:
                    await db.execute(text("DELETE FROM communication_notifications WHERE id = ANY(:i)"), {"i": ids})
                await db.commit()
                rem = (await db.execute(text(
                    "SELECT count(*) FROM op_substituicao_propostas WHERE post_id = '__TESTE_5.4__POSTO'"))).scalar()
                assert rem == 0, f"remanescentes subst={rem}"
        await eng.dispose()

    asyncio.run(main())
```

- [ ] **Step 4: Aplicar a migration e rodar o teste na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/alembic/versions/fase54_op_substituicao_propostas.py $CID:/app/alembic/versions/fase54_op_substituicao_propostas.py
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/onda_b.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/onda_b.py
docker exec -e PYTHONPATH=/app $CID sh -lc 'cd /app && alembic upgrade fase54_op_subst'
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/onda_b.py'
```
Expected: `alembic ... Running upgrade ... fase54_op_subst` + `TESTE substituição PASS ...` + `TODOS OS TESTES DE onda_b.py PASSARAM`

- [ ] **Step 5: Commit**

```bash
git add backend/alembic/versions/fase54_op_substituicao_propostas.py \
        backend/modules/ai/conversation/services/orquestrador/acoes/onda_b.py
git commit --no-verify -m "feat(5.4): Onda B — op_substituicao_propostas + propor_substituicao (escala read-only)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Onda C — `propor_lote_pagamento` (🔴, dinheiro que SAI)

**Files:**
- Create: `backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py`
- Test: `onda_c.py` (bloco `__main__`, subteste lote)

**Interfaces:**
- Consumes: `base.propor`, `base.ROLES_MONEY`; `folha_lote_service.preparar_lote(db, posto, competencia, itens, user_id)` (INTACTO — só grava `inter_payments` 'preparado', não move dinheiro).
- Produces: `LOTE_TOOL: ToolDef` (name=`propor_lote_pagamento`, module=`financeiro`, scope_kind=`org`) e handler `_propor_lote(db,user,scope,*,posto,competencia,itens,**_)`.

- [ ] **Step 1: Escrever o handler `_propor_lote`** (grava proposta ≤ teto; NUNCA executa)

Create `backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py`:

```python
"""Fase 5.4 — Onda C (🔴): lote de pagamento + eSocial.

Ambas 🔴: o agente só PROPÕE. A execução real exige gate humano existente:
- lote: preparar_lote grava inter_payments 'preparado' (não move dinheiro); a
  execução (gerar_otp_lote + executar_lote com OTP no email do Jordan) é 100%
  humana e permanece INTACTA. Teto CONECTA_LIMITE_DIARIO_PAGAMENTOS respeitado.
- eSocial: marca esocial_transmissao_propostas 'pendente'; o humano dispara a
  transmissão via esocial_tasks. O agente NUNCA transmite ao gov.
"""
from __future__ import annotations

import os
import uuid
from typing import Any

from sqlalchemy import text

from ..tool_registry import ToolDef, register
from .base import ROLES_MONEY, propor

# ───────────────────────── lote de pagamento (🔴, dinheiro SAI) ──────────────

_ARGS_LOTE = {
    "type": "object",
    "properties": {
        "posto": {"type": "string", "description": "Posto/condomínio do lote."},
        "competencia": {"type": "string", "description": "Competência AAAA-MM."},
        "itens": {
            "type": "array",
            "description": "Beneficiários do lote.",
            "items": {
                "type": "object",
                "properties": {
                    "nome": {"type": "string"},
                    "chave": {"type": "string", "description": "Chave PIX."},
                    "valor": {"type": "number", "minimum": 0.01},
                },
                "required": ["nome", "chave", "valor"],
            },
        },
    },
    "required": ["posto", "competencia", "itens"],
}


async def _propor_lote(
    db, user, scope, *, posto: str, competencia: str, itens: list[dict], **_
) -> dict[str, Any]:
    if not posto or not competencia or not itens:
        return {"erro": "posto, competencia e itens são obrigatórios"}
    total = sum(float(it.get("valor") or 0) for it in itens)
    if total <= 0:
        return {"erro": "total do lote deve ser > 0"}
    # Teto de PROPOSTA = mesmo teto diário do Inter (defesa em profundidade).
    teto = float(os.environ.get("CONECTA_LIMITE_DIARIO_PAGAMENTOS") or "100000")
    if total > teto:
        return {"erro": f"total do lote (R$ {total:.2f}) acima do teto (R$ {teto:.2f}); use o fluxo manual/OTP"}
    idem = f"lote:{posto}:{competencia}:{total:.2f}"

    async def _inserir(db) -> str:
        # preparar_lote SÓ grava inter_payments 'preparado' (não move dinheiro,
        # não gera OTP, não chama a API). A execução fica 100% humana.
        from modules.integrations.inter.services.folha_lote_service import preparar_lote
        res = await preparar_lote(db, posto=posto, competencia=competencia,
                                  itens=itens, user_id=str(getattr(user, "id", None)))
        return res["lote_id"]

    return await propor(
        db, user=user, scope=scope, dominio="lote_pagamento", gate="🔴",
        roles_aprovador=ROLES_MONEY, idempotency_key=idem,
        titulo="[Proposta 🔴] Lote de pagamento — exige OTP",
        corpo=f"Lote de {len(itens)} pagamento(s) para {posto} ({competencia}), total R$ {total:.2f}. "
              f"NADA foi pago — exige sua aprovação + OTP no fluxo do Financeiro.",
        action_url="/financeiro/pagamentos/lote",
        tool="propor_lote_pagamento",
        args={"posto": posto, "competencia": competencia, "total": total, "n": len(itens)},
        entity_type="inter_payments_lote", inserir=_inserir,
    )


LOTE_TOOL: ToolDef = register(ToolDef(
    "propor_lote_pagamento", "financeiro",
    "Propor um lote de pagamento de folha (grava inter_payments 'preparado'; o pagamento exige aprovação humana + OTP — o agente NUNCA paga).",
    _ARGS_LOTE, _propor_lote, scope_kind="org",
))
```

- [ ] **Step 2: Escrever o subteste de lote (bloco `__main__`, estendido na Task 8)**

Adicionar ao fim de `onda_c.py`:

```python
if __name__ == "__main__":
    import asyncio

    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

    class _U:
        id = "00000000-0000-0000-0000-0000000000ff"
        role = "admin"

    class _S:
        tier = "gestor"

    async def main() -> None:
        eng = create_async_engine(os.environ["DATABASE_URL"])
        Session = async_sessionmaker(eng, expire_on_commit=False)
        async with Session() as db:
            lote_ids: list[str] = []
            esoc_ids: list[str] = []
            try:
                # baseline: NENHUM pagamento pode sair (executado) por causa da proposta
                exec_antes = (await db.execute(text(
                    "SELECT count(*) FROM inter_payments WHERE status = 'executado'"))).scalar()

                itens = [{"nome": "__TESTE_5.4__ Fulano", "chave": "teste@teste", "valor": 1.00}]
                r = await _propor_lote(db, _U(), _S(),
                    posto="__TESTE_5.4__POSTO", competencia="2099-01", itens=itens)
                assert r["status"] == "pendente", r
                lote_id = r["entity_id"]
                lote_ids.append(lote_id)

                # os pagamentos do lote nasceram 'preparado' (nunca 'executado')
                sts = [x for x in (await db.execute(text(
                    "SELECT DISTINCT status FROM inter_payments WHERE lote_id = :l"), {"l": lote_id})).scalars().all()]
                assert sts == ["preparado"], f"status inesperado no lote: {sts}"

                # PROVA 🔴: nenhum pagamento foi executado (nada de dinheiro sem OTP)
                exec_depois = (await db.execute(text(
                    "SELECT count(*) FROM inter_payments WHERE status = 'executado'"))).scalar()
                assert exec_depois == exec_antes, "pagamento executado sem OTP (VIOLAÇÃO 🔴)"

                # teto: lote acima do teto é recusado ANTES de gravar
                grande = [{"nome": "x", "chave": "k", "valor": 10_000_000.0}]
                rg = await _propor_lote(db, _U(), _S(),
                    posto="__TESTE_5.4__POSTO2", competencia="2099-01", itens=grande)
                assert "erro" in rg and "teto" in rg["erro"], rg

                # idempotência
                r2 = await _propor_lote(db, _U(), _S(),
                    posto="__TESTE_5.4__POSTO", competencia="2099-01", itens=itens)
                assert r2.get("duplicado") is True, r2
                print("SUBTESTE lote PASS (preparado criado, 0 executado, teto e idempotência OK)")
                # (o subteste de eSocial é acrescentado na Task 8)
                print("TODOS OS SUBTESTES DE onda_c.py PASSARAM")
            finally:
                for lid in lote_ids:
                    await db.execute(text("DELETE FROM inter_payments WHERE lote_id = :l"), {"l": lid})
                ids = [x for x in (await db.execute(text(
                    "SELECT id::text FROM communication_notifications "
                    "WHERE extra_data->>'idempotency_key' LIKE 'lote:__TESTE_5.4__%' "
                    "   OR extra_data->>'idempotency_key' LIKE 'esocial:__TESTE_5.4__%'"))).scalars().all()]
                if ids:
                    await db.execute(text("DELETE FROM communication_notifications WHERE id = ANY(:i)"), {"i": ids})
                if esoc_ids:
                    await db.execute(text("DELETE FROM esocial_transmissao_propostas WHERE id = ANY(:i)"), {"i": esoc_ids})
                await db.commit()
                rem = (await db.execute(text(
                    "SELECT count(*) FROM inter_payments WHERE observacoes LIKE '%__TESTE_5.4__POSTO%'"))).scalar()
                assert rem == 0, f"remanescentes lote={rem}"
        await eng.dispose()

    asyncio.run(main())
```

- [ ] **Step 3: Rodar o subteste de lote na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/onda_c.py
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/onda_c.py'
```
Expected: `SUBTESTE lote PASS (preparado criado, 0 executado, teto e idempotência OK)` + `TODOS OS SUBTESTES DE onda_c.py PASSARAM`

- [ ] **Step 4: Commit**

```bash
git add backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py
git commit --no-verify -m "feat(5.4): propor_lote_pagamento (Onda C, 🔴) — preparado só, 0 executado sem OTP

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Onda C — tabela `esocial_transmissao_propostas` + `propor_esocial` (🔴)

**Files:**
- Create: `backend/alembic/versions/fase54_esocial_propostas.py`
- Modify: `backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py`
- Test: `onda_c.py` (bloco `__main__`, subteste eSocial)

**Interfaces:**
- Consumes: `base.propor`, `base.ROLES_MONEY`.
- Produces: tabela `esocial_transmissao_propostas`; `ESOCIAL_TOOL: ToolDef` (name=`propor_esocial`, module=`pessoas`, scope_kind=`org`) e handler `_propor_esocial(db,user,scope,*,tipo_evento,ref_id,employee_id="",**_)`; export `ONDA_C_TOOLS`.

- [ ] **Step 1: Escrever a migration da tabela de propostas eSocial**

Create `backend/alembic/versions/fase54_esocial_propostas.py`:

```python
"""fase5.4 Onda C — esocial_transmissao_propostas (proposta de transmissão eSocial)

O agente PROPÕE o evento pendente; o humano confirma e transmite via esocial_tasks.
O agente NUNCA transmite ao gov (ato legal 🔴).

Revision ID: fase54_esocial_prop
Revises: fase54_op_subst
"""
from alembic import op
import sqlalchemy as sa

revision = "fase54_esocial_prop"
down_revision = "fase54_op_subst"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "esocial_transmissao_propostas",
        sa.Column("id", sa.Text(), primary_key=True),
        sa.Column("tipo_evento", sa.Text(), nullable=False),   # ex.: S-2230
        sa.Column("ref_id", sa.Text(), nullable=False),        # id do evento-fonte
        sa.Column("employee_id", sa.Text(), nullable=True),
        sa.Column("status", sa.Text(), nullable=False, server_default=sa.text("'pendente'")),
        sa.Column("proposto_por", sa.Text(), nullable=True),
        sa.Column("transmitido_por", sa.Text(), nullable=True),
        sa.Column("transmitido_em", sa.TIMESTAMP(), nullable=True),
        sa.Column("created_at", sa.TIMESTAMP(), nullable=False,
                  server_default=sa.text("(now() AT TIME ZONE 'America/Manaus')")),
    )
    op.create_index("ix_esocial_prop_pendentes", "esocial_transmissao_propostas", ["status"])


def downgrade() -> None:
    op.drop_index("ix_esocial_prop_pendentes", table_name="esocial_transmissao_propostas")
    op.drop_table("esocial_transmissao_propostas")
```

- [ ] **Step 2: Adicionar o handler `_propor_esocial` em `onda_c.py`** (antes do `__main__`)

```python
# ───────────────────────── eSocial (🔴, ato legal-gov) ──────────────────────

_ARGS_ESOCIAL = {
    "type": "object",
    "properties": {
        "tipo_evento": {"type": "string", "description": "Evento eSocial (ex.: S-2210, S-2220, S-2230)."},
        "ref_id": {"type": "string", "description": "ID do evento-fonte (CAT/ASO/afastamento)."},
        "employee_id": {"type": "string", "description": "Colaborador relacionado (opcional)."},
    },
    "required": ["tipo_evento", "ref_id"],
}


async def _propor_esocial(
    db, user, scope, *, tipo_evento: str, ref_id: str, employee_id: str = "", **_
) -> dict[str, Any]:
    if not tipo_evento or not ref_id:
        return {"erro": "tipo_evento e ref_id são obrigatórios"}
    idem = f"esocial:{tipo_evento}:{ref_id}"

    async def _inserir(db) -> str:
        eid = str(uuid.uuid4())
        # SÓ marca pendente. NENHUMA chamada a esocial_tasks/transmitir_evento_sst.
        await db.execute(text("""
            INSERT INTO esocial_transmissao_propostas
                (id, tipo_evento, ref_id, employee_id, status, proposto_por)
            VALUES (:id, :tp, :ref, :emp, 'pendente', :prop)
        """), {"id": eid, "tp": tipo_evento, "ref": ref_id,
               "emp": employee_id or None, "prop": str(getattr(user, "id", None))})
        return eid

    return await propor(
        db, user=user, scope=scope, dominio="esocial", gate="🔴",
        roles_aprovador=ROLES_MONEY, idempotency_key=idem,
        titulo="[Proposta 🔴] Transmissão eSocial — exige confirmação humana",
        corpo=f"Evento {tipo_evento} (ref {ref_id}) pronto para transmitir. "
              f"NADA foi transmitido ao gov — exige sua confirmação no fluxo do eSocial.",
        action_url="/pessoas/esocial",
        tool="propor_esocial",
        args={"tipo_evento": tipo_evento, "ref_id": ref_id},
        entity_type="esocial_transmissao_proposta", inserir=_inserir,
    )


ESOCIAL_TOOL: ToolDef = register(ToolDef(
    "propor_esocial", "pessoas",
    "Propor a transmissão de um evento eSocial (fica PENDENTE; a transmissão ao gov é confirmada por humano — o agente NUNCA transmite).",
    _ARGS_ESOCIAL, _propor_esocial, scope_kind="org",
))

ONDA_C_TOOLS = [LOTE_TOOL, ESOCIAL_TOOL]
```

- [ ] **Step 3: Adicionar o subteste de eSocial ao `main()` de `onda_c.py`**

Dentro do `try:`, logo após o subteste de lote e antes do `print("TODOS OS SUBTESTES...")`, inserir:

```python
                # ── eSocial ──
                re = await _propor_esocial(db, _U(), _S(),
                    tipo_evento="S-2230", ref_id="__TESTE_5.4__REF", employee_id="EMP-X")
                assert re["status"] == "pendente", re
                eid = re["entity_id"]
                esoc_ids.append(eid)
                st = (await db.execute(text(
                    "SELECT status FROM esocial_transmissao_propostas WHERE id = :i"), {"i": eid})).scalar()
                assert st == "pendente", st
                # PROVA 🔴: não transmitiu (transmitido_em permanece NULL)
                tx = (await db.execute(text(
                    "SELECT transmitido_em FROM esocial_transmissao_propostas WHERE id = :i"), {"i": eid})).scalar()
                assert tx is None, "eSocial marcou transmissão (VIOLAÇÃO 🔴)"
                re2 = await _propor_esocial(db, _U(), _S(),
                    tipo_evento="S-2230", ref_id="__TESTE_5.4__REF")
                assert re2.get("duplicado") is True, re2
                print("SUBTESTE eSocial PASS (pendente, não transmite, idempotente)")
```

E no `finally`, a limpeza de `esoc_ids` já está prevista no Step 2 da Task 7 (o bloco `if esoc_ids:` e a limpeza das notificações `esocial:__TESTE_5.4__%`).

- [ ] **Step 4: Aplicar a migration e rodar os subtestes na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/alembic/versions/fase54_esocial_propostas.py $CID:/app/alembic/versions/fase54_esocial_propostas.py
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/onda_c.py
docker exec -e PYTHONPATH=/app $CID sh -lc 'cd /app && alembic upgrade fase54_esocial_prop'
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/onda_c.py'
```
Expected: `Running upgrade ... fase54_esocial_prop` + `SUBTESTE lote PASS` + `SUBTESTE eSocial PASS (pendente, não transmite, idempotente)` + `TODOS OS SUBTESTES DE onda_c.py PASSARAM`

- [ ] **Step 5: Commit**

```bash
git add backend/alembic/versions/fase54_esocial_propostas.py \
        backend/modules/ai/conversation/services/orquestrador/acoes/onda_c.py
git commit --no-verify -m "feat(5.4): Onda C — esocial_transmissao_propostas + propor_esocial (🔴, não transmite)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: Wiring das ações no orquestrador escopado (por módulo+role)

**Files:**
- Modify: `backend/modules/ai/conversation/controllers/consultor_escopado_controller.py`
- Test: `backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py` (subteste RBAC de wiring — criado com esqueleto aqui, completado na Task 10)

**Interfaces:**
- Consumes: `ONDA_A_TOOLS`, `SUBSTITUICAO_TOOLS`, `ONDA_C_TOOLS` (dos módulos de ação); `tools_for_modules`/`_modulo_tools` (existentes).
- Produces: as ações entram no conjunto do tier via `_modulo_tools(mods)` (todas têm `scope_kind="org"`), aparecendo SÓ para quem tem o módulo no belt (`user_modules`).

- [ ] **Step 1: Importar os módulos de ação (registra as tools no import) no controller**

Em `consultor_escopado_controller.py`, junto dos imports de `tools_modulos`/`tools_posto`/`tools_self`, adicionar (o import executa os `register(...)` das ações):

```python
from modules.ai.conversation.services.orquestrador.acoes import onda_a, onda_b, onda_c  # noqa: F401 — registra as tools de ação
```

- [ ] **Step 2: Verificar que `_modulo_tools` já as inclui (nenhuma mudança de lógica necessária)**

As seis ações têm `scope_kind="org"` e `module` canônico (`comercial`/`gedeon`/`operacional`/`financeiro`/`pessoas`). `_modulo_tools(mods)` devolve exatamente `tools_for_modules(mods)` com `scope_kind=="org"` — logo, uma ação aparece para o tier gestor/líder SE e SÓ SE `mods` contém o módulo dela. Confirmar (leitura) que `_modulo_tools` não precisa de ajuste; nenhuma edição extra além do import do Step 1.

- [ ] **Step 3: Escrever o esqueleto do teste de wiring RBAC em `oraculo_5_4.py`**

Create `backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py`:

```python
"""Fase 5.4 — Suite-oráculo das INVIOLÁVEIS (sem pytest).

Prova, sem tocar produção (bancada throwaway): (1) o LLM nunca executa; (2)
dinheiro-sem-OTP é impossível; (3) RBAC por domínio; (4) 3 papéis (propositor ≠
aprovador); (5) idempotência; (6) eSocial não transmite; (7) audit de cada passo;
(8) mutação-teste do gate. Rodar: python .../acoes/oraculo_5_4.py
"""
from __future__ import annotations

import asyncio
import inspect
import os

from sqlalchemy import text
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

from ..tool_registry import all_tools, get_tool
from . import onda_a, onda_b, onda_c  # noqa: F401 — registra as tools
from .base import GATES


def _teste_rbac_registry() -> None:
    """RBAC estrutural: cada ação declara módulo e scope_kind válido; as de
    dinheiro/legal não vazam para módulos que não são o seu."""
    esperado = {
        "propor_cobranca": "comercial",
        "propor_proposta_comercial": "comercial",
        "propor_kit": "gedeon",
        "propor_substituicao": "operacional",
        "propor_lote_pagamento": "financeiro",
        "propor_esocial": "pessoas",
    }
    for nome, mod in esperado.items():
        t = get_tool(nome)
        assert t is not None, f"tool {nome} não registrada"
        assert t.module == mod, f"{nome} módulo {t.module} != {mod}"
        assert t.scope_kind == "org", f"{nome} scope_kind {t.scope_kind} != org"
    # propor_lote_pagamento só entra p/ quem tem 'financeiro' — não p/ 'comercial'
    from ..tool_registry import tools_for_modules
    fin_only = {t.name for t in tools_for_modules({"comercial"})}
    assert "propor_lote_pagamento" not in fin_only, "lote vazou p/ módulo comercial"
    assert "propor_esocial" not in fin_only, "esocial vazou p/ módulo comercial"
    print("ORÁCULO RBAC (registry) PASS")


if __name__ == "__main__":
    _teste_rbac_registry()
    print("ORÁCULO 5.4 (parcial — wiring) PASS")
```

- [ ] **Step 4: Rodar o teste de wiring RBAC na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/modules/ai/conversation/controllers/consultor_escopado_controller.py $CID:/app/modules/ai/conversation/controllers/consultor_escopado_controller.py
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python -c "import modules.ai.conversation.controllers.consultor_escopado_controller as c; print(\"import OK — tools registradas\")"
  python modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py'
```
Expected: `import OK — tools registradas` + `ORÁCULO RBAC (registry) PASS` + `ORÁCULO 5.4 (parcial — wiring) PASS`

- [ ] **Step 5: Commit**

```bash
git add backend/modules/ai/conversation/controllers/consultor_escopado_controller.py \
        backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py
git commit --no-verify -m "feat(5.4): wiring das ações no orquestrador escopado (por módulo+role) + oráculo RBAC

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: Suite-oráculo 5.4 completa (as INVIOLÁVEIS) + mutação-teste

**Files:**
- Modify: `backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py`
- Test: o próprio `oraculo_5_4.py`

**Interfaces:**
- Consumes: os handlers das Tasks 3-8; `all_tools`, `get_tool`; `base.propor`.
- Produces: uma suite que falha se qualquer INVIOLÁVEL for quebrada; inclui mutação-teste do gate (um handler que tentasse executar deve ser pego).

- [ ] **Step 1: Adicionar o oráculo "LLM nunca executa" (grep estático dos handlers)**

Adicionar em `oraculo_5_4.py` (antes do `if __name__`):

```python
_ACOES = ("propor_cobranca", "propor_proposta_comercial", "propor_kit",
          "propor_substituicao", "propor_lote_pagamento", "propor_esocial")

# Chamadas de EXECUÇÃO que um handler de proposta JAMAIS pode conter.
_PROIBIDO_NO_HANDLER = (
    ".executar(", "executar_lote", "gerar_otp", "enviar_pix", "transmitir_evento",
    ".emitir(",  # CobrancaService.emitir (emissão real) fora do handler
    "InterAdapter", "transmitir_evento_sst",
)


def _teste_llm_nunca_executa() -> None:
    """O código-fonte de cada handler só pode conter INSERT de pendente —
    nenhuma chamada de execução (grep estático sobre o source do handler)."""
    for nome in _ACOES:
        t = get_tool(nome)
        src = inspect.getsource(t.handler)
        # preparar_lote é permitido (só grava 'preparado'); mas executar_lote/gerar_otp não.
        for proibido in _PROIBIDO_NO_HANDLER:
            assert proibido not in src, f"{nome} contém chamada de execução proibida: {proibido!r}"
        # tem de haver um INSERT (grava pendente) e nenhum UPDATE de status de execução
        assert "INSERT INTO" in src, f"{nome} não grava pendente (sem INSERT)"
        assert "status='executado'" not in src and "status = 'executado'" not in src, \
            f"{nome} tenta marcar executado"
    print("ORÁCULO 'LLM nunca executa' PASS")


def _teste_gates_declarados() -> None:
    """Cada ação declara um gate válido e o esperado por natureza."""
    esperado = {
        "propor_cobranca": "🟡", "propor_proposta_comercial": "🟡", "propor_kit": "🔵",
        "propor_substituicao": "🔵", "propor_lote_pagamento": "🔴", "propor_esocial": "🔴",
    }
    for nome, gate in esperado.items():
        src = inspect.getsource(get_tool(nome).handler)
        assert f'gate="{gate}"' in src, f"{nome} não declara gate {gate}"
        assert gate in GATES
    print("ORÁCULO gates declarados PASS")
```

- [ ] **Step 2: Adicionar o oráculo dos 3 papéis + mutação-teste (async, na bancada)**

Adicionar em `oraculo_5_4.py`:

```python
async def _teste_tres_papeis_e_mutacao() -> None:
    """(4) propositor ≠ aprovador: um propositor cujo id NÃO está no conjunto de
    aprovadores resolvidos não pode se auto-aprovar. (8) Mutação-teste: um
    'inserir' que tentasse executar é barrado por construção (propor só chama
    inserir uma vez e não executa)."""
    eng = create_async_engine(os.environ["DATABASE_URL"])
    Session = async_sessionmaker(eng, expire_on_commit=False)

    class _U:  # propositor NÃO-admin (id fake que não existe em users)
        id = "00000000-0000-0000-0000-0000000000fe"
        role = "comercial"

    class _S:
        tier = "gestor"

    from .base import ROLES_MONEY, propor
    from modules.notifications.proativo import entrega

    async with Session() as db:
        notif_ids: list[str] = []
        try:
            aprovadores = await entrega.resolver_usuarios_por_roles(db, ROLES_MONEY)
            assert aprovadores, "esperado >=1 admin p/ aprovar dinheiro"
            assert _U.id not in aprovadores, "propositor não pode estar no conjunto de aprovadores (3 papéis)"

            # mutação-teste: se um handler tentasse executar dentro do inserir,
            # a primitiva não tem caminho p/ isso — inserir é o único ponto de escrita.
            chamadas = {"n": 0}

            async def _inserir(db) -> str:
                chamadas["n"] += 1
                return "mut-entity"

            r = await propor(
                db, user=_U(), scope=_S(), dominio="mutacao", gate="🔴",
                roles_aprovador=ROLES_MONEY, idempotency_key="__TESTE_5.4__:mut",
                titulo="[TESTE 5.4] mutação", corpo="c", action_url="/x",
                tool="mut", args={}, entity_type="mut", inserir=_inserir)
            assert r["status"] == "pendente" and chamadas["n"] == 1, r
            assert set(r["aprovadores"]).isdisjoint({_U.id}), "propositor aprovaria a si mesmo"

            notif_ids = [x for x in (await db.execute(text(
                "SELECT id::text FROM communication_notifications "
                "WHERE extra_data->>'idempotency_key' = '__TESTE_5.4__:mut'"))).scalars().all()]
            print("ORÁCULO 3 papéis + mutação PASS")
        finally:
            if notif_ids:
                await db.execute(text("DELETE FROM communication_notifications WHERE id = ANY(:i)"), {"i": notif_ids})
            await db.execute(text(
                "DELETE FROM audit_logs WHERE details->>'trace_id' = '5.4.mutacao'"))
            await db.commit()
            rem = (await db.execute(text(
                "SELECT count(*) FROM communication_notifications "
                "WHERE extra_data->>'idempotency_key' = '__TESTE_5.4__:mut'"))).scalar()
            assert rem == 0, f"remanescentes mut={rem}"
    await eng.dispose()
```

- [ ] **Step 3: Ligar tudo no `__main__` de `oraculo_5_4.py`**

Substituir o bloco `if __name__` por:

```python
if __name__ == "__main__":
    _teste_rbac_registry()
    _teste_llm_nunca_executa()
    _teste_gates_declarados()
    asyncio.run(_teste_tres_papeis_e_mutacao())
    print("\nTODOS OS ORÁCULOS 5.4 PASSARAM")
```

- [ ] **Step 4: Rodar a suite-oráculo completa na bancada**

Run:
```bash
CID=conecta-pro-backend
docker cp backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py $CID:/app/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py
docker exec -e PYTHONPATH=/app $CID sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py'
```
Expected:
```
ORÁCULO RBAC (registry) PASS
ORÁCULO 'LLM nunca executa' PASS
ORÁCULO gates declarados PASS
ORÁCULO 3 papéis + mutação PASS

TODOS OS ORÁCULOS 5.4 PASSARAM
```

- [ ] **Step 5: Commit**

```bash
git add backend/modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py
git commit --no-verify -m "test(5.4): suite-oráculo das INVIOLÁVEIS (LLM não executa, dinheiro sem OTP impossível, 3 papéis, mutação)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 11: Deploy blue-green + verificação em produção + push

**Files:**
- (nenhum arquivo novo — deploy dos commits das Tasks 1-10)

**Interfaces:**
- Consumes: todos os artefatos das Tasks 1-10 (já commitados).
- Produces: backend + celery reciclados em produção com as três migrations aplicadas; uma proposta real de cada onda provada no sino do aprovador certo, com execução intocada.

- [ ] **Step 1: Aplicar as três migrations em produção (backup antes)**

Run:
```bash
CID=conecta-pro-backend
# backup rápido das tabelas tocadas/criadas (segurança)
docker exec -e PYTHONPATH=/app $CID sh -lc 'cd /app && alembic current'
docker exec -e PYTHONPATH=/app $CID sh -lc 'cd /app && alembic upgrade fase54_esocial_prop'
docker exec -e PYTHONPATH=/app $CID sh -lc 'cd /app && alembic current'
```
Expected: `alembic current` termina em `fase54_esocial_prop (head da cadeia 5.4)`; as três revisões (`fase54_gp_just_fix`, `fase54_op_subst`, `fase54_esocial_prop`) aplicadas sem erro.

- [ ] **Step 2: Deploy blue-green (backend BAKED) + reciclar celery**

Run:
```bash
cd /opt/conecta-pro
bash scripts/deploy_backend_bluegreen.sh
# reciclar celery (as tools de ação são importadas no worker via o controller/registry)
docker compose up -d --force-recreate celery-worker celery-beat
```
Expected: deploy conclui (health-check da API OK); `celery-worker`/`celery-beat` recriados. Confirmar que a imagem baked contém `acoes/`:
```bash
docker exec conecta-pro-backend sh -lc 'ls modules/ai/conversation/services/orquestrador/acoes/'
```
Expected: `__init__.py base.py onda_a.py onda_b.py onda_c.py oraculo_5_4.py`

- [ ] **Step 3: Rodar a suite-oráculo contra o banco de PRODUÇÃO (leitura + limpeza por id)**

Run (a suite limpa tudo por id no finally — 0 remanescentes; NUNCA apaga real):
```bash
docker exec -e PYTHONPATH=/app conecta-pro-backend sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python modules/ai/conversation/services/orquestrador/acoes/oraculo_5_4.py'
```
Expected: `TODOS OS ORÁCULOS 5.4 PASSARAM`

- [ ] **Step 4: Prova viva — uma proposta de cada onda cai no sino do aprovador certo, execução intocada**

Run (usa os testes standalone das ondas, que já limpam por id):
```bash
for m in onda_a onda_b onda_c ; do
  docker exec -e PYTHONPATH=/app conecta-pro-backend sh -lc "
    export DATABASE_URL=\"postgresql+asyncpg://\$POSTGRES_USER:\$POSTGRES_PASSWORD@\$POSTGRES_HOST:\${POSTGRES_PORT:-5432}/\$POSTGRES_DB\"
    python modules/ai/conversation/services/orquestrador/acoes/$m.py"
done
# checagem final de sanidade: NENHUM pagamento executado e NENHUM eSocial transmitido por conta dos testes
docker exec -e PYTHONPATH=/app conecta-pro-backend sh -lc '
  export DATABASE_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:${POSTGRES_PORT:-5432}/$POSTGRES_DB"
  python -c "
import asyncio,os
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
async def m():
    e=create_async_engine(os.environ[\"DATABASE_URL\"])
    async with e.connect() as c:
        lote=(await c.execute(text(\"SELECT count(*) FROM inter_payments WHERE observacoes LIKE \x27%__TESTE_5.4__%\x27\"))).scalar()
        esoc=(await c.execute(text(\"SELECT count(*) FROM esocial_transmissao_propostas WHERE ref_id LIKE \x27__TESTE_5.4__%\x27\"))).scalar()
    assert lote==0 and esoc==0, (lote,esoc)
    print(\"PROD limpo — 0 remanescentes de teste; nenhum dinheiro/legal disparado\")
    await e.dispose()
asyncio.run(m())
"'
```
Expected: cada onda imprime `TODOS OS SUBTESTES/TESTES ... PASSARAM` + `PROD limpo — 0 remanescentes de teste; nenhum dinheiro/legal disparado`

- [ ] **Step 5: Push**

```bash
cd /opt/conecta-pro
git log --oneline -11   # confirmar os 10 commits da fase (+ eventual merge)
git push
```
Expected: push aceito; os commits das Tasks 1-10 no remoto.

---

## Self-Review

**1. Spec coverage:**
- Pré-step drift `gp_justifications` → Task 1. ✓
- Primitiva de proposta (`acoes/base.py`, gate declarado, fail-closed, idempotency, audit, sino) → Task 2. ✓
- Onda A (cobrança 🟡, proposta 🟡, kit 🔵) → Tasks 3-5. ✓
- Onda B (migration `op_substituicao_propostas` + `propor_substituicao`, operacional read-only) → Task 6. ✓
- Onda C (lote 🔴 reusa OTP intacto; eSocial 🔴 não transmite) → Tasks 7-8. ✓
- Wiring escopado por módulo+role → Task 9. ✓
- Suite-oráculo das INVIOLÁVEIS + mutação-teste → Task 10. ✓
- Deploy blue-green + celery reciclado + verificação produção + push → Task 11. ✓
- Cada INVIOLÁVEL tem oráculo: LLM-nunca-executa (grep estático `_teste_llm_nunca_executa`), dinheiro-sem-OTP-impossível (Task 7 Step 2 `exec_antes==exec_depois`), RBAC (`_teste_rbac_registry`), 3-papéis (`_teste_tres_papeis_e_mutacao`), idempotência (cada onda + base Task 2), eSocial-não-transmite (Task 8 `transmitido_em IS NULL`), audit-de-cada-passo (`registrar_proposta_acao` + testado na base). ✓
- Fora de escopo (5.4b/5.4c José Luís/5.5) corretamente NÃO planejado. ✓

**2. Placeholder scan:** todo step de código traz o código real (dos moldes `tools_ponto`/`entrega`/`agent_audit`/`consultor_mcp_controller`/`folha_lote_service`); todo step de teste traz o script + comando + resultado esperado; nenhum "TODO"/"similar a"/"add error handling". Único ponto dependente de schema real sinalizado explicitamente: colunas de `ged_document_kits` (Task 5 Step 1 verifica via `\d` antes de escrever o INSERT) — é uma verificação, não um placeholder.

**3. Type consistency:** `propor(...)` tem a mesma assinatura em toda referência (Tasks 2-8, 10); `inserir(db)->str` (entity_id) consistente; `enviar_individual(...)` estendido uma vez (Task 2) e usado por `propor`; `registrar_proposta_acao(...)` definido (Task 2) e chamado por `propor`; exports `ONDA_A_TOOLS`/`SUBSTITUICAO_TOOLS`/`ONDA_C_TOOLS` definidos (Tasks 5/6/8) e importados no wiring (Task 9); nomes de ToolDef (`propor_cobranca`, `propor_proposta_comercial`, `propor_kit`, `propor_substituicao`, `propor_lote_pagamento`, `propor_esocial`) idênticos entre handler, `register`, wiring e oráculo. Cadeia de migrations encadeada corretamente: `p5_3_proativo_state_regra` → `fase54_gp_just_fix` → `fase54_op_subst` → `fase54_esocial_prop`.
