# RE-REBUILD#2 — Durabilização (SQLi fix + tool use do agente)

- **Data:** 2026-06-09
- **Servidor:** `srv1134814` (`82.25.75.74`)
- **Objetivo:** Bakar na imagem do backend as 2 mudanças que rodavam só via `docker cp` (revertiam num recreate).
- **Imagem nova:** `conecta-pro-backend:latest` = **`5958168f7f82`**
- **Veredito:** ✅ Completo. 9 containers na imagem nova, healthy, gates 200/401. As 2 correções agora **duráveis** (recreate não reverte mais).

---

## Mudanças durabilizadas
1. **SQLi fix** — `contact_controller.py` (bind params nas linhas 57/183). Commit `417d1a75`.
2. **Tool use do agente** — `agent_service.py` (`consultar_cnpj` + `buscar_cliente` + loop de tool-calling). Commit `ba7f2559`.

## Execução (3 blocos, com gates entre eles)

### Bloco 1 — backup + tag rollback + build + validação efêmera (NÃO tocou produção)
- **Backup do banco:** `backups/rebuild/pre_rerebuild2_20260609_0341.dump` (3.8M).
- **Tag de rollback da imagem atual:** `conecta-pro-backend:pre-rerebuild2-20260609` = `f0ee38d1a48a`.
- **Build:** `conecta-pro-backend:rebuild-20260609` = `5958168f7f82` (sem tocar `:latest`).
- **Validação efêmera** (`docker run --network none`): tool use **PRESENTE**, SQLi fix **PRESENTE**, f-string vulnerável **REMOVIDA**, imports `agent_service`+`contact_controller` **OK**.

### Bloco 2A — swap + recreate backend
- `:latest` → `5958168f7f82`. Backend recriado (`up -d --no-deps --no-build backend`).
- Correções no container: tool use **OK**, SQLi fix **OK**.
- **Gates pós-healthy:** `/health` **200** · webhook token válido **200** · **webhook token errado 401** (auth intacta).
- ⚠️ Houve um **502 transitório** medido aos 10s (app ainda `starting`); após `healthy` voltou a **200**. Sem downtime real além do recreate.

### Bloco 2B — celery + flower na imagem nova
- Recriados: `celery-beat`, `celery-operacional`, `celery-nfse`, `celery-sefaz`, `celery-batch`, `celery-priority`, `celery-integrations`, `flower`.
- **Todos os 9 containers** = `5958168f7f82`, `status=running`.
- **Gate final:** `/health` **200**.

## Estado final
| Container | Imagem | Status |
|-----------|--------|--------|
| backend + 7 celery + flower (9) | `5958168f7f82` | running / healthy |

- `AGENT_ENABLED=true` (copiloto). `cwi_message_log` = 24 linhas (intacto).
- Webhook secret ativo inalterado (`8ab9e3…`) — entrada não quebrou.

## Rollback disponível
- Imagem: `pre-rerebuild2-20260609` = `f0ee38d1` (anterior) · `pre-rerebuild-20260608` = `47b3f7` · `pre-rebuild-20260608` = `bfa169a2`.
- Banco: `pre_rerebuild2_20260609_0341.dump`.
- Reverter: `docker tag conecta-pro-backend:pre-rerebuild2-20260609 conecta-pro-backend:latest` + recreate.

## Observação
- O `compose up` emitiu warning de **orphan containers** (staging/celery) — benigno (efeito de `--no-deps` por serviço); nenhum container indevido foi tocado.

---

## Resumo
- 2 correções (SQLi `417d1a75` + tool use `ba7f2559`) **bakadas e duráveis**. ✅
- 9 containers na imagem nova `5958168f`, healthy, gates 200/200/401. ✅
- Backup + tags de rollback no lugar. ✅
- **Fim das pendências de rebuild** — recreate agora é seguro.

*Re-rebuild#2 completo.*
