# Diagnóstico — Por que `opportunities` tem 0 registros
## STEP-0-OPP-DIAG · §13.1 (Chesterton) · §13.5 (dado real) · §13.3 (escopo: só opportunities)

**Data:** 2026-06-23
**Natureza:** INVESTIGAÇÃO READ-ONLY — nenhum INSERT/UPDATE/DELETE/DDL. O único acesso ao banco foram
`SELECT COUNT` e um `GET` HTTP (leitura). Nada foi criado nem consertado.
**Branch:** `fix/crm-qa-aprovado-20260614` (git repo confirmado). Working-tree tem 436 arquivos
modificados/untracked — todos da sessão atual (marketing + builder de proposta) ou runtime de agente
(CLAUDE.md, snapshots cto). **Nenhum arquivo `opportunity*` está modificado** → o código investigado está
em estado committed/limpo (não há edição pela metade). Prosseguido.

---

## VEREDITO CENTRAL: **(b) EXISTE, mas NUNCA FOI USADO.**

Não é falta de código (não é "a"). Não é bug que impede criar (não é "c"). O fluxo de oportunidade está
**inteiro e funcional** — backend + frontend + rota viva retornando 200 — mas **nada nunca criou uma
oportunidade**, porque (1) **não há fluxo automático** que transforme lead/proposta em oportunidade, e
(2) **ninguém criou manualmente** pelo Kanban. É lacuna de **fiação + adoção**, não de código.

---

## STEP 1 — Backend para criar oportunidade? **SIM (completo).**

| Camada | Arquivo:linha | Evidência |
|---|---|---|
| Model | `backend/modules/crm/models/opportunity.py:64` | `class Opportunity(Base)` + enum `OpportunityStage:31` |
| Schema | `backend/modules/crm/schemas/opportunity.py:31` | `OpportunityCreate` (stage/priority com default), `OpportunityCreateFromLead:40`, `OpportunityResponse:90`, `OpportunityClose:79`, `OpportunityStageUpdate:72` |
| Repository | `backend/modules/crm/repositories/opportunity_repository.py:33` | `async def create(...)` + `create_from_lead:68` |
| Controller | `backend/modules/crm/controllers/opportunity_controller.py:25` | prefix `/opportunities` |
| — POST criar | `opportunity_controller.py:28-29` | `@router.post("")` + `/` |
| — POST from-lead | `opportunity_controller.py:46` | converte lead → oportunidade |
| — GET listar | `:74` · GET id `:136` · PUT `:157` | CRUD |
| — PATCH estágio | `:182` | `/{id}/stage` (mover de estágio) |
| — POST fechar | `:207` | `/{id}/close` (ganhou/perdeu) |
| — DELETE | `:233` | |
| Registro | `main_production.py:375` + `api/v1/__init__.py:45` | incluído com prefix `/crm` → **`/api/v1/crm/opportunities`** |

**Veredito STEP 1: SIM** — há rota POST de criação (e até `from-lead`), service/repository e CRUD completo.

---

## STEP 2 — Frontend para criar/ver oportunidade? **SIM (Kanban completo).**

- Página: `frontend/src/app/modulos/crm/oportunidades/page.tsx` (476 linhas).
- **Kanban drag-and-drop** entre estágios (`@dnd-kit`: `DroppableColumn`, `KanbanBoard`, `onStageChange`),
  com toggle tabela/kanban e stats por estágio.
- **Criação existe:** `useCreateOpportunity` + `handleCreate → createMutation.mutateAsync({data})`
  (`page.tsx:146,180`), via modal `OportunidadeFormModal` (`components/crm/oportunidade-form-modal`).
- Detalhe: `OportunidadeDetailModal`. Client de API gerado: `types/generated/crm/crm-oportunidades/`.
- **No MENU e acessível:** `config/modules.ts:23` → "Oportunidades" `/modulos/crm/oportunidades`
  (perm `crm:oportunidades`). Admin tem `['*']` (`types/modules.ts:49`) e `hasPermission` libera tudo no
  wildcard (`types/modules.ts:100`) → **o menu aparece para o admin**, não está escondido.

**Veredito STEP 2: SIM** — UI completa (criar + Kanban + detalhe), reachable.

---

## STEP 3 — A rota funciona ou está quebrada? **PROVÁVEL SIM — e confirmado vivo (200).**

1. **Leitura do código de criação** (`opportunity_controller.py:28` → `repository.py:33`): mapeia os
   campos do `OpportunityCreate` para o model; `id=uuid4()`, `stage=data.stage.value`,
   `priority=data.priority.value` (ambos têm default no schema). **Sem campo NOT NULL sem default**
   (title/contact_name/contact_email são obrigatórios no schema; value/probability têm default).
2. **Padrão de bug do lead (response exigindo campo None) — NÃO se aplica aqui:** `OpportunityResponse`
   expõe propriedades calculadas, mas todas são **None-safe**:
   - `weighted_value` (`model:117`) = `value * probability/100` → campos NOT NULL.
   - `is_open/is_won/is_lost` (`:122/131/135`) → derivam de `stage` (NOT NULL).
   - `days_in_pipeline` (`:204`) → guarda `actual_close_date`; usa `created_at.date()` (NOT NULL).
   - `is_overdue` (`:211`) → `if not self.expected_close_date or not self.is_open: return False` (trata None).
3. **Prova viva (read-only GET):** `GET /api/v1/crm/opportunities/` → **HTTP 200**
   `{"items":[],"total":0,"page":1,"page_size":20,"total_pages":0}`. E
   `GET /api/v1/crm/dashboard/charts/opportunities-by-stage` → **200**. A rota está de pé e responde —
   **não está quebrada**; está vazia por falta de dados.

**Veredito STEP 3: PROVÁVEL SIM** — o código aparenta são (None-safe) e a rota responde 200. Criação não
foi exercida (nenhum POST jamais), mas não há bug latente identificado que a impeça.

---

## STEP 4 — Como lead/proposta deveriam virar oportunidade? **A fiação NÃO existe.**

1. **Criação automática: NÃO existe.** `grep opportunit` nos services/controllers/repositories de **lead
   e proposta** retorna apenas `dashboard_service.py` (que só LÊ oportunidades para montar stats —
   `dashboard_service.py:12,28-32,89-90,105`). **Nenhum ponto** cria oportunidade ao qualificar um lead,
   ao criar/aprovar/aceitar uma proposta, ou em qualquer evento. Existe a rota `POST /from-lead`
   (`controller:46`), mas **nada no sistema a chama automaticamente** — só seria por ação manual.
2. **Vínculo real no banco (read-only):**
   - `proposals`: 6 no total, **0 com `opportunity_id`**, **6 com `opportunity_id` NULL**.
   - `contracts`: **0 com `opportunity_id`**.
   - `opportunities`: **0 registros**.
3. **Veredito STEP 4:** a FK `proposals.opportunity_id`/`contracts.opportunity_id` existe, mas **nada
   nunca a popula**. O builder de proposta atual (inclusive o recém-feito) cria proposta com
   `opportunity_id` NULL. O lead (José Luís) vira `lead`, nunca `opportunity`. O elo lead→deal→proposta
   está **desconectado na prática**.

---

## MAPA: o que existe × o que falta (factual)

**EXISTE e funciona:**
- Model + 6 estágios (StrEnum) + propriedades calculadas None-safe.
- Schema completo (Create, CreateFromLead, Update, StageUpdate, Close, Response, Filter, PipelineStats).
- Repository (create, create_from_lead, …).
- Controller CRUD completo + `/stage` + `/close` + `/from-lead`, registrado em `/api/v1/crm/opportunities`.
- Frontend: Kanban drag-drop + modal de criar/editar + detalhe, no menu, visível ao admin.
- Rota viva: GET 200.

**FALTA (fato, não proposta):**
- **Nenhum gatilho automático** lead→oportunidade ou proposta→oportunidade (a `from-lead` existe mas é
  órfã — nada a invoca).
- **Nenhum uso manual** registrado (0 oportunidades criadas pelo Kanban).
- **Propostas não passam por oportunidade** (`opportunity_id` sempre NULL), então o estágio do deal nunca
  é alimentado e o Kanban fica sempre vazio.

---

## CONCLUSÃO (sem propor solução — §13.3)

A tabela está vazia **não por ausência de código nem por bug**, e sim porque **o fluxo nunca foi ligado
ao dia a dia**: a máquina de pipeline (backend + Kanban) está pronta e responde 200, mas (a) nenhum
evento cria oportunidade automaticamente e (b) ninguém criou manualmente. É **(b) EXISTE mas NUNCA
USADO** — gap de fiação/adoção, não de engenharia.

---

## Metadados
- Steps: 0,1,2,3,4 — todos read-only (SELECT/COUNT + 1 GET HTTP). Nenhuma escrita.
- Evidências: arquivo:linha (código/rota), `pg` counts, e resposta HTTP 200 da rota.
- **Nota de completude da investigação (0–10): 9,5** — cobriu backend (model/schema/repo/controller/
  registro), frontend (página/modal/menu/permissão), prova viva da rota (GET 200), padrões de bug
  (None-safe verificado) e a fiação automática (inexistente) + vínculos no banco. −0,5: não li linha-a-
  linha o corpo de `OportunidadeFormModal` nem de `create_from_lead` (verificados por fiação + rota viva).
- Arquivo: `/opt/conecta-pro/auditoria/OPP_DIAG_2026-06-23.md`
